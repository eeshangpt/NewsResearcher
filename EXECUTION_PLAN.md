# Execution Plan
## Multi-Agent News Research & Bias-Aware Briefing System

**Companion documents:** PRD.md, TRD.md, news_research_arch.md
**Repo state at time of writing:** blank scaffold — stub `main.py`, empty `pyproject.toml` (no deps), empty `README.md`, `uv`-managed `.venv`. No source code, tests, or config exist yet.
**Package manager:** `uv` (add deps via `uv add`, dev deps via `uv add --group dev`).
**Embeddings default:** local `sentence-transformers` (via `langchain-huggingface`), chosen over OpenAI embeddings API for cost — configurable/swappable.
**Local infrastructure:** Docker Compose is now required for local dev (Postgres for app persistence + a self-hosted Langfuse stack). This is a deliberate departure from the TRD's original "SQLite: zero-ops" rationale, traded for local LLM-call tracing/debugging — see Cross-Cutting Concerns below.

---

## Cross-Cutting Concerns (established in Phase 0, threaded through every phase)

These aren't phases themselves — they're foundations every later phase depends on, so they must exist before Phase 1 starts and stay consistent afterward.

- **Config system (NFR-5)**: one `Settings` object (`pydantic-settings` `BaseSettings`) loaded from `.env` (secrets/connection strings) + optional `config.yaml` (tunables), env vars always override file. Structure as nested settings, not a flat bag: `Settings.pipeline.max_subtopics`, `Settings.reputation.staleness_days` / `min_score_threshold`, `Settings.clustering.similarity_threshold` / `subtopic_match_threshold`, `Settings.models.{subtopic,claim_extraction,summarization,bias_framing,briefing}`, `Settings.embeddings.backend` (`"local"` default | `"openai"`). Every agent/module reads from this object — never hardcodes a constant that's listed as configurable.

- **LLM layer — full LangChain idioms (NFR-6)**: `llm/models.py` holds factory functions returning LangChain objects — `get_chat_model(stage) -> BaseChatModel` (`ChatOpenAI` via `langchain-openai`, model name from `Settings.models.<stage>`) and `get_embeddings() -> Embeddings` (`HuggingFaceEmbeddings` for the local default, `OpenAIEmbeddings` as the swappable alternate — both behind the same `langchain_core.embeddings.Embeddings` interface). `llm/prompts/*.txt` stay plain template files (vendor-agnostic content), loaded into `ChatPromptTemplate.from_template()` at call time. `llm/schemas.py` holds pydantic output schemas; call sites use `model.with_structured_output(Schema)` instead of hand-rolled JSON parsing. LangGraph nodes call these LangChain runnables directly — LangGraph orchestrates control flow (gates, fan-out, sequencing), LangChain handles individual model/prompt construction; LangChain does not replace LangGraph as the orchestrator. Porting vendors later means changing `llm/models.py`'s factories, nothing else.

- **Cost/token logging (NFR-1)**: a custom `observability/cost_callback.py` (`BaseCallbackHandler` subclass) is attached at every top-level `graph.invoke(state, config={"callbacks": [...]})` call — LangChain callbacks propagate automatically through every nested call in every node, so no per-agent instrumentation code is needed. It captures token usage and writes `{run_id, stage, model, input_tokens, output_tokens, estimated_cost, latency_ms}` to a Postgres `run_costs` table, independent of whether Langfuse is reachable (fails soft — same graceful-degradation pattern as the Google News RSS backfill, NFR-3). `persistence/queries.py::cost_summary(run_id)` answers the PRD's "cost per run" success metric directly from the app's own database, without depending on Langfuse's API.

- **Observability / agent tracing & debugging — MLflow + Langfuse, distinct roles (no overlap):**
  - **Langfuse** (self-hosted) is the LLM-call-level tracing/debugging tool — a `langfuse` `CallbackHandler` attached alongside the cost callback at the same top-level graph invocation, so every agent's prompts/responses/latency are traced automatically. Traces are tagged with `run_id` + `subtopic_id` so a Langfuse trace maps back to a specific pipeline run. Self-hosted via Docker Compose (see Phase 0) rather than Langfuse Cloud, per your preference — accessible locally at `http://localhost:3000`.
  - **MLflow** is pipeline/experiment-level tracking, not raw call tracing (avoids duplicating Langfuse's job). Local file-store backend (`./mlruns`), zero extra infra. One MLflow run per pipeline run, keyed by `run_id`: logs config snapshot as params (model choices, `max_subtopics`, reputation weights, clustering thresholds) and the final snapshot JSON as an artifact — useful for comparing config changes across runs, and the natural home for the PRD's flagged v2 eval-harness work (`mlflow.evaluate()`) once a golden dataset exists.

- **Testing approach**: given GDELT/RSS/OpenAI/Postgres/Langfuse are external, non-deterministic, and/or stateful —
  - **Unit-testable now**: reputation scoring formula, dedup logic (URL normalize + title similarity), config loading/precedence, DB schema/queries, clustering functions given fixed embedding vectors, topic canonicalization/hashing, subtopic cross-run matching math, cost-logging arithmetic.
  - **Needs live calls / manual inspection**: GDELT/RSS response shape drift, WHOIS reliability, LLM prompt quality (subtopic proposals, claims, framing labels, briefing text), end-to-end gate flow, Langfuse trace correctness.
  - Tooling: `pytest`, `respx` (mocks `httpx` for GDELT/RSS/WHOIS-HTTP — record real responses once into `tests/fixtures/*.json`, replay offline), `freezegun` (staleness-window tests need controllable "now"), `pytest-mock`, `testcontainers[postgres]` (spins up an ephemeral Postgres container per test session — hermetic, doesn't depend on the dev Docker Compose stack being up, works in CI later). Keep `tests/live/` marked `@pytest.mark.live` for real-API smoke tests — opt-in only, never CI-blocking. `ruff` for lint/format.

- **Dev-time pipeline harness**: Streamlit (Phase 6) is deliberately last, but Phases 2–5 all involve blocking human gates — without a UI there's no way to approve/edit Gate 1 or view the Gate 2 report while building them. Resolve by repurposing `main.py` into a `typer` CLI from Phase 0 onward: `uv run newsresearch run "<topic>"` drives the graph, prints Gate 1 candidates to stdout, accepts approve/edit via stdin, prints the Gate 2 report per subtopic, prompts to continue. Throwaway-quality UX (replaced by Streamlit in Phase 6) but load-bearing for manual validation in every phase between.

---

## Phase 0 — Project Scaffolding

**Goal:** everything later phases assume exists actually exists.

**Directory layout:**
```
docker-compose.yml            # postgres (app db) + Langfuse self-host stack — all in this repo
deploy/
  langfuse/                   # Langfuse self-host compose/config, committed in this repo, not fetched externally
newsresearch/
  __init__.py
  cli.py                     # typer CLI (dev harness + eventual real entrypoint)
  config.py                  # Settings (pydantic BaseSettings)
  llm/
    models.py                 # LangChain chat-model + embeddings factories
    schemas.py                # pydantic models for structured LLM outputs
    prompts/                  # template files per agent
  observability/
    cost_callback.py           # LangChain callback handler -> run_costs (Postgres)
    langfuse_setup.py          # Langfuse CallbackHandler factory, run_id/subtopic_id tagging
    mlflow_setup.py            # MLflow run lifecycle helpers, keyed by run_id
  persistence/
    db.py                    # psycopg connection/pool, init_db(), schema_version check
    schema.sql                # Postgres DDL: TRD section 5 tables + run_costs + schema_version
    queries.py                 # typed read/write helpers, no raw SQL scattered in agents
  graph/
    state.py                  # LangGraph shared state schema (TypedDict/pydantic)
    build.py                  # graph assembly, empty passthrough nodes to start
    nodes/                    # one module per pipeline node
  agents/                     # one module per agent, added phase by phase
  sourcing/
  reputation/
  clustering/
  scheduling/
  data/
    trusted_outlets.yaml       # seed whitelist tier data (Phase 1 needs this)
tests/
  fixtures/
  live/
main.py                        # thin: `from newsresearch.cli import app; app()`
```

**Tasks:**
1. `uv add langgraph langgraph-checkpoint-postgres langchain langchain-core langchain-openai langchain-huggingface pydantic pydantic-settings python-dotenv typer psycopg[binary] psycopg_pool mlflow langfuse` — core deps.
2. `uv add --group dev pytest respx freezegun pytest-mock testcontainers[postgres] ruff` — dev deps.
3. `docker-compose.yml`: a `postgres` service for the app's own database (`newsresearch`), plus Langfuse's self-host services defined in `deploy/langfuse/` (Postgres, ClickHouse, Redis, MinIO, `langfuse-web`, `langfuse-worker`) — all committed directly in this repo, nothing referenced or pulled from outside it. Kept as Langfuse's own isolated stack, not sharing the app's database, so its internal migrations never touch app schema. Document `docker compose up -d` as the required pre-step for local dev.
4. `config.py`: define `Settings` with every NFR-5 tunable stubbed in, even before consumers exist.
5. `persistence/schema.sql`: transcribe TRD section 5 as Postgres DDL (`BYTEA` for `subtopics.embedding`, `TIMESTAMPTZ` for all timestamps) + `run_costs` table + a `schema_version(version INTEGER)` single-row table for lightweight future migrations (`CREATE TABLE IF NOT EXISTS`, bump `schema_version` manually when DDL changes — no Alembic needed at this scale).
6. `persistence/db.py`: `init_db(database_url) -> Connection`/pool via `psycopg_pool`, idempotent schema application. (No WAL-mode workaround needed — Postgres handles concurrent per-subtopic writes during fan-out natively.)
7. `graph/state.py`: top-level state (topic, canonical_topic, run_id, subtopics: list, approved: bool) + per-subtopic sub-state (used by `Send`-based fan-out in Phase 2).
8. `graph/build.py`: wire the full node topology from the TRD diagram as no-op passthrough nodes (Subtopic → Gate1 → fan-out → Sourcing → Clustering → Gate2 → Claims → Summarize → Bias → Briefing → Snapshot → Timeline), compiled with a `PostgresSaver` checkpointer — **not** an in-memory saver, since gates must survive process restarts (this matters again in Phase 6, where Streamlit reruns the script per interaction).
9. `llm/models.py`: chat-model and embeddings factories per the LLM layer design above.
10. `observability/cost_callback.py`, `langfuse_setup.py`, `mlflow_setup.py`: implement per the Cross-Cutting Concerns design; wire both callbacks into `cli.py`'s top-level `graph.invoke(...)` call.
11. `cli.py`: `typer` app, `run <topic>` command invoking the compiled graph end-to-end through the no-op nodes, with callbacks attached.
12. `.env.example`: `OPENAI_API_KEY`, `NEWSRESEARCH_DATABASE_URL`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST` (default `http://localhost:3000`), `MLFLOW_TRACKING_URI` (default `./mlruns`).

**Done when:**
- `docker compose up -d` brings up Postgres (app db) + the full local Langfuse stack.
- `uv run newsresearch run "test topic"` executes the full no-op graph start to finish and exits cleanly.
- `init_db()` applies the schema against Postgres, producing every TRD table + `run_costs` + `schema_version`.
- A stub LLM call through the graph produces a `run_costs` row in Postgres, a visible trace in the local Langfuse UI (`localhost:3000`), and an MLflow run under `./mlruns` — the full observability stack verified end-to-end before any real agent exists.
- `Settings()` loads without error from `.env.example`-shaped input.
- `pytest` (Postgres-backed tests via `testcontainers`) runs and `ruff check` passes.

### Phase 0 — Story/Task Breakdown (PM decomposition)

**Status as of last update: Phase 0 code is complete and merged; one Done-when bullet is BLOCKED pending a human/UI step.** Stories 0.1–0.9 are implemented and merged to `master` (Story 0.7 via PR #6, Story 0.8 + Task 0.7.4 together via PR #7, branch `story/phase0-cli-dev-harness`). `acceptance-verifier` ran the Story 0.10 walkthrough independently (re-deriving from live repo/infra state, not trusting prior self-reports) and returned a **BLOCKED** verdict:
- `docker compose ps` — all 7 services (`postgres`, `langfuse-postgres`, `clickhouse`, `redis`, `minio`, `langfuse-web`, `langfuse-worker`) healthy. **MET.**
- `uv run newsresearch run "..."` exits 0 (re-confirmed independently by the verifier with its own fresh run). **MET.**
- `\dt` against the app Postgres shows all 9 TRD tables + `run_costs` + `schema_version` (`version=2`, per Story 0.7's migration) + the 4 LangGraph checkpoint tables. **MET.**
- `Settings()` loads cleanly from `.env.example`-shaped env vars. **MET.**
- `uv run ruff check .` and `uv run pytest` both pass clean on `master` (30 passed, 1 deselected live test). **MET.**
- The observability triad (one stub LLM call → `run_costs` row + Langfuse trace + MLflow run, all from the *same* run_id): **BLOCKED, not met.** Correction to an inaccurate claim in a prior version of this note: no single run_id has actually been shown to produce all three artifacts together. Every run made from this environment (mine and the verifier's) used an invalid/placeholder Langfuse secret key — no `.env` is committed and Postgres only stores hashed secrets, so no valid key is retrievable headlessly — meaning the Langfuse export correctly soft-fails (`401`, exit 0 anyway) but no trace is produced for those run_ids. Separately, two *real* Langfuse traces do exist in ClickHouse (from earlier agent runs made with a genuine, now-discarded disposable API key), but neither of those run_ids has a matching MLflow run, and their timestamps predate the final merged CLI wiring. The soft-fail behavior itself is correctly verified (twice, independently); the triad-in-one-run claim is not.
  **Action needed to close this out:** log into the Langfuse UI at `http://localhost:3000` (existing accounts: `eeshangpt+langfuse@gmail.com`, or the disposable `backend-agent@example.com`), generate a fresh API key pair, export `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`, run `uv run newsresearch run "<topic>"` once, and confirm that one run_id has a `run_costs` row, a Langfuse trace, and an MLflow run all together. This is a manual/UI step no agent can do headlessly.

The CI-scoping decision below remains a separate, explicitly non-blocking open item, deferred to whenever `project-manager`/`tech-lead` picks it up.

Repo-state check confirmed: no `newsresearch/` package, no `docker-compose.yml`, no `deploy/`, no `tests/` — nothing below has been built yet. The numbered Tasks above are preserved as-is except where a task bundled more than one independently-verifiable outcome; those are split below and cross-referenced back to their original task number for traceability. Splits: original Task 3 (docker-compose.yml) → app-Postgres service and Langfuse stack are separately verifiable; original Task 9 (`llm/models.py`) → `get_chat_model` and `get_embeddings` are separately verifiable; original Task 10 (observability) → three distinct modules plus a separate wiring step.

**Ownership split (per `devops-engineer`'s own scope boundary):** Story 0.2 (`docker-compose.yml` + `deploy/langfuse/`) is `devops-engineer`-owned — infra/containers only, no application code. Every other story (0.1, 0.3–0.9) is `backend-engineer`-owned — this includes the observability *code* (`cost_callback.py`, `langfuse_setup.py`, `mlflow_setup.py`) even though it integrates with devops-provisioned infra; devops owns the containers those modules talk to, not the modules themselves. Several backend tasks have a **runtime** (not build-time) dependency on Story 0.2 actually being up (`docker compose up -d`) rather than merely written — called out explicitly at each task below, since `testcontainers`-backed unit/integration tests are deliberately hermetic and do NOT carry this dependency. CI (running `ruff`/`pytest` in a pipeline) is not itself a Phase 0 task in the list below — `devops-engineer`'s own scope doc flags CI as unscoped in this plan and asks `project-manager` to scope it explicitly rather than have it freelanced; this is an open item, not a silent gap, and is called out again at the end of this breakdown.

**Story 0.1 — Dependency baseline established** ✅ MERGED (`master`)
Acceptance: `uv sync` installs core + dev groups with no resolution errors.
- [x] Task 0.1.1 (orig. Task 1): `uv add langgraph langgraph-checkpoint-postgres langchain langchain-core langchain-openai langchain-huggingface pydantic pydantic-settings python-dotenv typer psycopg[binary] psycopg_pool mlflow langfuse`.
      Acceptance: `pyproject.toml`/`uv.lock` list every named package; `uv sync` exits 0; each imports cleanly in `.venv`.
      Depends on: none
- [x] Task 0.1.2 (orig. Task 2): `uv add --group dev pytest respx freezegun pytest-mock testcontainers[postgres] ruff`.
      Acceptance: dev group lists all named packages; `uv run pytest --version` and `uv run ruff --version` succeed.
      Depends on: none

**Story 0.2 — Local infra (Postgres + self-hosted Langfuse) via Docker Compose** ✅ MERGED (`master`)
Acceptance: `docker compose up -d` brings up app Postgres and the full Langfuse stack, UI reachable at `localhost:3000`.
- [x] Task 0.2.1 (orig. Task 3, split a): `docker-compose.yml` `postgres` service for the app DB (`newsresearch`).
      Acceptance: `docker compose up -d postgres` starts the container; `pg_isready` against it succeeds.
      Depends on: none
- [x] Task 0.2.2 (orig. Task 3, split b): `deploy/langfuse/` self-host stack (Postgres, ClickHouse, Redis, MinIO, `langfuse-web`, `langfuse-worker`), wired into root `docker-compose.yml`, isolated from the app's own Postgres service/volume.
      Acceptance: `docker compose up -d` brings up all Langfuse services; `http://localhost:3000` loads; `docker compose config` shows Langfuse's Postgres as a distinct service/volume from Task 0.2.1's.
      Depends on: none
- [x] Task 0.2.3 (orig. Task 3, doc note): document `docker compose up -d` as the required local-dev pre-step.
      Acceptance: README states the pre-step; `docker compose ps` on a fresh clone shows all services healthy.
      Depends on: 0.2.1, 0.2.2

**Story 0.3 — Config system (`Settings`) loads every NFR-5 tunable** ✅ MERGED (`master`)
Acceptance: `Settings()` instantiates from `.env.example`-shaped input and exposes every nested field named in Cross-Cutting Concerns.
- [x] Task 0.3.1 (orig. Task 4): `config.py` — nested `Settings(BaseSettings)` with every NFR-5 tunable stubbed in.
      Acceptance: unit test instantiates `Settings` from an in-memory env + optional `config.yaml`, asserts every named nested field is present and env vars override yaml.
      Depends on: none
- [x] Task 0.3.2 (orig. Task 12): `.env.example` — `OPENAI_API_KEY`, `NEWSRESEARCH_DATABASE_URL`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST`, `MLFLOW_TRACKING_URI`.
      Acceptance: copying to `.env` and loading via `Settings()` succeeds with no missing-required-field error.
      Depends on: 0.3.1

**Story 0.4 — Postgres schema applies cleanly and idempotently** ✅ MERGED (`master`)
Acceptance: `init_db()` run twice produces every TRD table + `run_costs` + `schema_version`, second run is a no-op.
- [x] Task 0.4.1 (orig. Task 5): `persistence/schema.sql` — TRD section 5 DDL (`BYTEA` embeddings, `TIMESTAMPTZ` timestamps) + `run_costs` + `schema_version`, all `CREATE TABLE IF NOT EXISTS`.
      Acceptance: `psql -f persistence/schema.sql` against an empty DB creates every table with no errors; re-running is a no-op.
      Depends on: none
- [x] Task 0.4.2 (orig. Task 6): `persistence/db.py::init_db(database_url)` via `psycopg_pool`, idempotent schema application.
      Acceptance: `testcontainers`-backed test calls `init_db()`, asserts all tables + `schema_version` row exist; second call doesn't raise.
      Depends on: 0.4.1

**Story 0.5 — LangGraph skeleton compiles and executes as a no-op pipeline with durable checkpointing** ✅ MERGED (`master`)
Acceptance: full TRD node topology exists as passthrough nodes, compiled with `PostgresSaver`, `graph.invoke()` completes without error.
- [x] Task 0.5.1 (orig. Task 7): `graph/state.py` — top-level state + per-subtopic sub-state for `Send`-based fan-out.
      Acceptance: state schema importable; instance constructs with all named fields.
      Depends on: none
- [x] Task 0.5.2 (orig. Task 8): `graph/build.py` — wire full topology (Subtopic→Gate1→fan-out→Sourcing→Clustering→Gate2→Claims→Summarize→Bias→Briefing→Snapshot→Timeline) as no-op nodes, compiled with `PostgresSaver` (not in-memory).
      Acceptance: `graph.invoke(initial_state, config={"configurable": {"thread_id": "test"}})` runs through every node and returns; a checkpoint row exists in Postgres afterward.
      Depends on: 0.5.1, 0.4.2
      Runtime note (backend-engineer, cross-track): the Postgres row check in this acceptance criterion requires the real app Postgres from devops Story 0.2 Task 0.2.1 to be up via `docker compose up -d` — this is distinct from 0.4.2's `testcontainers` tests, which are hermetic and need nothing from Story 0.2.
      Verified: confirmed against the real running dev-compose Postgres (not just testcontainers) — 14 checkpoint rows observed via `psql` for the test thread.

**Story 0.6 — LLM layer factories exist and satisfy the LangChain interface contract** ✅ MERGED (`master`)
Acceptance: `get_chat_model(stage)` returns `BaseChatModel` for every `Settings.models.*` stage; `get_embeddings()` returns an `Embeddings`-interface object for both backends.
- [x] Task 0.6.1 (orig. Task 9, split a): `llm/models.py::get_chat_model(stage) -> BaseChatModel`, model name from `Settings.models.<stage>`, backed by `ChatOpenAI`.
      Acceptance: calling it per stage name returns a `BaseChatModel` constructed with the `Settings`-sourced model name (constructor-arg inspection, no network call in test).
      Depends on: 0.3.1
- [x] Task 0.6.2 (orig. Task 9, split b): `llm/models.py::get_embeddings() -> Embeddings` — `HuggingFaceEmbeddings` for `"local"` (default), `OpenAIEmbeddings` for `"openai"`, both `isinstance(..., langchain_core.embeddings.Embeddings)`.
      Acceptance: backend switch via `Settings.embeddings.backend` returns the correct implementation in each case.
      Depends on: 0.3.1
- [x] Task 0.6.3 (convention scaffold, not separately numbered above): `llm/prompts/` directory + `llm/schemas.py` stub, establishing the convention before Phase 1+ agents add real content.
      Acceptance: `llm/prompts/` exists with at least one example `.txt` template loadable via `ChatPromptTemplate.from_template()`; `llm/schemas.py` exists and imports cleanly.
      Depends on: none
      Note: added `sentence-transformers` as an explicit dependency beyond Story 0.1's original list — required at construction time by `HuggingFaceEmbeddings`, not pulled in transitively as assumed.

**Story 0.7 — Observability stack verified end-to-end through a stub LLM call** ✅ CODE MERGED (`master`, PR #6) — ⚠️ end-to-end acceptance not yet actually demonstrated (see Task 0.7.4)
Acceptance: one stub LLM call through the graph produces a `run_costs` row, a visible Langfuse trace, and an MLflow run — simultaneously, from one top-level `graph.invoke()`.
Note: this was originally built unrequested/out of sequence (ahead of Story 0.8, which Task 0.7.4 depends on); it was reviewed and merged as-is. `cost_callback.py`'s per-token pricing table still has real rates for only two models, placeholders otherwise — not yet trustworthy for real cost figures, tracked as a known gap rather than a blocker.
- [x] Task 0.7.1 (orig. Task 10, split a): `observability/cost_callback.py` — `BaseCallbackHandler` capturing `{run_id, stage, model, input_tokens, output_tokens, estimated_cost, latency_ms}`, writes to `run_costs`, fails soft independent of Langfuse reachability.
      Acceptance: unit test with a mocked LLM call asserts a fully-populated `run_costs` row is written; simulated DB-write failure logs-and-continues, never raises.
      Depends on: 0.4.2, 0.6.1
- [x] Task 0.7.2 (orig. Task 10, split b): `observability/langfuse_setup.py` — `CallbackHandler` factory, tags traces with `run_id` + `subtopic_id`.
      Acceptance: a stub call attached with this handler produces a trace visible at `localhost:3000` tagged with the given `run_id`.
      Depends on: 0.2.2, 0.3.2
- [x] Task 0.7.3 (orig. Task 10, split c): `observability/mlflow_setup.py` — run lifecycle helpers keyed by `run_id`, `./mlruns` file-store backend.
      Acceptance: start/end helpers around a stub invocation produce exactly one MLflow run under `./mlruns` tagged with `run_id`.
      Depends on: 0.3.2
- [x] Task 0.7.4 (orig. Task 10, wiring clause + Task 11's callback half): wire all three callbacks into `cli.py`'s top-level `graph.invoke(state, config={"callbacks": [...]})`.
      Acceptance: `uv run newsresearch run "test topic"` (with a real/stubbed chat-model call in one node) produces all three artifacts from Story 0.7's acceptance in one command.
      Depends on: 0.7.1, 0.7.2, 0.7.3, 0.8.1
      Status: code merged to `master` via PR #7 (branch `story/phase0-cli-dev-harness`), together with Story 0.8. **Acceptance criterion not yet actually demonstrated** — `acceptance-verifier` ran this independently and found that no single run_id has produced `run_costs` + Langfuse trace + MLflow run together: every headless run in this environment lacks a retrievable valid Langfuse secret key (soft-fails correctly, `401`, exit 0, but no trace), while the two real traces that do exist in ClickHouse (from an earlier disposable-key run) have no matching MLflow run and predate this PR's merge. The wiring code itself is verified correct (soft-fail behavior confirmed twice independently, `run_costs`+MLflow confirmed together from one run); only the three-artifacts-from-one-run acceptance check needs a human to close, via a real Langfuse API key generated through the UI. See the section-level status note above for the exact steps.

**Story 0.8 — CLI dev harness runs the compiled no-op graph end-to-end** ✅ MERGED (`master`, PR #7)
Acceptance: `uv run newsresearch run "test topic"` executes the full no-op graph and exits 0.
- [x] Task 0.8.1 (orig. Task 11, minus callback wiring which moved to 0.7.4): `cli.py` — `typer` app, `run <topic>` command invoking the compiled graph; `main.py` reduced to `from newsresearch.cli import app; app()`.
      Acceptance: `uv run newsresearch run "test topic"` invokes `graph.invoke()` on the Phase-0 no-op graph and exits 0.
      Depends on: 0.5.2
      Note: this task's implementation also touches the already-merged `graph/build.py` (Story 0.5) — the `subtopic` node became a small in-file stub `BaseChatModel` (not `llm.models.get_chat_model`, deliberately, so Phase 0 needs no real `OPENAI_API_KEY`) so there's an actual LLM call for Task 0.7.4's callbacks to capture. Flagged explicitly by the implementing agent rather than silently bundled; reviewed and merged as-is.

**Story 0.9 — Testing/lint baseline passes** ✅ VERIFIED (`master`)
Acceptance: `pytest` (testcontainers-backed) and `ruff check` both pass against everything built in Phase 0.
- [x] Task 0.9.1: baseline tests — `Settings` load/precedence, schema idempotency, cost-callback correctness, graph compile+invoke, CLI end-to-end.
      Acceptance: `uv run pytest` exits 0, using `testcontainers[postgres]` (no dependency on the dev compose stack being up).
      Depends on: 0.3.1, 0.4.1, 0.4.2, 0.7.1, 0.5.2
      Verified: 30 passed, 1 deselected (`@pytest.mark.live`) on `master` post-merge.
- [x] Task 0.9.2: `ruff` configuration + clean pass over `newsresearch/` as it stands.
      Acceptance: `uv run ruff check .` exits 0.
      Depends on: 0.1–0.8 code tasks
      Verified: clean on `master` post-merge.

**Story 0.10 — Phase 0 Done-when verified end-to-end (integration/demo)** ⚠️ BLOCKED — 5 of 6 bullets MET, 1 BLOCKED
Acceptance: all six bullets in the phase-level "Done when" block above hold simultaneously on a clean checkout.
- [ ] Task 0.10.1: manual end-to-end walkthrough of the exact Done-when command sequence.
      Acceptance: each bullet confirmed in order; any failure is filed against the specific task above, not against "Phase 0" generally.
      Depends on: 0.1–0.9 all complete
      Verdict (`acceptance-verifier`, run independently against live repo/infra state): docker-compose-healthy, CLI-exits-0, schema-completeness, `Settings()`-loads, and pytest+ruff all **MET**. The observability-triad-in-one-run bullet is **BLOCKED** — see Task 0.7.4's status note and the section-level status note above for the exact gap and the human action needed to close it. **Phase 0 is not fully done** until that one bullet is demonstrated; everything else genuinely is.

**Open item flagged for `project-manager` scoping (not a Phase 0 task, do not silently add it here):** `devops-engineer`'s own scope doc calls out that CI (a pipeline running `uv run ruff check .` and `uv run pytest` on PRs, `testcontainers`-backed, `@pytest.mark.live` excluded) is not yet scoped as its own story anywhere in this plan. Phase 0's "Done when" only requires these commands to pass locally, not that CI exists — so this isn't blocking Phase 0 completion, but it should be decided explicitly (as its own Phase 0 story, or deferred to a later phase) rather than left implicit.

---

## Phase 1 — Sourcing Agent + Reputation Scoring + Caching

**Depends on: Phase 0 complete.** Every task below assumes `Settings` (Task 0.3.1), `init_db`/`persistence/db.py` (Task 0.4.2), the compiled no-op graph (Task 0.5.2), and the `cli.py` skeleton (Task 0.8.1) already exist — Phase 1 tasks name the specific Phase 0 task they build on, not just "Phase 0" generally.

**Rationale (per TRD, preserved intentionally):** validate source quality/coverage before investing in subtopic-discovery/clustering machinery on top of it. Runtime order differs — the Subtopic Agent runs first in the actual graph — this is a deliberate build-order choice to retire sourcing risk first, not a graph reordering.

**Modules:**
- `sourcing/gdelt.py` — GDELT DOC 2.0 client: query by keyword + date range, JSON mode. **Handle the API's 250-record-per-call cap explicitly** — page via repeated queries over sub-windows of the lookback range if a subtopic's article count is expected to exceed that; basic backoff on 429s.
- `sourcing/rss.py` — direct outlet RSS fetch via `feedparser`, filtered by keyword/date.
- `sourcing/google_news_backfill.py` — kept as a structurally separate module (not just a conditionally-called function) so it's obvious from the module boundary that it's non-load-bearing per NFR-3.
- `sourcing/dedup.py` — URL normalization (strip tracking params, trailing slash, scheme-normalize) + exact-match drop; `rapidfuzz` title similarity for cross-source wire-story dupes.
- `reputation/scorer.py` — TRD 4.2 formula exactly: `base_tier_score + w1*norm(domain_age) + w2*norm(backlink_proxy) + w3*norm(gdelt_rss_presence_freq) + w4*legitimacy_flags`, weights loaded from `Settings`, bounded adjustment range enforced (clip, don't sum unbounded).
- `reputation/signals.py` — signal collectors, each independently mockable:
  - GDELT/RSS presence frequency — cheap, derivable from Phase 1's own fetch results, no external dependency — **build and trust this one first**.
  - domain age via `python-whois` — **feasibility risk**: WHOIS is frequently rate-limited/blocked for bulk automated lookups. Fail soft (return `None`/neutral-normalize) rather than raising, and cache aggressively so it's a rare call, not per-run.
  - backlink proxy — **feasibility risk**: there is no simple live-query API for this. The realistic v1 approach is a small pre-downloaded/curated rank list (e.g. Tranco list or Common Crawl host-rank snapshot) shipped as a static data file, not a live network call. Decide this explicitly before writing the collector.
  - HTTPS + about-page heuristic via a plain HEAD/GET (`httpx`).
- `reputation/cache.py` — `domain_reputation` table read/write, staleness-window check (`Settings.reputation.staleness_days`), manual-invalidation function.
- `agents/sourcing_agent.py` — orchestrates: query → backfill-if-below-min-count → dedup → score → filter by `Settings.reputation.min_score_threshold`. Signature: `(keywords: list[str], lookback_days: int) -> list[ScoredArticle]`.
- `data/trusted_outlets.yaml` — seed whitelist (domain → tier), curated manually (Reuters, AP, BBC, Guardian, etc. per TRD example).

**Libraries:** `feedparser`, `httpx`, `python-whois`, `rapidfuzz`, `pyyaml`.

**Tasks (dependency order):**
1. `data/trusted_outlets.yaml` seed list.
2. GDELT client + pagination/backoff handling.
3. RSS client (trusted feeds) + isolated Google News backfill module.
4. Dedup pass over combined results.
5. GDELT/RSS-presence-frequency signal (no external dep — do first).
6. WHOIS + backlink-proxy-snapshot + HTTPS/about-page signals (each with soft-fail).
7. Scoring formula + `domain_reputation` cache read/write (Postgres).
8. Backfill trigger logic: invoke Google News RSS only below `Settings.sourcing.min_primary_article_count`; wrap in try/except that logs-and-continues, never raises (NFR-3).
9. Wire `agents/sourcing_agent.py`.
10. CLI command: `newsresearch dev sourcing-test "<keywords>"` for manual inspection.

**Done when:** given a hardcoded sample subtopic (no Subtopic Agent yet), `agents/sourcing_agent.py` returns a deduplicated, reputation-scored article list from real GDELT/RSS calls; manually spot-check score sanity against 3–5 known-good and known-bad domains; confirm Google News backfill failure (simulate by blocking it) doesn't crash the agent.

### Phase 1 — Story/Task Breakdown (PM decomposition)

The numbered Tasks above are preserved as the task-level breakdown; grouped into stories below with sharper per-task acceptance criteria and explicit cross-phase dependencies. No split was needed — each original task was already a single verifiable unit, apart from adding the Phase-1-library `uv add` step which the original list omitted.

**Story 1.0 — Phase-1 library additions**
Acceptance: `pyproject.toml` gains the Phase 1 libraries and `uv sync` succeeds.
- [x] Task 1.0.1: `uv add feedparser httpx python-whois rapidfuzz pyyaml`.
      Acceptance: all five packages present in `pyproject.toml`; `uv sync` exits 0.
      Depends on: Phase 0 Task 0.1.1 (uv/pyproject baseline)

**Story 1.1 — Trusted-outlet whitelist seed data exists**
Acceptance: `data/trusted_outlets.yaml` exists with domain→tier entries, loadable as a dict.
- [x] Task 1.1.1 (orig. Task 1): author `data/trusted_outlets.yaml` (Reuters, AP, BBC, Guardian, etc. per TRD example).
      Acceptance: file parses via `pyyaml`; each entry maps a domain to a tier string; loader test confirms structure.
      Depends on: none

**Story 1.2 — GDELT DOC 2.0 client fetches and paginates beyond the 250-record cap** ✅ tech-lead approved, ready to merge (branch `task/gdelt-doc-client`)
Acceptance: a query expected to exceed 250 raw results returns a combined list larger than 250 via sub-window paging, with 429 backoff.
- [x] Task 1.2.1 (orig. Task 2, part a): `sourcing/gdelt.py` — single-window keyword+date-range query, JSON mode.
      Acceptance: returns parsed article dicts (title, url, domain, published date) from a real GDELT call under 250 results.
      Depends on: Task 1.0.1 (httpx)
      **POST-MERGE DEFECT FOUND — FIXED (tech-lead, 2026-07-22, discovered during Story 1.10's required live-run review, not a Story 1.2 relitigation; fix landed and re-verified 2026-07-23, commit `36e75a0` on branch `task/dev-sourcing-cli`):** `_build_query()` unconditionally quotes every keyword and OR-joins them with no enclosing parens (`" OR ".join(f'"{keyword}"' for keyword in keywords)`). Verified directly against the real GDELT DOC 2.0 endpoint (bypassing `gdelt.py` entirely, to isolate this from the already-known rate-limiting issue): a single quoted keyword (`"Iran"`) gets a real HTTP 200 with a non-JSON body `"The specified phrase is too short."`; two-plus quoted keywords OR-joined without wrapping parens (`"Iran" OR "nuclear"`) gets a real HTTP 200 with a non-JSON body `"Queries containing OR'd terms must be surrounded by ()"`. Both are genuine GDELT DOC 2.0 query-grammar rejections, not rate-limiting, and `query_window`'s existing `except ValueError` (JSON-parse failure) correctly turns each into a `GDELTError` — which then propagates uncaught out of `sourcing_agent`, per Story 1.2's own approved raise-on-exhaustion design. This has been silently masked in every "verified against real GDELT" review to date (Story 1.9's live test, Task 1.11.2) because their only live coverage used a single-keyword list (`KEYWORDS = ["Iran"]`) wrapped in `except GDELTError: return []` — which cannot distinguish "genuinely rate-limited," "genuinely 0 JSON matches," and "query-grammar rejected" from each other; all three previously-reported "0 GDELT results" observations are equally consistent with this defect having fired instead of a real empty match. Not reopening those approvals (their RSS-side results were independently real and sufficient for what they were verifying), but flagging so the record isn't misread as "GDELT single/multi-keyword queries have been proven to work against the live API" — they haven't. **Required fix:** only quote a keyword when it contains whitespace (quoting exists to force phrase-matching for multi-word phrases; a lone token doesn't need it and GDELT rejects a quoted lone token as too short a "phrase"), and wrap the full OR-joined expression in `(...)` whenever there's more than one term. Re-verify against the real endpoint directly (a scratch script hitting `api.gdeltproject.org` is sufficient) before trusting a mocked/`respx`-based unit test again, since respx fixtures can't catch a query the real API itself rejects.
      **FIX CONFIRMED (tech-lead re-review, 2026-07-23):** `_build_query` now quotes a keyword only when it contains whitespace and wraps the OR-joined expression in `(...)` only when there's more than one term, matching the required fix exactly. Independently re-verified directly against the real GDELT DOC 2.0 endpoint (not the `respx`-mocked unit tests): `Iran`, `"climate change"`, `(Iran OR Russia)`, and `("climate change" OR wildfire)` each returned a real HTTP 200 + genuine parseable JSON body — no "phrase too short" or "must be surrounded by ()" rejection for any of the four single/multi, bare/phrase keyword-list shapes. `tests/test_gdelt.py` replaced the one stale case that encoded the original bug with three cases covering bare-single-token, single-phrase, and multi-term-wrapped behavior. The characterization above (that prior "verified against real GDELT" reviews using single-keyword `except GDELTError: return []` couldn't distinguish rate-limiting from query-grammar rejection from genuine zero-matches) still stands as historical record and is not retroactively changed by this fix — it explains why the defect went undetected for as long as it did.
- [x] Task 1.2.2 (orig. Task 2, part b): pagination over sub-windows when expected volume exceeds 250, plus 429 backoff/retry.
      Acceptance: a lookback range known to exceed 250 raw hits returns >250 combined results via internal sub-window splitting; a simulated 429 triggers backoff-and-retry rather than immediate failure.
      Depends on: 1.2.1

**Story 1.3 — RSS sourcing and Google News backfill exist as structurally separate modules** ✅ tech-lead approved, ready to merge (branch `story/rss-google-news-backfill`)
Acceptance: `sourcing/rss.py` fetches trusted-feed articles by keyword/date; `sourcing/google_news_backfill.py` is independently callable and structurally isolated (NFR-3).
- [x] Task 1.3.1 (orig. Task 3, part a): `sourcing/rss.py` — fetch official RSS feeds for trusted-tier outlets from `data/trusted_outlets.yaml`, filter by keyword/date.
      Acceptance: given a keyword + lookback window, returns article dicts from at least 2 real trusted-outlet RSS feeds, correctly filtered.
      Depends on: 1.1.1, Task 1.0.1 (feedparser)
- [x] Task 1.3.2 (orig. Task 3, part b): `sourcing/google_news_backfill.py` — structurally separate module wrapping Google News RSS fetch.
      Acceptance: has its own public function callable independently of `rss.py`/`gdelt.py`; import-graph check confirms `gdelt.py`/`rss.py` never import from it (only the orchestrator does).
      Depends on: Task 1.0.1 (feedparser/httpx)

**Story 1.4 — Deduplication removes exact-URL and cross-source near-duplicate articles** ✅ tech-lead approved, ready to merge (branch `task/dedup-sourcing`)
Acceptance: `sourcing/dedup.py` collapses normalized-identical URLs and rapidfuzz-similar cross-domain titles (wire-story dupes).
- [x] Task 1.4.1 (orig. Task 4, part a): URL normalization + exact-match drop (strip tracking params, trailing slash, scheme-normalize).
      Acceptance: unit test with `http://x.com/a?utm_source=y` and `https://x.com/a/` confirms only one survives.
      Depends on: Task 1.0.1 (no new dep beyond stdlib/httpx already added)
- [x] Task 1.4.2 (orig. Task 4, part b): `rapidfuzz` title-similarity cross-source dedup.
      Acceptance: unit test with two near-identical titles from different domains above threshold confirms one is dropped; a below-threshold pair both survive.
      Depends on: 1.4.1, Task 1.0.1 (rapidfuzz)
      Reuses `Settings.clustering.similarity_threshold` (pre-provisioned for exactly this, per TRD.md line 88) rather than a new config field.

**Story 1.5 — GDELT/RSS presence-frequency reputation signal (build and trust first, no external dependency)** ✅ tech-lead approved, ready to merge (branch `task/gdelt-rss-presence-frequency`)
Acceptance: `reputation/signals.py`'s presence-frequency collector computes a per-domain frequency score from Phase 1's own fetch results, zero additional network calls.
- [x] Task 1.5.1 (orig. Task 5): presence-frequency signal collector.
      Acceptance: given a fixed list of (domain, source_type) fetch results, returns a normalized per-domain frequency score deterministically; covered by a unit test with fixed input, no live call.
      Depends on: 1.2.2 (GDELT results shape), 1.3.1 (RSS results shape), 1.4.2 (operates on the deduped set, per stated task order)

**Story 1.6 — Domain-age, backlink-proxy, and HTTPS/about-page signals, each soft-failing independently** ✅ tech-lead approved, ready to merge (branch `story/domain-reputation-signals`)
Acceptance: three collectors in `reputation/signals.py` each return a normalized value or neutral/`None` on failure, never raising.

*FLAGGED FEASIBILITY RISKS (carried forward from EXECUTION_PLAN / PRD risk table, not footnotes):*
- **WHOIS rate-limiting**: WHOIS is frequently rate-limited/blocked for bulk automated lookups. The soft-fail path is the primary defense, not a corner case — Task 1.6.1 must not assume WHOIS reliability, and must cache aggressively (feeds Story 1.7's cache) so it's a rare call, not per-run.
- **Backlink-proxy data source — RESOLVED (`tech-lead`):** use the **Tranco list**, not Common Crawl host-rank. Reasoning: Tranco ships as a single canonical, already-`rank,domain`-shaped CSV download, whereas Common Crawl's host-graph product requires joining a separate vertex file against a harmonic-centrality/pagerank file per crawl snapshot with no fixed "top N" cut and no pre-trimmed small slice — an order of magnitude more preprocessing for a noisier signal. Both are free/permissively licensed (Tranco: CC BY/CC BY-SA, attribution required), satisfying the no-paid-data-license constraint.
  - **File**: commit the **top 100,000** rows (not the full 1M) at `newsresearch/data/tranco_top100k.csv` — plain `rank,domain` CSV, no header, Tranco's native format, no transformation needed beyond truncation. Full top-1M is ~22.7MB uncompressed (confirmed by direct download, not estimated); the top-100k slice is ~2.3MB, trivially committable/diffable, no Git LFS needed. Candidate news/outlet domains overwhelmingly fall within a global top-100k rank; the TRD 4.2 formula only needs enough resolution to distinguish "clearly-established" from "not clearly established," not long-tail precision. Add a one-line attribution note (source URL + license) alongside the file, e.g. a sibling `newsresearch/data/tranco_top100k.csv.README`.
  - **Freshness**: this is a frozen artifact, refreshed manually/occasionally by re-running the truncation step against a fresh Tranco download and committing the update — a separate axis from `Settings.reputation.staleness_days`, which governs per-domain *cached score* recomputation, not the snapshot file's own vintage. Task 1.6.2's implementer should note this distinction in `reputation/signals.py`'s docstring so the two aren't conflated.
  - **Normalization**: continuous log-scale decay, not discrete tier buckets (buckets create artificial score cliffs at boundaries): `normalized = clip(1 - log10(rank) / log10(100_000), 0.0, 1.0)` for a domain present in the snapshot (rank 1 → 1.0, rank 10 → 0.8, rank 1,000 → 0.4, rank 100,000 → 0.0). **Neutral value 0.5 (not 0) if the domain is absent from the snapshot** — absence from a global top-100k popularity list is expected/common for legitimate small/regional/niche outlets and shouldn't read as evidence of illegitimacy; this also matches the fail-open convention already used by the other two Story 1.6 signals (WHOIS, HTTPS/about-page), so `reputation/scorer.py` doesn't need per-signal special-casing. If Task 1.11.1's good-vs-bad empirical spot-check later shows this doesn't discriminate well enough, the documented fallback is discrete tiers (top-1k=1.0, top-10k=0.8, top-100k=0.5, absent=0.5) — file that as a tuning follow-up against this specific signal, not against "Phase 1" generally, only if an inversion actually shows up.
  - **Naming caveat**: Tranco is a popularity/traffic rank, not a literal backlink-graph rank. TRD 4.2's formula variable is named `backlink_proxy` — keep that field/variable name as-is (lower churn, `Settings`/scorer already assume it) but add a one-line doc comment clarifying "Tranco combined popularity rank, used as a backlink/authority proxy" so it isn't later "fixed" to expect literal backlink counts.

- [x] Task 1.6.1 (orig. Task 6, part a): WHOIS domain-age collector (`python-whois`), fail-soft, cache-friendly.
      Acceptance: unit test with mocked `python-whois` raising/timing out confirms the collector returns `None`, never an exception; a live/manual spot-check against 2–3 real domains returns a plausible age.
      Depends on: Task 1.0.1 (python-whois)
- [x] Task 1.6.2 (orig. Task 6, part b): backlink-proxy signal from the Tranco top-100k snapshot at `newsresearch/data/tranco_top100k.csv` (decision recorded above) — not a live network call.
      Acceptance: collector loads the committed CSV, returns `clip(1 - log10(rank)/log10(100_000), 0, 1)` for a present domain, `0.5` neutral for an absent one; unit test covers a known-high-rank domain, a known-absent domain, and the log-scale formula's exact output for a fixed rank.
      Depends on: none beyond the resolved decision above (no code dependency on Task 1.0.1 — CSV loading needs no new library)
- [x] Task 1.6.3 (orig. Task 6, part c): HTTPS + about-page heuristic via HEAD/GET (`httpx`).
      Acceptance: unit test mocks an `httpx` response confirming HTTPS presence + about-page hit both contribute a legitimacy flag; a timeout/error case fails soft (flag = neutral, no raise).
      Depends on: Task 1.0.1 (httpx)

**Story 1.7 — Reputation scoring formula + `domain_reputation` cache implement TRD 4.2 exactly** ✅ tech-lead approved, ready to merge (branch `story/reputation-scoring-cache`)
Acceptance: `reputation/scorer.py` computes the TRD 4.2 formula with `Settings`-sourced weights, clipped to a bounded adjustment range; `reputation/cache.py` respects `Settings.reputation.staleness_days`.
- [x] Task 1.7.1 (orig. Task 7, part a): `reputation/scorer.py` — scoring formula, weights from `Settings.reputation`, bounded/clipped adjustment (not summed unbounded).
      Acceptance: unit test with fixed signal inputs + known weights asserts exact formula output; an adversarial extreme-signal combination is confirmed clipped rather than pushing score outside the configured bound.
      Depends on: 1.5.1, 1.6.1, 1.6.2, 1.6.3, Phase 0 Task 0.3.1 (`Settings.reputation.*` weights)
      **Integration checkpoint (tech-lead review, Task 1.5.1):** `get_presence_frequency_scores` deliberately normalizes each domain's distinct-source-type count against the *batch's own* distinct-source-type count, not a fixed universe of 3 (`gdelt`/`rss`/`google_news_backfill`) — reasoned and correct for a single batch (see its docstring), but `domain_reputation` is a cross-run persisted cache (TRD 4.2/4.6: "one row per domain, independent of any single run"), and whether Google News backfill fired in the specific batch that happens to trigger a given domain's staleness-triggered recompute is incidental to that domain's legitimacy (it depends on Story 1.8's primary-article-count threshold for *that day's* subtopic query, not on the domain itself). Concretely: the same domain, with identical GDELT+RSS coverage, can score 1.0 in a batch where backfill never fired and ~0.67 in a batch where backfill fired for unrelated reasons — a scale shift in a cached, long-lived signal caused by batch composition rather than domain quality. Task 1.7.1's implementer must make an explicit choice here (don't let it pass through unexamined): (a) fix the denominator to the known 3-source-type universe so the same coverage always maps to the same value regardless of whether backfill fired, accepting that non-backfill batches structurally can't reach 1.0 on this one signal, or (b) keep the batch-relative denominator and document in `scorer.py` why the resulting run-to-run noise on this one weighted term (`w3`, bounded per the TRD 4.2 formula's ±0.3 adjustment range) is acceptable. Either is defensible; leaving it undecided is not — record whichever is chosen in `scorer.py`'s docstring so it isn't relitigated per-recompute.
      **RESOLVED (implementer's choice, tech-lead verified 2026-07-22):** kept the batch-relative denominator (option b), documented in `reputation/scorer.py`'s module docstring. Tech-lead verified the stated magnitude arithmetically against the actual configured defaults (`weight_presence_frequency=0.05`, `adjustment_bound=0.3`): worst-case swing (batch-relative 1.0 vs. 0.67 for the same domain) costs `0.05 * 0.33 ≈ 0.017` final-score points, ~5.7% of the ±0.3 bound — small and acceptable, as claimed. One wording nit (non-blocking): the docstring's "it self-corrects via `staleness_days`" overstates what actually happens — a stale row's next recompute lands in *some* batch composition, not necessarily a more "correct" one, so it bounds the *duration* of any single noisy value rather than correcting it toward truth. Re-word if this file is touched again; not worth a fix-only PR. If Task 1.11.1's spot-check later surfaces an actual threshold-flip, the fix belongs in `reputation/signals.py::get_presence_frequency_scores`'s denominator per option (a) above, filed against Task 1.5.1/1.6.x, not against this scorer.
- [x] Task 1.7.2 (orig. Task 7, part b): `reputation/cache.py` — `domain_reputation` table read/write, staleness-window check, manual-invalidation function.
      Acceptance: `testcontainers`-backed integration test writes a score, reads it back within the staleness window (cache hit, no recompute), confirms manual invalidation forces recompute; a `freezegun`-controlled clock verifies the staleness-window boundary precisely.
      **Known gotcha (found during Story 1.3, verified by tech-lead 2026-07-22):** `freeze_time` triggers a fresh lazy import of `langfuse.api`'s Pydantic models while time is frozen, and pydantic-core's schema generation chokes on it — confirmed to reproduce only when the full suite runs (langfuse must already be loaded by an earlier test, e.g. `test_config.py`/`test_langfuse_setup.py`, before `freeze_time` does its module-attribute scan); running `tests/test_rss.py` alone does not trigger it, which is why this is easy to miss in isolation. Any test using `freeze_time` alongside langfuse-touching code needs `freeze_time(..., ignore=["langfuse"])`. Since Story 1.3 and this task are now two independent occurrences of the same paper cut, and `tests/conftest.py` does not yet exist: **Task 1.7.2's implementer should add one** with an autouse fixture (or `freezegun.config.configure(default_ignore_list=["langfuse"])` at collection time) so future `freeze_time` users don't each have to rediscover and repeat the `ignore=` kwarg.
      Depends on: Phase 0 Task 0.4.2 (`init_db`/schema for the `domain_reputation` table), 1.7.1

**Story 1.8 — Backfill trigger logic invokes Google News RSS only as a non-load-bearing fallback** ✅ tech-lead approved, ready to merge (branch `task/backfill-trigger-logic`)
Acceptance: Google News RSS is invoked only when primary (GDELT+RSS) results fall below `Settings.sourcing.min_primary_article_count`; a simulated backfill failure never raises out of the sourcing path (NFR-3).
- [x] Task 1.8.1 (orig. Task 8): trigger-threshold logic + try/except wrap that logs-and-continues, never raises.
      Acceptance: unit test with primary-count below threshold confirms backfill is invoked; at/above threshold confirms it is NOT invoked; a test forcing `google_news_backfill` to raise confirms the sourcing path logs and continues with primary-only results.
      Depends on: 1.3.2, Phase 0 Task 0.3.1 (`Settings.sourcing.min_primary_article_count`)

**Story 1.9 — Sourcing Agent orchestrates the full pipeline end-to-end** ✅ tech-lead approved, ready to merge (branch `task/sourcing-agent-orchestrator`)
Acceptance: `agents/sourcing_agent.py(keywords: list[str], lookback_days: int) -> list[ScoredArticle]` — given a hardcoded sample subtopic, returns a deduplicated, reputation-scored, threshold-filtered list from real GDELT/RSS calls.
- [ ] Task 1.9.1 (orig. Task 9): wire `agents/sourcing_agent.py`: query → backfill-if-below-min-count → dedup → score → filter by `Settings.reputation.min_score_threshold`.
      Acceptance: calling it against real GDELT/RSS returns `ScoredArticle` objects each above `Settings.reputation.min_score_threshold`, no duplicate URLs/near-dupe titles present. This is the Phase 1 phase-level Done-when target itself.
      Depends on: 1.2.2, 1.3.1, 1.3.2, 1.4.2, 1.7.1, 1.7.2, 1.8.1
      **Integration checkpoint (tech-lead review, Task 1.4.2) — RESOLVED (tech-lead verified 2026-07-22):** `dedup.py`'s cross-source-only guard compares `article["domain"]` by plain string equality; confirm at wiring time that `gdelt.py` (1.2.x) and `rss.py`/`google_news_backfill.py` (1.3.x) emit `domain` in a consistent normalized form (lowercase, no `www.` prefix) for the same outlet — otherwise the same-domain exemption can silently misfire in either direction. Fix at the article-dict-construction site in the affected sourcing client, not by adding normalization logic to `dedup.py` (keep dedup's input contract a plain string-equality comparison). Also worth an empirical look once real titles flow through: `dedup_by_title_similarity` uses `rapidfuzz.fuzz.ratio` (character-edit-distance); real cross-outlet wire-story headlines often reorder clauses more than they edit characters, so `fuzz.token_sort_ratio`/`token_set_ratio` may catch more true dupes if 1.11.1's spot-check shows under-matching — file that as a tuning follow-up against Task 1.4.2 specifically, not against "Phase 1" generally, same convention as the Tranco signal note above.
      Confirmed: `gdelt.py` lowercases+strips at construction (already correct); `rss.py`'s `domain` values come straight from `OUTLET_RSS_FEEDS`'s dict keys, which are hand-authored already-lowercase/no-`www.` (already correct); `google_news_backfill.py`'s `_domain_from_url` was the one gap — it stripped `www.` but never lowercased the resolved host, so a mixed-case Google News `<source>` href for an outlet also seen via GDELT/RSS (e.g. `WWW.Reuters.COM`) could defeat `dedup.py`'s same-domain string-equality guard in either direction. This is a real, previously-latent bug (not a pretext to touch the file), correctly fixed at the sourcing-client construction site per this checkpoint's own instruction (not by adding normalization logic to `dedup.py`), and covered by a direct regression test (`test_domain_from_url_lowercases_mixed_case_host`).
      **Orchestration wiring — verified correct:** `sourcing_agent()` calls `gdelt.fetch` + `rss.fetch_trusted_rss` → `backfill_trigger.maybe_backfill` → `dedup.dedup` → cache-first reputation scoring (`cache.get_fresh_domain_reputation`, falling back to `signals.*` + `scorer.score_domain` + `cache.write_domain_reputation` on miss) → filter by `Settings.reputation.min_score_threshold`, in that order, and every call site matches the real signature of the already-merged module it calls (checked against source, not assumed). No parallel config/LLM-call/cost-logging path introduced; correctly makes zero LLM calls (Phase 1 has no agentic step) and touches no persistence beyond the already-approved `domain_reputation` cache. No `cli.py` scope creep (that's Story 1.10). The 6 unit tests in `tests/test_sourcing_agent.py` (hermetic `testcontainers` Postgres + mocked network collectors) have real, specific assertions — not "doesn't raise" — covering dedup (near-dupe AP article and the unknown-tier domain both correctly drop, only the wire-tier survivor remains), threshold-filtering, cache-hit-skips-recompute (asserts the three signal collectors are *not* called and the returned score is exactly the pre-written cached value), backfill-triggering (`assert_called_once` + backfilled domain present in the result), and NFR-3 backfill-failure resilience (backfill raises, result still returns the primary-source article).
      **REQUIRED FIX before merge:** `tests/live/test_sourcing_agent_live.py`'s assertions all hold vacuously on an empty result list (`for item in result: ...` and the dedup-uniqueness checks pass trivially when `result == []`) — it never asserts `len(result) > 0`. The implementer's one live run returned zero `ScoredArticle` objects, and as currently written the test would have passed identically whether that was "everything correctly scored below threshold" or "the pipeline is broken and returns nothing" — it demonstrates the code runs without crashing, not the stated acceptance criterion ("returns `ScoredArticle` objects... above threshold"). This is a real gap against this repo's own precedent: `tests/live/test_rss_live.py` deliberately picks a broad, near-certain-to-match keyword (`"the"`) specifically "so this doesn't flake on a quiet news day" and asserts `len(articles) > 0` — the sourcing-agent live test should follow the same convention for at least its RSS leg (its `KEYWORDS = ["artificial intelligence regulation"]` was tuned only for GDELT's single-window-cap safety per `test_gdelt_live.py`'s precedent, not for guaranteeing an RSS hit). Add an explicit non-emptiness assertion and a keyword choice that reliably clears it.
      Tech-lead independently re-ran the live path to check whether this is achievable at all before requiring it: real GDELT genuinely exhausted its 5-retry/155s backoff budget with real HTTP 429s in this sandbox on a narrow single-keyword 3-day window (`GDELTError` propagated, consistent with `gdelt.py`'s own documented rate-limiting behavior — an environment condition affecting GDELT specifically, not a Story 1.9 defect). With `gdelt.fetch` mocked to `[]` and keyword `"Iran"` (confirmed via direct feed inspection to overlap that day's actual BBC/Guardian/NPR/Al Jazeera headlines), the real remaining pipeline (real trusted-RSS fetch, real WHOIS/HTTPS signal collection, real reputation-cache write, real dedup, real threshold filter) returned 14 correctly-deduped, "major"-tier, threshold-clearing `ScoredArticle` objects (scores 0.929–0.955) with no duplicate URLs or (domain, title) pairs. This confirms the pipeline itself is correct and the acceptance criterion is genuinely achievable — the required fix is to the test's rigor, not the implementation.
      **Non-blocking forward note (file against Phase 5 / Story 5.5's NFR-3 re-verification, not "Phase 1" generally):** unlike `google_news_backfill.py`'s failure (wrapped by `backfill_trigger.maybe_backfill`'s soft-fail), a real GDELT retry-exhaustion (`GDELTError`) propagates uncaught out of `sourcing_agent` — and does so *before* `rss.fetch_trusted_rss` even runs, discarding any RSS results that would otherwise have succeeded independently. This matches GDELT's already-approved Story 1.2 design (raise-on-exhaustion) and sits outside NFR-3's literal scope (which names the *backfill-only* source, not GDELT, as what must degrade gracefully), so it is not a Story 1.9 blocker and not something this review reopens. But this review's own live run reproduced a real GDELT 429-exhaustion on a narrow, single-keyword, 3-day window — stronger evidence than a hypothetical that this will recur under a scheduled/unattended run. Worth Phase 5 explicitly deciding whether a scheduled run should tolerate a GDELT outage by degrading to RSS(+backfill-trigger)-only rather than failing the whole run, rather than discovering this the first time a scheduled run happens to land during a GDELT rate-limit window.
      **REQUIRED FIX — RESOLVED (tech-lead verified 2026-07-22, commit `478082e`):** the live test now asserts `len(result) > 0` with an explicit message tying it to the acceptance criterion, switched keyword to `"Iran"` (a broad sustained-topic choice matching `test_rss_live.py`'s own "the"-for-reliability precedent, confirmed via direct feed inspection to overlap real BBC/Guardian/NPR/Al Jazeera headlines), and added assertions that at least one surviving result is wire/major tier. GDELT is still called for real (not mocked) via `_bounded_real_gdelt_fetch`, a thin wrapper around the real `gdelt.fetch` with a smaller test-local retry budget (`max_retries=2, backoff_seconds=5.0`) that swallows only a genuine `GDELTError` (rate-limit exhaustion) to `[]` — a narrowly-scoped accommodation for this sandbox's observed GDELT throttling, not a canned/mocked response and not a silent regression of Story 1.2's raise-on-exhaustion design (the real design is unchanged; only this one test's tolerance for that already-known condition is scoped down). Independently re-ran the live test: real GDELT was genuinely attempted (two real HTTP 429s, backoff, then a real 200 that returned 0 matching articles for the query/window — not a mock), and 15 real RSS articles from BBC/Guardian/NPR/Al Jazeera survived dedup and threshold filtering (`0.929`–`0.955`-range scores), correctly deduped (no duplicate URLs or (domain, title) pairs) and clearing the wire/major-tier assertion. Full suite: 127 passed, 6 deselected; `ruff check .` clean. No regressions. Approved.

**Story 1.10 — Manual dev CLI for sourcing inspection** ✅ tech-lead approved, ready to merge (branch `task/dev-sourcing-cli`)
Acceptance: `newsresearch dev sourcing-test "<keywords>"` runs the sourcing agent and prints a human-readable article+score listing.
- [ ] Task 1.10.1 (orig. Task 10): add `dev sourcing-test` typer subcommand.
      Acceptance: `uv run newsresearch dev sourcing-test "<keywords>"` prints each returned article's URL, domain, and reputation score to stdout; exits 0.
      Depends on: 1.9.1, Phase 0 Task 0.8.1 (typer CLI app must exist to extend)
      **✅ tech-lead approved, ready to merge (branch `task/dev-sourcing-cli`)** (2026-07-23, commit `36e75a0`, supersedes the "approve-with-required-fixes" verdict below — both required fixes confirmed landed and re-verified live.)
      **Structurally sound:** `dev sourcing-test` follows `run`'s established conventions exactly — `Settings()` construction, `database_url` guard via `typer.BadParameter`, pool via `persistence/db.py::init_db`, and is in fact more careful than `run` about pool lifecycle (`run` never closes its pool; this command does, in a `finally`). The call site (`sourcing_agent(keywords.split(), lookback_days, pool=pool, settings=settings)`) matches `sourcing_agent`'s real, already-merged signature exactly. Naive whitespace keyword-splitting and a `--lookback-days` default of 7 are reasonable and honestly documented in `--help`; `lookback_days` isn't a `Settings`-managed constant elsewhere in the codebase (it's a required positional arg on `sourcing_agent` itself), so defaulting it in the CLI is not a violation of the "never hardcode a configurable constant" cross-cutting rule. The `dev` sub-app (`app.add_typer(dev_app, name="dev")`) is clean, idiomatic Typer with no bleed into `run`'s code path. `tests/test_cli.py`'s new tests are correctly scoped to what's actually CLI-layer (arg parsing/splitting, pool guard, output formatting, empty-result message) via a hermetic mock of `sourcing_agent` itself — re-testing `sourcing_agent`'s own internals here would duplicate `tests/test_sourcing_agent.py`, not add value.
      **Required fix 1 (blocking, scoped to `cli.py` only — not `sourcing_agent`):** independently ran `uv run newsresearch dev sourcing-test "Iran"` twice, live, in this branch's own checkout against the real dev Postgres + real GDELT/RSS (per this review's mandate — the implementer's own tests never did this). Both runs crashed with a raw traceback and a **confirmed non-zero exit code (1)**, never printing readable output or a clean "no results" message: run 1 hit the `_build_query` grammar defect above (real HTTP 200, non-JSON rejection body, raised as `GDELTError`); run 2 hit a genuine GDELT rate-limit exhaustion (5 real retries, real 429s, ~155s backoff, raised as `GDELTError`). `sourcing_test` has zero exception handling around the `sourcing_agent(...)` call, so any real GDELT failure — a known, already-accepted characteristic of `sourcing_agent`'s approved design per Story 1.9's forward note — surfaces as an unreadable crash rather than the "exits 0" this story's own acceptance criterion requires. Fix: a narrow `try/except GDELTError` around the `sourcing_agent(...)` call in `cli.py::sourcing_test` that prints a short, readable diagnostic and exits via `raise typer.Exit(code=1)` (or 0, implementer's call, as long as it isn't a bare traceback) — this is dev-UX polish for a manual inspection tool, not a change to `sourcing_agent`'s own soft-fail/hard-fail contract (GDELT's raise-on-exhaustion stays exactly as approved in Story 1.2/1.9; NFR-3 only ever covered the backfill source).
      **Required fix 2 (blocking, filed against Task 1.2.1, not this diff):** see the `_build_query` grammar defect documented above — the CLI is the first real caller to expose it because it's the first thing that forwards free-form, real single/multi-keyword input straight to `sourcing_agent` → `gdelt.fetch` without a test-only `GDELTError`-swallowing wrapper. This needs to land before re-verifying Story 1.10's live acceptance, since right now essentially any realistic keyword input trips it independent of rate-limiting.
      **Next step:** once both are fixed, re-run `uv run newsresearch dev sourcing-test "<keywords>"` live against real GDELT/RSS in this same worktree and confirm readable per-article output (or the clean empty-result message) and exit 0 before marking this "ready to merge."
      **RE-REVIEW (tech-lead, 2026-07-23, commit `36e75a0`): both required fixes confirmed, approved.** `_build_query` now quotes a keyword only when it contains whitespace and wraps the OR-joined expression in `(...)` only when there's more than one term (verified correct by direct code inspection for all five cases: zero keywords still raises `ValueError`; a bare single-word keyword returns unquoted/unwrapped; a single multi-word phrase returns quoted, unwrapped; multiple single-word keywords and mixed multi-word/single-word keywords both return `(...)`-wrapped OR joins). Independently re-verified directly against the real GDELT DOC 2.0 endpoint (not mocked): `Iran`, `"climate change"`, `(Iran OR Russia)`, and `("climate change" OR wildfire)` each got a real HTTP 200 + genuine parseable JSON body (5 articles each) — the "phrase too short" / "must be surrounded by ()" rejections are gone. `cli.py::sourcing_test` now wraps the `sourcing_agent(...)` call in `try/except GDELTError`, printing a readable one-line diagnostic to stderr and exiting via `raise typer.Exit(code=1) from exc` instead of a raw traceback; `sourcing_agent`/`gdelt.py`'s own raise-on-exhaustion design (GDELT is a primary source, outside NFR-3's literal backfill-only scope) is unchanged — this is CLI-layer presentation only. `tests/test_gdelt.py` now has three `_build_query` cases (single bare token, single multi-word phrase, multi-term OR-wrap) replacing the one stale case that encoded the bug; `tests/test_cli.py` adds a `GDELTError`-side-effect case asserting non-zero exit, the diagnostic message, and no `Traceback` in output, using the real `GDELTError` type via a mocked `sourcing_agent`. Full suite: 134 passed, 6 deselected; `ruff check .` clean. No scratch/debug files in the diff (only the four expected files touched). Approved as-is, ready to merge.

**Story 1.11 — Phase 1 Done-when verified end-to-end (integration/demo)**
Acceptance: matches the phase-level Done-when block above.
- [x] Task 1.11.1: manual spot-check pass — run `sourcing_agent` against 3–5 known-good domains (Reuters, AP, BBC, etc.) and 3–5 known-bad/low-quality domains.
      Acceptance: a recorded good-vs-bad score comparison shows good domains consistently scoring higher; any inversion is filed as a defect against Task 1.7.1 (formula) or the relevant Task 1.6.x (signal), not against "Phase 1" generally.
      Depends on: 1.9.1
      **PASS (acceptance-verifier):** good domains 0.904–0.992 (reuters.com, apnews.com, bbc.com, theguardian.com, npr.org — all wire/major tier), bad domains 0.464–0.492 (5 known-low-quality domains, all unknown-tier). Clean separation, no inversions; driven mostly by `base_tier_score`. Non-blocking follow-up filed: reuters.com's HTTPS/about-page check (Task 1.6.3) got a false `https_present=False` because Reuters returns HTTP 401 specifically on `HEAD` requests (anti-bot behavior) — didn't affect ordering here since base tier dominates, but worth a future fix (fall back to GET, or treat non-2xx-non-error as neutral rather than False).
- [x] Task 1.11.2: backfill-failure resilience check — force/block `google_news_backfill` and confirm `sourcing_agent` still returns primary-source results without crashing.
      Acceptance: with backfill forced to raise/timeout, `sourcing_agent(...)` still returns a non-crashing result set built from GDELT+RSS alone — directly re-verifies NFR-3/Task 1.8.1 under real (not just unit-mocked) conditions.
      Depends on: 1.9.1, 1.8.1
      **CLEAN PASS, caveat resolved (re-verified 2026-07-23, post GDELT query-builder fix):** re-run with `min_primary_article_count` forced high (100000) to guarantee backfill triggering, keywords `["glacier", "permafrost"]`, `fetch_google_news_backfill` patched at its real import site (`newsresearch.sourcing.backfill_trigger.fetch_google_news_backfill`) to raise `RuntimeError`. Result: `BACKFILL_CALLED: True`, `sourcing_agent(...)` returned **67 real `ScoredArticle`s, all `source_type: "gdelt"`** (`BY_SOURCE_TYPE: {"gdelt": 67}`) — no crash, no stub. This supersedes the prior "stubbed GDELT" caveat: GDELT's own contribution is now confirmed live and non-empty in a genuine backfill-failure scenario, closing out NFR-3/Task 1.8.1 resilience under fully real conditions with no shortcuts remaining. **Phase 1 is now completely done.**

**Story 1.12 — Harden GDELT from hard-fail to soft-fail, mirroring the existing backfill precedent** ✅ tech-lead approved, ready to merge (branch `story/gdelt-soft-fail-hardening`)

**✅ tech-lead approved, ready to merge (branch `story/gdelt-soft-fail-hardening`)** (2026-07-23, commit `22d38e9`): verified the `gdelt.py` client-boundary fix wraps `httpx.HTTPError` (both `raise_for_status()`'s `HTTPStatusError` and network-level failures from the retry loop's `http_client.get(...)`) into `GDELTError` chained via `from exc`, without re-catching the already-existing rate-limit-exhaustion `GDELTError` raise (confirmed `GDELTError` is not a subclass of `httpx.HTTPError`, so the nested `except httpx.HTTPError` block is correctly scoped). Verified `sourcing_agent.py` wraps only `gdelt.fetch(...)` in its own `try/except GDELTError` (log + continue with `gdelt_articles = []`) while `rss.fetch_trusted_rss(...)` sits outside that block and always runs — this is the actual bug fix (RSS no longer gets discarded when GDELT fails first), not merely "GDELT is now tolerant of its own failures." `cli.py`'s dead `except GDELTError` branch was correctly left in place with an accurate "defensive, not provably unreachable" comment rather than deleted. New tests in `test_gdelt.py` (HTTP 500, network timeout) and `test_sourcing_agent.py` (`test_sourcing_agent_survives_gdelt_failure`, correctly isolated from backfill via `min_primary_article_count=1`) are genuine, not vacuous. `uv run pytest`: 137 passed, 6 deselected. `uv run ruff check .`: clean. `EXECUTION_PLAN.md`'s Story 1.12 checkboxes and Phase 5 Task 5.5.2 addition are in place. No scope creep — the diff is a strict subset of failure-handling change, and correctly defers the GDELT-as-provider question to the separate Flagged Open Decision already on record below.

*Context (why this is being proposed now, not silently added):* this session surfaced real operational pain specific to GDELT DOC 2.0 — aggressive rate-limiting under sustained use, a since-fixed query-grammar bug that was masking as rate-limiting (see Task 1.2.1's post-merge-defect note), and live-verification runs taking 10+ minutes due to real 429 backoff retries. Story 1.9/1.10's own tech-lead reviews already found and *deliberately did not fix* this: `sourcing_agent`'s raise-on-GDELT-exhaustion is Story 1.2/1.9's explicitly **approved** design (GDELT is a primary source, outside NFR-3's literal backfill-only scope) — see the "Non-blocking forward note" on Task 1.9.1 above, which explicitly punts this to Phase 5. This story proposes pulling that forward: not because Phase 5's framing was wrong, but because Phase 2 Story 2.2 (Task 2.2.2) and Story 2.5 (Task 2.5.1) both directly call Phase 1's `sourcing_agent` again for the broad and per-subtopic fetches — meaning every manual CLI spot-check in Phase 2 (Tasks 2.2.2, 2.5.1, 2.8.1) re-exercises this exact hard-fail/slow-retry path, not just Phase 1's. Waiting for Phase 5 to decide this means paying the 10+-minute-live-run tax repeatedly through three more phases of manual verification work first.

**Explicitly out of scope for this story:** whether to keep/replace/drop GDELT as a *provider* — that is a separate, larger decision (technical comparison owned by `tech-lead` in parallel; see the new Flagged Open Decision below). This story only proposes changing *failure-handling*, not the provider choice, and is a strict subset of work that's useful regardless of how the provider question resolves.

Acceptance: a real or simulated GDELT retry-exhaustion (`GDELTError`) no longer propagates uncaught out of `sourcing_agent` — it logs a warning and continues with RSS(+backfill-trigger)-only results, the same soft-fail shape Story 1.8 already established for Google News backfill.
- [x] Task 1.12.1: wrap `sourcing_agent.py`'s `gdelt.fetch(...)` call in `try/except GDELTError`, logging a warning and continuing with `rss_articles` alone on failure. Also fixed the client-boundary gap found during planning: `gdelt.py`'s `query_window()` now wraps `httpx.HTTPError` (HTTP error statuses, timeouts, connection errors) into `GDELTError` too, so it's the single, complete signal for "this GDELT call failed for any operational reason" — not just the two previously-existing raise sites (retry-exhaustion, non-JSON body). `cli.py::sourcing_test`'s `except GDELTError` branch left in place as defensive dead code with a comment explaining it's now unreachable for the GDELT case in normal operation.
      Acceptance: a unit test forcing `gdelt.fetch` to raise `GDELTError` confirms `sourcing_agent(...)` still returns RSS(+backfill-if-triggered)-only results with no crash — the mirror image of the existing Task 1.11.2 backfill-failure test. New `tests/test_gdelt.py` cases confirm HTTP-error-status and network-timeout failures are also re-raised as `GDELTError`. `uv run pytest`: 137 passed, 6 deselected; `uv run ruff check .`: clean.
      Depends on: Task 1.9.1 (`sourcing_agent`, existing), Task 1.8.1 (soft-fail pattern to mirror, existing)
      **Note — this reopens an explicitly-approved design decision, on purpose:** Story 1.2/1.9's raise-on-GDELT-exhaustion behavior was reviewed and approved as-is; this task changes it deliberately, at the user's direction, not as a silent drift. `sourcing_agent.py`'s module docstring updated accordingly.
- [x] Task 1.12.2: extended Phase 5 Story 5.5's acceptance text to explicitly cover GDELT soft-fail (new Task 5.5.2), not only backfill soft-fail.
      Acceptance: Story 5.5's acceptance bullet now names both a simulated backfill failure and a simulated GDELT failure, so Phase 5 doesn't silently forget to re-verify the new soft-fail path under the unattended/scheduled run condition it was originally motivated by.
      Depends on: 1.12.1

---

## Phase 2 — Subtopic Agent + Gate 1 + Topical Clustering Agent + Gate 2

**Rationale:** validate human-in-the-loop UX and clustering quality before adding expensive LLM agents downstream.

**Modules:**
- `clustering/embeddings.py` — thin wrapper over `llm/models.py::get_embeddings()`; `embed(texts: list[str]) -> np.ndarray` convenience for clustering code.
- `clustering/cluster.py` — `cluster(vectors) -> labels`, HDBSCAN primary, KMeans fallback when article count < `Settings.clustering.kmeans_fallback_threshold`.
- `agents/subtopic_agent.py` — LLM proposes N candidates (`llm/prompts/subtopic_propose.txt`, via `get_chat_model("subtopic").with_structured_output(...)`) → broad topic-scoped fetch (reuses Phase 1 `sourcing_agent`, not yet subtopic-filtered) → embed+cluster → reconcile (merge candidates mapping to same cluster, split clusters spanning multiple candidates, drop unsupported candidates) → rank by volume/distinctiveness → truncate to `Settings.pipeline.max_subtopics` → retain excess for "also detected."
- `agents/topical_clustering_agent.py` — coarse per-subtopic clustering, reuses `clustering/cluster.py` and Phase 1's sourcing.
- `graph/nodes/gate1.py`, `graph/nodes/gate2.py` — LangGraph `interrupt()`-based nodes; resume via `Command(resume=...)`. Requires the `PostgresSaver` checkpointer from Phase 0 — verify gate state actually survives a process restart, not just an in-process pause.
- `reports/gate2_report.py` — pure aggregation (cluster sizes, sample headlines, source spread), zero LLM calls.

**Libraries:** `hdbscan`, `scikit-learn`.

**Tasks:**
1. `clustering/cluster.py` HDBSCAN + KMeans fallback.
2. Subtopic-propose prompt + pydantic schema for candidate list.
3. Broad fetch (topic-scoped) via Phase 1 sourcing agent.
4. Embed + cluster broad set; reconciliation logic (merge/split/drop).
5. Rank/cap/excess-retention.
6. `graph/nodes/gate1.py`: interrupt, present candidates + excess, accept approve/edit, re-run reconciliation on edit.
7. Fan-out wiring in `graph/build.py` using LangGraph `Send` per approved subtopic (concurrent branches).
8. `agents/topical_clustering_agent.py` per subtopic.
9. `reports/gate2_report.py` + `graph/nodes/gate2.py` interrupt, per-subtopic blocking.
10. Extend CLI harness: render Gate 1 candidates, accept stdin approve/edit, render Gate 2 report per subtopic.

**Done when:** a real topic string run via the CLI harness produces a reviewable subtopic list at Gate 1; approving proceeds to concurrent per-subtopic sourcing+clustering; Gate 2 shows a correct zero-LLM-cost report per subtopic; kill and restart the process mid-gate to confirm the `PostgresSaver` checkpoint actually resumes correctly (not just that functions work in isolation); confirm the Subtopic Agent's LLM calls appear as traces in the local Langfuse UI.

### Phase 2 — Story/Task Breakdown (PM decomposition)

**Depends on: Phase 0 complete, Phase 1 complete.** Tasks below name the specific prior-phase deliverable they build on (e.g. Phase 1 Task 1.9.1's `sourcing_agent`, Phase 0 Task 0.5.1's state schema), not just "Phase 0/1" generally.

The numbered Tasks above are preserved as the task-level backbone. One prerequisite named only in Modules (the `clustering/embeddings.py` wrapper) is promoted to its own task since Task 1 (`cluster.py`) depends on it but the original list didn't number it separately. Original Task 9 is split (report generation vs. the interrupt node itself), matching the Phase 0/1 precedent of splitting only where a task bundles more than one independently-verifiable outcome.

**Story 2.0 — Phase-2 library additions**
Acceptance: `pyproject.toml` gains `hdbscan` + `scikit-learn`, `uv sync` succeeds.
- [x] Task 2.0.1: `uv add hdbscan scikit-learn`.
      Acceptance: both packages present; `uv sync` exits 0.
      Depends on: Phase 0 Task 0.1.1
      ✅ tech-lead approved, ready to merge (branch `origin/task/clustering-embeddings-wrapper`) — exactly the two named packages added as direct deps; `uv.lock` diff confirms `scikit-learn`/`joblib`/`scipy` were already-present transitive deps (via `sentence-transformers`) promoted to direct, and `hdbscan` is the only genuinely new lock entry. `uv sync` verified clean in a fresh worktree.

**Story 2.1 — Clustering primitives: embeddings wrapper + HDBSCAN/KMeans**
Acceptance: given fixed embedding vectors, `cluster()` returns correct labels via HDBSCAN, falling back to KMeans below `Settings.clustering.kmeans_fallback_threshold` — this is one of the plan's explicitly unit-testable-now items (clustering functions given fixed vectors).
- [x] Task 2.1.1 (named in Modules, not separately numbered above): `clustering/embeddings.py` — thin wrapper, `embed(texts: list[str]) -> np.ndarray` over Phase 0's `get_embeddings()`.
      Acceptance: returns a correctly-shaped array using the `Settings.embeddings.backend`-selected implementation from Phase 0 Task 0.6.2.
      Depends on: Phase 0 Task 0.6.2
      ✅ tech-lead approved, ready to merge (branch `origin/task/clustering-embeddings-wrapper`) — genuinely delegates to Phase 0's `get_embeddings()` via the standard `Embeddings.embed_documents(texts)` interface, no backend reimplemented or hardcoded; shape correctness (`(len(texts), dim)`) and delegation are both asserted with a real hermetic fake (records call args, per-row vectors of a known dim), matching `tests/test_llm_models.py`'s convention; empty-input case returns `shape[0] == 0` without raising. `uv run ruff check .` clean; `uv run pytest` gives 140 passed / 6 deselected, reproduced twice cleanly plus once under concurrent load — could not reproduce the reported `test_reputation_cache.py` `PoolTimeout` flake at all, consistent with it being a pre-existing load-dependent flake unrelated to this branch (this branch touches no DB/pool code).
- [x] Task 2.1.2 (orig. Task 1): `clustering/cluster.py` — `cluster(vectors) -> labels`, HDBSCAN primary, KMeans fallback.
      Acceptance: unit test with a fixed multi-cluster embedding fixture confirms correct HDBSCAN grouping; a fixture below `kmeans_fallback_threshold` confirms KMeans is used instead.
      Depends on: 2.0.1, 2.1.1

      ✅ tech-lead approved, ready to merge (branch `origin/task/2-1-2b-clustering-cluster`, Task 2.1.2b) — this closes the Task 2.1.2a caveat at line 558 above (the real `hdbscan` re-validation gate). Independently reproduced from scratch (own script, real `hdbscan.HDBSCAN`, not trusting the branch's reported numbers): at `min_cluster_size=4` on the committed `tests/fixtures/clustering_synthetic_topics.json`, `min_samples=2` (the data-scientist's original pick) gives ARI=0.324 and finds only 2 of the 4 true clusters; `min_samples=1` gives ARI=0.781 and finds all 4 — exact match to the reported values, and a real, meaningful divergence from the sklearn-based sweep, not a rounding artifact. Also independently reproduced the `kmeans_fallback_threshold=20` subsample degradation curve at `min_samples=1` by re-running the exact same seeded (`np.random.default_rng(42)`) subsample procedure from `notebooks/phase2_clustering_eval.py`: 0.554 ARI @ n=28, 0.242 @ n=20, 0.000 @ n=12 — exact match. `Settings.clustering.hdbscan_min_samples=1`/`hdbscan_min_cluster_size=4`/`kmeans_fallback_threshold=20` are correctly earned, not asserted. `newsresearch/clustering/cluster.py` imports the real `hdbscan.HDBSCAN` (not sklearn's), correctly gated on `Settings.clustering.kmeans_fallback_threshold` with KMeans as fallback. The `k_hint: int | None = None` parameter is a sound interface for Task 2.2.3b's caller (the Subtopic Agent has a natural LLM-proposed candidate count to pass) without forcing it on Task 2.5.1's per-subtopic clustering path, which has no such count and stays on HDBSCAN for anything above the fallback threshold anyway; the silhouette-sweep-over-k=2..min(5,n-1) default for the no-hint case is a reasonable fallback specifically because it only fires in the KMeans-fallback branch (small n), where an exhaustive small sweep is cheap, not a general-purpose k-selection mechanism being relied on at scale. `tests/test_clustering_cluster.py` genuinely exercises both branches with real ARI-threshold assertions (not doesn't-crash checks): full 32-point fixture through HDBSCAN (ARI ≥ 0.7, 4 clusters found), an 8-point subsample forced through KMeans via `k_hint=4` (ARI == 1.0, no noise label), plus an empty-input and a no-hint-estimation case. `uv run ruff check .` clean in an isolated worktree; `uv run pytest -q` gives 153 passed / 7 deselected, with one unrelated pre-existing failure (`test_sourcing_agent_raises_without_database_url_or_pool`) reproduced identically on `master` outside this branch's worktree too — a local-environment artifact (an ambient `.env` leaking `DATABASE_URL` into that test's explicit-`None` override), not something this branch introduced; not blocking. No scope creep: no parallel config/LLM-call path, `Settings.clustering.*` is the sole hyperparameter source per the Cross-Cutting Concerns convention, and the `cluster(vectors, k_hint=None)` signature is exactly what Task 2.2.3b's reconciliation logic (in parallel design via Task 2.2.3a) will need to call.

**Story 2.2 — Subtopic Agent proposes, fetches, clusters, and reconciles candidates**
Acceptance: given a topic string, `agents/subtopic_agent.py` returns a ranked, capped subtopic list plus a retained "also detected" excess set.
- [x] Task 2.2.1 (orig. Task 2): subtopic-propose prompt (`llm/prompts/subtopic_propose.txt`) + pydantic candidate-list schema, via `get_chat_model("subtopic").with_structured_output(...)`.
      Acceptance: given a sample topic, returns N schema-conformant candidate labels; call is traced in the local Langfuse UI.
      Depends on: Phase 0 Task 0.6.1, 0.6.3
      ✅ Done (Tasks 2.2.1a design + 2.2.1b wiring, both merged). Real-API acceptance closed out for real: called against 4 real sample topics with a genuine `OPENAI_API_KEY`, got schema-conformant output each time, and independently confirmed real Langfuse traces in ClickHouse twice (implementer + tech-lead, different topics, both showing real token usage/cost on `gpt-4.1-2025-04-14`). `propose_candidates` also forwards an ambient `RunnableConfig` correctly (merges rather than replaces callbacks/metadata), closing the interface gap flagged for Task 2.2.3b's future node-wiring.
- [x] Task 2.2.2 (orig. Task 3): broad topic-scoped fetch reusing Phase 1's `sourcing_agent` (not yet subtopic-filtered).
      Acceptance: calling Phase 1 Task 1.9.1's agent with topic-level keywords returns an article set spanning multiple candidate subtopics.
      Depends on: Phase 1 Task 1.9.1
      ✅ tech-lead approved, ready to merge (branch `origin/task/2-2-2-broad-topic-fetch`) — `broad_topic_fetch` is a genuine thin wrapper: forwards `[topic]`/`lookback_days`/`pool`/`settings` straight to Phase 1's already-approved `sourcing_agent` unchanged, and correctly unwraps `list[ScoredArticle]` to plain article dicts for downstream embedding/clustering (Task 2.2.3 needs text/title, not reputation scores). Single-keyword-from-topic-string is the right call at this task's scope, not a skipped data-scientist decision: `sourcing_agent` already fans a single keyword out across GDELT/RSS/backfill, and per-subtopic keyword refinement is explicitly Task 2.5.1's job once real subtopics exist — inventing topic-decomposition here would duplicate Task 2.2.1/2.2.3's actual candidate-proposal-and-reconciliation logic ahead of schedule. The live test's domain-diversity proxy for "spans multiple candidate subtopics" is honestly caveated as a stand-in (subtopic detection doesn't exist until Task 2.2.3) rather than overclaimed, and is a defensible-if-weak mechanical signal for a smoke test whose only job is confirming the wrapper isn't accidentally narrow — it does not (and doesn't claim to) verify actual subtopic breadth, which is Task 2.2.3/2.2.4's acceptance to prove. Mocked unit test genuinely exercises keyword-derivation + full argument forwarding (`assert_called_once_with`) + dict-vs-`ScoredArticle` unwrapping (explicit `isinstance` checks both ways), not a bare doesn't-crash check. Verified independently: `uv run ruff check .` clean; `uv run pytest -q` gives 139 passed / 7 deselected; branch confirmed as a fast-forward descendant of latest master (includes 8c65107) via `git merge-base --is-ancestor`. The reported 641s live-run duration is consistent with this codebase's already-accepted GDELT/WHOIS/HTTPS-probe latency pattern (same shape as `tests/live/test_sourcing_agent_live.py`'s bounded-retry precedent, driven by per-unique-domain WHOIS+HTTPS signal collection plus GDELT backoff, not a bug in this new wrapper — the wrapper itself makes exactly one `sourcing_agent` call, no accidental repetition). Docstring correctly scopes remaining Story 2.2 content (candidate proposal, clustering, reconciliation, ranking) as separate, not-yet-done Wave 2/3 tasks. No scope creep: introduces no LLM calls (cost_callback convention doesn't apply here), no parallel config path, and composes cleanly as a building block for the eventual `subtopic_agent()` orchestration.
- [x] Task 2.2.3 (orig. Task 4): embed+cluster the broad set; reconciliation logic (merge candidates mapping to the same cluster, split clusters spanning multiple candidates, drop unsupported candidates).
      Acceptance: unit test with a fixed embedding fixture triggers each of merge/split/drop independently under its respective condition.
      Depends on: 2.1.2, 2.2.1, 2.2.2
      Done via split 2.2.3a (design, data-scientist) + 2.2.3b (wiring, backend-engineer). Full tech-lead review at Task 2.2.3b's entry below (search "tech-lead approved, ready to merge (branch `origin/task/2-2-3b-2-2-4-reconciliation`"). Merged PR #27.
- [x] Task 2.2.4 (orig. Task 5): rank by volume/distinctiveness, truncate to `Settings.pipeline.max_subtopics`, retain excess as "also detected."
      Acceptance: given more reconciled clusters than the cap, output length equals the cap exactly; every excess cluster is recorded in a separate field, none silently dropped.
      Depends on: 2.2.3, Phase 0 Task 0.3.1
      `rank_and_cap_subtopics()` in `subtopic_agent.py`. Same tech-lead review as Task 2.2.3 above (Task 2.2.3b entry, covers both). Merged PR #27.

**Story 2.3 — Gate 1: human approves/edits the subtopic list, durably**
Acceptance: `graph/nodes/gate1.py` interrupts with candidates + excess, accepts approve/edit via `Command(resume=...)`, re-runs reconciliation on edit, and the interrupt state survives a process restart.
- [x] Task 2.3.1 (orig. Task 6): `graph/nodes/gate1.py` — interrupt node; approve/edit handling; edit triggers re-reconciliation.
      Acceptance: an approve-resume proceeds with the original list unchanged; an edit-resume (e.g. removing one candidate) re-triggers Task 2.2.3's reconciliation on the edited set.
      Depends on: 2.2.4
      Status: ✅ fully closed, tech-lead approved (branch `origin/task/2-3-1-gate1-real-reconcile`, PR #28) — edit-resume now re-triggers real reconciliation via `make_real_reconcile`, not the identity stub. See approval note below (near Task 2.2.3b/2.2.4's approval) for detail.
- [x] Task 2.3.2 (implied by the phase Done-when, not separately numbered above): kill-and-restart durability check specifically for Gate 1's interrupt state.
      Acceptance: killing the process mid-Gate-1-interrupt and restarting, then resuming via the same `thread_id`, produces the correct pending-approval state read back from Postgres — not merely an in-process pause.
      Depends on: 2.3.1, Phase 0 Task 0.5.2
      Status: done, tech-lead approved — verified against real `testcontainers` Postgres (fresh pool/checkpointer/graph object simulating a restart, a legitimate proxy since `PostgresSaver`'s guarantee is specifically no in-process state). Tested against the stub payload; property holds regardless of what real data later populates the same fields.

**Story 2.4 — Concurrent per-subtopic fan-out**
Acceptance: approving Gate 1 fans out to one concurrently-running branch per approved subtopic.
- [x] Task 2.4.1 (orig. Task 7): fan-out wiring in `graph/build.py` using LangGraph `Send` per approved subtopic.
      Acceptance: approving N subtopics produces N concurrent branches, each carrying its own subtopic sub-state per Phase 0 Task 0.5.1's schema.
      Depends on: 2.3.1, Phase 0 Task 0.5.1
      ✅ **tech-lead approved with notes, merged (branch `task/2-4-1-fan-out-send`, PR #29)** — reviewed `graph/build.py`, `graph/state.py`, `tests/test_graph_build.py`. `fan_out` is a real `Send`-based conditional edge (`fan_out_router`); `sourcing`→`clustering`→`gate2` relayed via `_make_relay_router`, with per-branch identity (`run_id`/`subtopic_id`/`label`) smuggled through concurrent supersteps via a new reducer-safe accumulator, `GraphState.fan_trace: Annotated[list[tuple[str,str,str]], operator.add]`. Confirmed this is a legitimate response to a real LangGraph constraint, not a workaround masking a self-inflicted problem: a `Send` overlay doesn't survive into a plain scalar channel across a subsequent ordinary edge, so there's no reducer-safe alternative to carrying identity this way; growth is bounded (per-thread checkpoint state, fresh per run, `hops × N-subtopics` tuples — trivial). New test `test_fan_out_sends_one_concurrent_branch_per_approved_candidate` genuinely proves N-in/N-out through all three hops (asserts the same 3-element `subtopic_id` set at `sourcing`/`clustering`/`gate2`, not just at the `Send` boundary). Scope respected — diff limited to exactly these three files, no touch to `gate1.py`/`gate2.py`/`subtopic_agent.py`/CLI (the graph's "gate2" node here remains the Wave 1 passthrough stub, not the real interrupt). Independently re-ran in the primary checkout: `uv run ruff check .` clean; `uv run pytest` → 167 passed / 7 deselected, matching the report (one failure seen on first run traced to a leaked `NEWSRESEARCH_DATABASE_URL` in the reviewer's own shell env, reproduced identically on `master` with the same env var set — pre-existing, not a PR regression). **Forward note (non-blocking) for the next fan-out story:** Story 2.5/2.6 extends this same per-branch chain through several more hops (sourcing→...→briefing→snapshot per TRD 3.1); continuing today's relay-router pattern means hand-maintaining a growing `FAN_OUT_TARGET_NODES`/relay list. LangGraph's per-`Send` subgraph pattern (a private `SubtopicState`-only subgraph invoked once per `Send`, no state merged into the parent graph) is the framework's native fit for exactly this multi-hop-per-branch shape and would eliminate the relay/accumulator machinery entirely — migrating 3 hops now is cheaper than migrating 8 later. Flagged as an open call for `tech-lead` to make before Story 2.5/2.6 starts, not a defect in this branch. **For Task 2.3.2's still-pending kill-restart re-check against real fan-out data**, specifically exercise: (1) kill mid-fan-out, after `fan_out` schedules N `Send`s but before all N `sourcing` tasks complete — confirm resume re-executes exactly the incomplete branches, and `fan_trace` doesn't double-count a branch that had partially checkpointed pre-kill; (2) kill between hops (e.g. after `clustering`'s `fan_trace` writes land but before `_make_relay_router` schedules `gate2`) — confirm resume reconstructs exactly N branches from `fan_trace`; (3) once Gate 2 is wired with a real `interrupt()` (not this stub), kill while one of N branches is parked at Gate 2 — `PostgresSaver` checkpoints per-thread, not per-branch, so resuming one subtopic's approval must not replay or resume sibling branches.

**Story 2.5 — Topical clustering per subtopic**
Acceptance: `agents/topical_clustering_agent.py` produces coarse per-subtopic clusters, reusing Phase 1 sourcing and Story 2.1's clustering primitives.
- [x] Task 2.5.1 (orig. Task 8): per-subtopic sourcing (Phase 1) + coarse clustering (2.1.2).
      Acceptance: given one approved subtopic, returns topical clusters with plausible article groupings on a real subtopic — manually spot-checked, since clustering quality is this phase's flagged risk.
      Depends on: 2.1.2, 2.4.1, Phase 1 Task 1.9.1
      ✅ **tech-lead approved with notes, merged (branch `task/2-5-1-topical-clustering-agent`, PR #30)** — reviewed new `newsresearch/agents/topical_clustering_agent.py::topical_clustering_agent(subtopic_id, label, lookback_days, *, pool=None, settings=None)`. Genuine reuse, no parallel path: calls `sourcing_agent(["label"], lookback_days, pool=, settings=)` then `embed()` then `cluster()` (no `k_hint`, matching `cluster.py`'s no-hint per-subtopic design) directly against their real `master` signatures. No LLM calls introduced (confirmed via grep — no `get_chat_model`/`ChatOpenAI`/`with_structured_output`). Returns `{subtopic_id, label, clusters: {id: [article...]}, noise: [...], total_articles}`; since `sourcing_agent`'s own field filter guarantees every article dict carries `title`/`domain`, this shape is directly consumable by Task 2.6.1's `cluster_sizes`/`sample_headlines`/`source_spread` with zero re-sourcing, and matches `SubtopicState.cluster_report`'s stub shape from Wave 1. Unit tests (`tests/test_topical_clustering_agent.py`) assert real wiring — exact call args to `sourcing_agent`/`embed`, correct cluster-id→article grouping, noise separation including no-noise/empty-set edge cases — not smoke-only. `tests/live/test_topical_clustering_agent_live.py` structurally mirrors the existing `test_subtopic_agent_live.py` pattern. Independently re-ran on `origin/task/2-5-1-topical-clustering-agent` (commit `874b832`): `uv run ruff check .` clean; `uv run pytest -q` → 170 passed / 8 deselected, with one pre-existing failure (`test_sourcing_agent_raises_without_database_url_or_pool`) reproduced identically on bare `master` under the same leaked `NEWSRESEARCH_DATABASE_URL` env var — not a PR regression (see note below). Live test not re-run in review (no network access in review environment); test file structure judged correct by inspection. Scope respected — empty diff against `graph/build.py`/`gate1.py`/`gate2.py`/`reports/`. Clustering-quality spot-check (subtopic "solar panel manufacturing," 4 real articles, KMeans-forced k=2: one tight genuine-match cluster, one looser pairing the agent itself flagged as possibly just KMeans forcing 2 groups at small n) was correctly *not* self-certified by backend-engineer — EXECUTION_PLAN's Phase 2 Role Ownership table assigns this judgment to `data-scientist`, formalized as a Task 2.8 Done-when co-sign, not a per-task merge gate; 2.5.1's own acceptance line doesn't require it before merge. **Needed before Phase 2's Task 2.8 closes:** an explicit `data-scientist` clustering-quality pass over real per-subtopic output (this task's own spot-check plus more samples), not yet done. **Separately flagged, non-blocking, pre-existing bug for whoever next touches `sourcing_agent`/`Settings`:** `test_sourcing_agent_raises_without_database_url_or_pool` fails whenever `NEWSRESEARCH_DATABASE_URL` is set in the calling shell even though `database_url=None`/`pool=None` are passed explicitly — env var is silently winning over an explicit `None` kwarg, violating `CLAUDE.md`'s stated `Settings` precedence (explicit kwargs > env vars > .env > config.yaml > secrets). Confirmed on `master`, unrelated to this PR.

**Story 2.6 — Gate 2: zero-LLM-cost cluster report, per-subtopic blocking**
Acceptance: `reports/gate2_report.py` aggregates cluster sizes/sample headlines/source spread with zero LLM calls; `graph/nodes/gate2.py` blocks each subtopic branch independently until that subtopic's report is reviewed.
- [x] Task 2.6.1 (orig. Task 9, part a): `reports/gate2_report.py` — pure aggregation, zero LLM calls.
      Acceptance: given Story 2.5's cluster output, produces cluster-size/sample-headline/source-spread fields with zero calls to `get_chat_model` (asserted via a call-count test double).
      Depends on: 2.5.1
      ✅ **tech-lead approved, merged (branch `task/2-6-1-gate2-report`, PR #31)** — reviewed new `newsresearch/reports/gate2_report.py::build_gate2_report(clustering_result: dict) -> dict`, aggregating Story 2.5's `topical_clustering_agent` output into `SubtopicState.cluster_report`'s documented shape (`graph/state.py`): `cluster_sizes` (len per non-noise cluster, correctly excludes noise), `sample_headlines` (up to 2 titles/cluster, guarded against missing `title`), `source_spread` (domain→count across clusters + noise combined — a reasonable, docstring-documented judgment call for full source-diversity visibility, not a stricter existing convention it conflicts with, since this is the field's first implementation). Zero-LLM claim verified genuine, not just test-asserted: grepped the module source directly, no `llm/models.py` import, no `get_chat_model`/`ChatOpenAI` reference outside the docstring's prose; the test double patches the real `newsresearch.llm.models.get_chat_model` definition site and asserts `assert_not_called()` — a legitimate negative proof, not a decoy. Scope respected: empty diff against `graph/build.py`/`gate1.py`/`gate2.py`/`topical_clustering_agent.py` — wiring this into Gate 2's real `cluster_report` output (replacing the Wave 1 stub) is correctly deferred as a separate follow-up, since this task's acceptance line only requires the aggregation function to exist, not to be wired in yet. Independently re-ran on `origin/task/2-6-1-gate2-report`: `uv run ruff check .` clean; `uv run pytest -q` → 173 passed / 1 failed / 8 deselected, the 1 failure being the same already-documented pre-existing `test_sourcing_agent_raises_without_database_url_or_pool` env-var-leak bug (see Task 2.5.1's review note above), reproduced on bare `master`, not a regression from this PR. **Note:** this PR was opened by the orchestrating session on the implementer's behalf — the implementer's own sandbox had GitHub API/credential access blocked for this run; code, tests, and commit are the implementer's own, only the PR-open mechanics were done externally.
- [x] Task 2.6.2 (orig. Task 9, part b): `graph/nodes/gate2.py` — interrupt node, blocking per subtopic branch independently.
      Acceptance: one subtopic branch paused at Gate 2 does not block a sibling branch's own Gate 2 from being reviewed and resumed independently.
      Depends on: 2.6.1, 2.4.1
      Status: Wave 1 interrupt/resume/independent-blocking mechanics merged (`task/gate1-gate2-interrupt-mechanics`), full acceptance now closed — see below.
      ✅ **tech-lead approved, merged (branch `task/2-6-2b-gate2-clustering-wiring`, PR #33) — real `cluster_report` wired in, closing the "stubbed cluster_report" gap noted at Wave 1** — this PR is a fix-and-resubmit after **PR #32 was REJECTED** in review for a real bug: PR #32 called `topical_clustering_agent`→`build_gate2_report` directly inside `gate2_node` *before* `interrupt()`; LangGraph replays a node's function from the top on every resume, so that call re-executed on resume — instrumented with a call counter and proved real sourcing/clustering ran twice per Gate 2 pass (doubled GDELT calls, staleness risk between the report a human reviewed and what got persisted after resume). PR #33's fix: moved the real work into `graph/build.py`'s already-reserved `"clustering"` node (a `Send`-relayed `FAN_OUT_TARGET_NODES` member, runs exactly once per fanned branch, strictly upstream of any `interrupt()`), leaving `gate2.py` reverted to a pure `state["cluster_report"]` read — confirmed byte-identical to its pre-PR#32 (Wave 1) state via `md5sum` against `master`, not just trusted. New `GraphState.cluster_reports: Annotated[list[tuple[str,dict]], operator.add]` accumulator + `_make_relay_router`'s new `carry_field` param fold each branch's computed report into its own outgoing `Send` payload — reviewed for the same class of concurrent-state bug that caught PR #32 (flat thread-scoped accumulator, unique keys per branch, no conflation risk found, no re-entry into `clustering` on resume). The fix was mutation-tested, not just read: reviewer reinserted PR #32's exact bug (clustering call inside `gate2_node`) against the new test and confirmed it fails (`'us executive order': 2` calls), then reverted — proving the new test genuinely catches this bug class rather than passing vacuously. Independently re-ran in the PR's own worktree: `uv run ruff check .` clean; `uv run pytest -q` → 175 passed / 8 deselected, matching the report exactly. Scope confirmed: diff exactly `graph/build.py`, `graph/state.py`, `tests/test_graph_build.py` — no touch to `gate1.py`/`subtopic_agent.py`/`reports/gate2_report.py`/`topical_clustering_agent.py`. **Honest scoping, not a partial fix dressed as complete:** production `build_graph()`'s compiled topology still wires `"gate2"` as a `FAN_OUT_TARGET_NODES` passthrough stub — no `interrupt()` fires there yet, so the fixed bug cannot manifest in production today either way; wiring the real `gate2_node` into `build_graph()` itself is separately tracked (below) and this PR doesn't claim to have done it. ✅ **Follow-up closed — real `gate2_node` wired into `build_graph()`'s compiled topology (branch `task/2-6-2-gate2-wire-production`, PR #34)** — new `_make_gate2_node()` replaces the `"gate2"` slot's generic passthrough, guards on `subtopic_id is None` (no-candidates fallback path) by skipping the interrupt entirely rather than interrupting with an empty report — correct, since that legacy path has no real subtopic branch to gate and no way to drive a resume for it. `gate2.py` itself untouched (zero-diff, confirmed). Tech-lead independently re-verified in a separate worktree (not the primary checkout, to avoid repeating the uncommitted-edit-wipe incident noted below): re-ran the call-counter proof directly against the real `build_graph(database_url=...)` output (old hand-assembled `StateGraph` scaffolding deleted, not just renamed) — sourcing called exactly once per branch, pre- and post-resume, closing the exact re-verification gap flagged in PR #33's review. Independent per-branch blocking re-proven through the actual `Send` mechanism (one shared `thread_id`, one branch's interrupt resumed via its specific `Command(resume=...)` id, sibling's interrupt confirmed still pending in `graph.get_state(config).tasks` before its own separate resume) — not hand-built `thread_id`s standing in for it, as Wave 1's original test did. The existing `test_fan_out_sends_one_concurrent_branch_per_approved_candidate` needed a resume step added since gate2's `fan_trace` write now only lands post-interrupt-resume (gate2 being real, not a stub) — confirmed this doesn't weaken the test, N-in/N-out through all three `FAN_OUT_TARGET_NODES` hops still holds. `uv run ruff check .` clean; `uv run pytest`: 176 passed, 8 deselected, no regressions. Scope confirmed: diff exactly `graph/build.py` + `tests/test_graph_build.py`, no touch to `gate1.py`/`subtopic_agent.py`/`reports/gate2_report.py`/`topical_clustering_agent.py`/`clustering/cluster.py`. **Two non-blocking doc-drift notes for whoever next touches these files:** `_make_gate2_node()`'s docstring references a nonexistent `make_gate2_node` factory (only a bare `gate2_node` function exists and is what's actually called); `gate2.py`'s own module docstring still describes the superseded thread_id-standin verification approach, not this PR's real-`Send` proof. **Real gap surfaced, not a defect in this PR:** `gate1` is still wired as a generic `_make_passthrough_node("gate1")` in `build_graph()` — Gate 1, the *earlier* human-approval gate, remains a no-op in the compiled production graph even though Gate 2 is now real. Needs its own task, same shape as this one, before Story 2.8's full CLI walkthrough can exercise a real Gate 1 interrupt end-to-end.

✅ **Follow-up closed — real `gate1_node` wired into `build_graph()`'s compiled topology (branch `task/2-6-2-gate1-wire-production`, PR #35)** — new `_make_gate1_node()` replaces the `"gate1"` slot's generic passthrough. Since `make_real_reconcile(articles, *, settings=None)` needs runtime state not known at graph-build time, the node builds a fresh `ReconcileFn` closure inside the node function body on every invocation (deferred-closure binding), matching the pattern already proven in `tests/test_gate1.py::test_gate1_edit_resume_runs_real_reconciliation`. `gate1.py` itself confirmed zero-diff. New `GraphState.articles` field added as a stopgap (the real "subtopic" node is still a no-op, so there's exactly one writer today — invoke-time seeding — with no collision risk yet; both the field comment and `build.py`'s docstring flag the future collision once a real subtopic node lands). `cli.py`'s `run` harness updated to auto-approve gate1 unconditionally, since gate1 now genuinely interrupts and no interactive stdin approve/edit exists yet (that's Story 2.7/Task 2.7.1) — confirmed the correct minimal stopgap, not scope creep, since without it `run` would silently dead-end at gate1 forever. Tech-lead independently re-verified in a fresh `git clone` (not the primary checkout): confirmed `gate1.py`/`gate2.py`/`subtopic_agent.py`/`reports/gate2_report.py`/`topical_clustering_agent.py`/`clustering/cluster.py` all zero-diff; confirmed the two new tests (`test_gate1_approve_resume_proceeds_through_real_build_graph`, `test_gate1_edit_resume_runs_real_reconciliation_through_real_build_graph`) genuinely prove real reconciliation ran through the actual compiled `build_graph()`, not the identity stub; confirmed the 4 modified pre-existing tests (adding an approve-resume step for gate1) were not weakened. `uv run ruff check .` clean; `uv run pytest`: 178 passed, 8 deselected, matching the report exactly. Scope confirmed: diff limited to `graph/build.py` (+43/-6), `graph/state.py` (+9), `cli.py` (+12), `tests/test_graph_build.py` (+150/-6).

✅ **Follow-up closed — real subtopic node wired into `build_graph()`'s compiled topology (branch `task/2-2-4-subtopic-node-wire-production`, PR #36)** — new `_make_subtopic_node()` replaces the `"subtopic"` slot's generic no-op stub (`_make_subtopic_stub_node()`/`_StubSubtopicChatModel`, both deleted), calling the real Story 2.2 pipeline (`propose_candidates` → `broad_topic_fetch` → embed/cluster/reconcile → `rank_and_cap_subtopics`) unconditionally, once per invocation, upstream of Gate 1 (plain edge, no `interrupt()` in this node — confirmed structurally, not just by docstring). `subtopic_agent.py` itself confirmed zero-diff, wiring-only as scoped. **Articles/candidates/excess ownership resolved**: the real subtopic node's `broad_topic_fetch`/pipeline output is now the sole writer of `GraphState.articles`/`candidates`/`excess` — the invoke-time stopgap seeds flagged as a future collision risk in PR #35's review were removed entirely from `cli.py` and tests, no dual writer remains (grepped and confirmed independently). `propose_candidates`'s `RunnableConfig` forwarding (`merge_configs`) confirmed still threaded through from the new node wrapper, not dropped. New end-to-end test (`test_subtopic_node_populates_candidates_excess_articles_through_real_build_graph`) mocks only true external boundaries (`get_chat_model`, `sourcing_agent`, `embed`) and runs through the actual compiled `build_graph()`, asserting a real reconciliation merge (4→3 subtopics from near-duplicate candidate labels) — judged genuine proof, not vacuous. All pre-existing tests adapted (seed literals replaced with an equivalent stubbed pipeline call), not weakened — same assertions, same coverage. `uv run ruff check .` clean; `uv run pytest`: 179 passed, 8 deselected, matching self-report exactly. Scope confirmed: diff limited to `graph/build.py`, `graph/state.py` (docstrings only, no schema change), `cli.py` (seed removal), `tests/test_graph_build.py`, `tests/test_cli.py`. **Flagged, not blocking:** a real end-to-end `uv run newsresearch run "<topic>"` against a live OpenAI key/GDELT/RSS could not be verified in either the implementing or reviewing agent's sandbox (no key/network access) — judged an acceptable gap given the unusually strong hermetic coverage (real compiled-graph composition, real config forwarding, real reconciliation-merge assertion, only true I/O boundaries mocked), consistent with this project's established precedent (Task 0.7.4/Story 2.5.1) of flagging such gaps honestly rather than blocking on infra an agent can't reach. A human or `acceptance-verifier` should still run it once for real when convenient.

**Still open, not yet started:** Task 2.3.2's kill-restart durability re-check against real fan-out data (flagged pending since PR #29/#28's reviews) — now that Gate 1, Gate 2, and the subtopic node are all real in `build_graph()`, this can finally be exercised against genuine full-pipeline state rather than stub payloads. Story 2.7 (CLI interactive gate rendering, still using gate1 auto-approve stopgap). Story 2.8 (Phase 2 Done-when: full CLI run through both real gates, kill/restart under the full pipeline, Langfuse trace confirmation).

✅ **Task 2.3.2 kill-restart re-check against real fan-out data, done** (branch `task/2-3-2-kill-restart-real-fanout-recheck`) — now that gate1 (PR #35), gate2 (PR #34), and subtopic (PR #36) are all real in `build_graph()`'s compiled topology, re-ran the durability property against real data. Four new tests, all simulating "kill" the same way as the original stub-based test (`del graph`, then a brand-new `ConnectionPool`/`PostgresSaver`/compiled graph against the same real `testcontainers` Postgres URL): `tests/test_gate1.py::test_gate1_real_reconciliation_survives_simulated_process_restart` (Gate 1's own kill-restart, now with `make_real_reconcile` over the real `clustering_synthetic_topics.json`/`reconciliation_merge.json` fixtures instead of the stub payload — pending-approval state including real candidates/excess reads back correctly, and edit-resume post-restart still runs genuine reconciliation, e.g. the merge/`distinctiveness_score` assertions); `tests/test_graph_build.py::test_kill_mid_fan_out_reexecutes_only_the_incomplete_branch` (PR #29 scenario 1 — one branch's `sourcing` made to raise, simulating a kill mid-superstep; restart resumes via `graph.invoke(None, config)`, and `fan_trace` shows each of the 3 branches' `sourcing` exactly once — LangGraph's pending-writes mechanism held on first try, no double-count of the two branches that had already succeeded pre-kill); `test_kill_between_clustering_and_gate2_hops_reconstructs_all_branches` (PR #29 scenario 2 — `interrupt_after=["clustering"]` used to land exactly at that hop boundary, then restarted with a plain `build_graph()`; all 3 branches correctly reconstructed from `fan_trace` and carried through to 3 independent Gate 2 interrupts); `test_kill_while_parked_at_gate2_does_not_replay_or_resume_sibling_branches` (PR #29 scenario 3 — restart inserted between resuming each of two sibling branches parked at Gate 2; the still-pending sibling's interrupt id is unchanged and unresumed across the restart, confirmed via `get_state(config).tasks` on the fresh graph object before its own independent resume). No durability bug found — the property held against real data exactly as tech-lead's original "regardless of what real data later populates the same fields" approval anticipated; the re-check surfaces no regression, only closes out the deferred proof obligation. Verified: `uv run ruff check .` clean; `uv run pytest` → 183 passed / 8 deselected (179 prior + 4 new, no regressions). Scope: diff limited to `tests/test_gate1.py` + `tests/test_graph_build.py`, no production code touched (this is a pure re-verification task, no fix was needed). ✅ **tech-lead APPROVE WITH NOTES, merged (PR #37)** — independently re-derived all 4 scenarios in a fresh clone, confirmed each "kill" is a genuine simulated restart (fresh `ConnectionPool`/`PostgresSaver`/compiled graph against the same DB, not reused in-memory objects). Mutation-tested scenario 2 by swapping in a non-durable `InMemorySaver` in place of the real `PostgresSaver` — the test hard-failed (`EmptyInputError`) rather than passing vacuously, proving it exercises a genuine Postgres round trip. `uv run ruff check .` clean; `uv run pytest` → 183 passed / 8 deselected, exact match. Note (non-blocking): this PR bundled its own EXECUTION_PLAN.md status update rather than leaving that to the orchestrating session post-merge — content was accurate, reconciled into this entry above rather than duplicated.

**Process note (not code):** mid-session, a tech-lead review agent's git cleanup in the primary checkout twice discarded my uncommitted `EXECUTION_PLAN.md` edits for this section (Tasks 2.5.1/2.6.1) before they were committed — reconstructed from conversation history and committed immediately afterward (`7197cb2`/`6c9b21a`). Later review rounds were instructed to use a separate worktree/stash rather than resetting the primary checkout's working tree, and to leave any pending `EXECUTION_PLAN.md` edits untouched.

**Story 2.7 — CLI harness renders both gates interactively**
Acceptance: the CLI harness renders Gate 1 candidates, accepts stdin approve/edit, then renders the Gate 2 report per subtopic and accepts continue.
- [x] Task 2.7.1 (orig. Task 10): extend `cli.py` — render Gate 1 candidates/excess, accept stdin approve/edit; render Gate 2 report per subtopic, prompt to continue.
      Acceptance: `uv run newsresearch run "<topic>"` pauses at Gate 1 with a readable candidate list, accepts a stdin edit, proceeds to per-subtopic Gate 2 output, and resumes on confirmation.
      Depends on: 2.3.1, 2.6.2, Phase 0 Task 0.8.1
      ✅ **tech-lead APPROVE WITH NOTES, merged (branch `task/2-7-1-cli-interactive-gates`, PR #38)** — new `_render_gate1`/`_prompt_gate1` render candidates+excess and prompt approve/edit; edit re-runs real reconciliation via the existing `make_real_reconcile` contract, no changes to `gate1.py`. `_prompt_gate2` renders each per-subtopic `cluster_report`. `_resume_payloads` dispatches by payload shape (`"candidates"` key → gate1, `"cluster_report"` key → gate2) into one id-keyed `Command(resume=...)` map per pass, correctly resuming gate1's single interrupt and gate2's N concurrent per-subtopic interrupts together; `run()` now loops `while "__interrupt__" in result`. Tech-lead confirmed zero diff on `gate1.py`/`gate2.py`/`graph/build.py`/`subtopic_agent.py`; confirmed the payload-shape dispatch is safe today (the two gates' interrupt shapes are disjoint by construction, with a loud `ValueError` fallback for any future third shape); confirmed the edit-path test genuinely proves real reconciliation (drops 1 of 4 candidates, merges a real duplicate pair down to 2 — an identity pass-through would show 3); confirmed N-concurrent Gate 2 interrupts are genuinely handled together, not sequentially faked, cross-checked against the pre-existing `test_graph_build.py` fan-out pattern; confirmed no pre-existing test weakened. `uv run ruff check .` clean; `uv run pytest` → 181 passed / 8 deselected, exact match. **Flagged, not blocking:** no real-terminal smoke run (`typer.prompt` echo/backspace/ctrl-C in an actual TTY) has been done yet — hermetic `CliRunner` coverage judged sufficient for merge, but this is the first task where the CLI is the actual interactive UX surface (not just internal graph wiring), so a real terminal run is worth doing once before relying on it daily.

**Story 2.8 — Phase 2 Done-when verified end-to-end (integration/demo)**
Acceptance: all bullets in the phase-level "Done when" block above hold simultaneously.
- [x] Task 2.8.1: full CLI run on a real topic through Gate 1 → fan-out → Gate 2, confirming per-subtopic zero-cost report correctness.
      Depends on: 2.7.1
      Status: **partially confirmed, real gap surfaced.** Run by the user on real infra (`uv run newsresearch run "stundent protesnt in india"`, live OpenAI key, real GDELT/RSS/WHOIS/HTTPS signals, real Docker Compose Postgres+Langfuse), `run_id=run-36ebbd42-...`, exited 0. Real GDELT DOC 2.0 429 rate-limiting hit hard on both the broad subtopic fetch and the per-subtopic clustering fetch (exhausted all 5 retries each time); soft-fail handled it correctly per NFR-3 — `GDELTError` caught, continued on RSS/backfill only, no crash — arguably better evidence for the graceful-degradation contract than a clean run would've been. **Consequence, not a bug in this PR chain:** only 1 Gate 1 candidate resulted (4 articles), and Gate 2's cluster report came back genuinely empty (`cluster_sizes=[]`, `source_spread={}`) because clustering's own per-subtopic GDELT call was also rate-limited down to too few articles to cluster — so "per-subtopic zero-cost report correctness" is not actually demonstrated by this run, only that the report-building code doesn't crash on a degenerate empty-clusters input. Only the approve path was exercised at Gate 1, not edit. **Recommend re-running once GDELT's rate-limit window clears** (or against a less contentious/rate-limited time window) to get a real non-empty Gate 2 report before fully closing this bullet — logged as open, not re-blocking Phase 2.
- [x] Task 2.8.2: kill/restart mid-gate durability re-check under the full real pipeline (not just Task 2.3.2's isolated Gate 1 check) — verify at both Gate 1 and Gate 2.
      Depends on: 2.3.2, 2.6.2
      Status: **BLOCKED — real CLI-layer gap found, not a pipeline bug.** User killed (Ctrl-C) `uv run newsresearch run "nepal student protest and coup"` while parked at the Gate 1 stdin prompt, then re-ran the identical command — it minted a brand-new `run_id`/`thread_id` (`run-8b396f66-...`) rather than resuming the killed run. Root cause confirmed by reading `cli.py`: `run(topic: str = typer.Argument(...))` (line 137) takes no `--thread-id`/`--resume` option — `"configurable": {"thread_id": run_id}` (line 161) always derives from a freshly-minted `run_id`, with no code path to rejoin an existing thread. **The underlying durability guarantee is real and already proven independently** (Task 2.3.2's `testcontainers`-backed re-check directly against `build_graph()`/`PostgresSaver` — mid-fan-out kill, kill between clustering→gate2, kill while parked at gate2 with a sibling pending, all held) — this is specifically that the CLI dev harness has no way to reach that guarantee today, a UX gap, not a data-loss risk. ✅ **Follow-up closed — `--thread-id` resume added (branch `task/2-8-3-cli-resume-thread-id`, PR #39)** — new `--thread-id` option on `run`, optional, `None` default. When given: skips seeding entirely, reads `graph.get_state(config)`, flattens `snapshot.tasks[*].interrupts` (same shape already proven in `test_graph_build.py`'s PR #34/#38 durability tests, not improvised), resumes through the existing PR #38 `_resume_payloads`/`_prompt_gate1`/`_prompt_gate2` machinery via a new shared `_drain_interrupts()` helper used by both the fresh and resume paths. Unknown/completed `thread_id` fails with a clear `typer.BadParameter`, not a raw crash. Default no-flag path confirmed byte-for-byte unchanged (tests only additive, no pre-existing test touched). `run_id` now echoed up front with a `--thread-id=<run_id>` resume hint, not just at completion. Tech-lead independently re-verified in a fresh clone: traced the resume branch directly and confirmed no `initial_state`/re-seed call exists on it; confirmed the no-reseed test is genuinely non-vacuous (resumes with a *different* topic arg, asserts the *original* topic won — a reseed bug would fail this); confirmed the unknown-thread-id test hits a real `PostgresSaver.get_state()`, not mocked. `uv run ruff check .` clean; `uv run pytest`: 188 passed, 8 deselected (180 prior + 3 new). Scope confirmed: diff limited to `cli.py` + `tests/test_cli.py`, zero touch on `gate1.py`/`gate2.py`/`graph/build.py`/`subtopic_agent.py`. **Two non-blocking notes:** MLflow logs the resume call's (ignored) topic arg rather than the real persisted topic — cosmetic, worth a follow-up one-liner; and since this PR exists because the user found the bug by hand, tech-lead recommends one real Ctrl-C-at-Gate-1 + `--thread-id` resume against live Postgres to actually close out this bullet's original acceptance, not just the hermetic proof. **✅ Done — live smoke test confirmed** (`uv run newsresearch run "bangladesh coup"`, `run_id=run-a74aa575-...`): first invocation killed via Ctrl-C while parked at the Gate 1 prompt; re-invoked with `--thread-id=run-a74aa575-...`, printed `(resuming)`, dropped straight back to the pending Gate 1 prompt with no GDELT/RSS/WHOIS re-fetch and no re-seed, approved, completed cleanly. Task 2.8.2 fully closed — both the graph-layer durability (Task 2.3.2) and the CLI-layer path to reach it (PR #39) are now proven against real infra, not just hermetic tests. (Bonus, unrelated: this run also hit real WHOIS failures — `WhoisError: Whois command returned no output` for two `.com.bd` domains — soft-failed correctly per `reputation/signals.py`'s documented fail-soft contract, no crash.)
- [x] Task 2.8.3: confirm the Subtopic Agent's LLM calls appear as traces in the local Langfuse UI.
      Depends on: 2.2.1, Phase 0 Task 0.7.2
      Status: **confirmed, done.** User inspected Langfuse UI (`localhost:3000`) for `run_id=run-36ebbd42-...`: real trace for the `subtopic` stage showing a genuine `ChatOpenAI` call (1,088 tokens, $0.00842), tagged with `run_id`/`thread_id` correctly. Also observed (bonus, not required by this task) a resume-trace showing the real `gate1`→`fan_out`→`sourcing`→`clustering`→`gate2` topology with `approved=true`, and a separate near-instant (0.05s) trace for the still-fully-stubbed downstream nodes (`claims`/`summarize`/`bias`/`briefing`/`snapshot`/`timeline`) — confirms Phase 3+ genuinely untouched, not accidentally wired ahead of schedule.
- [x] Task 2.8's data-scientist clustering-quality co-sign (flagged pending since Task 2.5.1's review, PR #30): done, `feat/datascientist` branch (commit `0c2bc46`), analysis-only, no PR (not code to merge).
      `topical_clustering_agent()` run live (real GDELT/RSS/backfill, local sentence-transformers embeddings, production `Settings.clustering.*`) against 5 varied real subtopics: OpenAI GPT-5 release (32 articles, HDBSCAN, 4 clusters/34% noise), Russia-Ukraine ceasefire (8, KMeans k=3, 0% noise), Fed rate decision (11, KMeans k=5, 0% noise), Tesla earnings (39, HDBSCAN, 2 clusters/3% noise), solar panel manufacturing (40, HDBSCAN, 2 clusters/55% noise). `kmeans_fallback_threshold=20` fires correctly at these counts. 3 of 5 samples read as genuinely coherent on manual headline inspection. **Two real, specific problems found** (not generic small-n noise): (1) Fed rate decision — `_estimate_k`'s silhouette sweep picked k=5 for n=11, producing a forced singleton cluster and splitting one coherent story angle into two; (2) solar panel manufacturing — 55% noise rate, and most of that bucket is legitimately on-topic coverage discarded because `min_cluster_size=4` needs 4+ near-duplicate stories, tuned against a broader multi-subtopic fixture than this per-subtopic scope's more diffuse article sets. **Verdict: acceptable to co-sign as Done** — qualitative read of 5 real samples, not a measured benchmark (no ground truth exists for real GDELT/RSS pulls, matching PRD §7's named v1 eval gap). Both issues flagged as concrete, non-blocking follow-up for `backend-engineer`/`tech-lead` to schedule, not fixed here (design-vs-wiring split — this task owns the quality judgment, not the fix): a cap/floor fix in `clustering/cluster.py::_estimate_k` for low-n degenerate KMeans results; re-tuning `hdbscan_min_cluster_size` specifically for Task 2.5.1's per-subtopic scope (needs more real samples before locking a value). Full write-up: `notebooks/phase2_task2.8_clustering_review.md` (+ `.py` sampling script, `.log` raw output, `.json` structured samples) on `feat/datascientist`.

**GDELT rate-limiting under concurrent fan-out — ✅ closed (branch `task/gdelt-rate-limit-concurrent-fanout`, PR #40, merged).** Diagnosis (confirmed by reading `sourcing/gdelt.py`, not guessing): each fan-out branch called `gdelt.fetch()` independently with its own `httpx.Client` and its own 5-retry backoff — no rate limit was shared *across* concurrent branches. Fix: module-level `_USER_AGENT` header applied to every `httpx.Client` construction, plus a `threading.Lock`-guarded `_throttle()` in `query_window()` enforcing a shared minimum inter-request interval across all concurrent fan-out branches (LangGraph fan-out runs concurrently in-process via a thread-pool Pregel executor, not separate processes, so a `threading.Lock` is the correct primitive — no cross-process/Postgres-backed coordination needed). New `Settings.sourcing.gdelt_min_request_interval_seconds` (default 5.0, matching the pre-existing `DEFAULT_INTER_REQUEST_DELAY_SECONDS`). Tech-lead-reviewed and re-verified independently.

**Task 2.8.1 re-run (2026-08-02, `uv run newsresearch run "Iraq and WMDs"`) surfaced two further real bugs, now fixed — PR #100 (merged):**
1. **GDELT persistent 429 on a single, non-concurrent request** — reproduced live via direct `curl`: this is a genuine external IP-level block from GDELT itself, observed staying 429'd for ~5 minutes straight, well past the 5-retry/~155s backoff window. Not fixable by any client-side change — tracked as a known external limitation, **issue #101 (open)**. Real code improvement shipped: `max_retries`/`backoff_seconds` (previously hardcoded literals) now exposed as `Settings.sourcing.gdelt_max_retries`/`gdelt_backoff_base_seconds`, plus a clearer exhausted-retries error message.
2. **Gate 2 not surfacing at all when sourcing returns zero subtopic candidates** — root cause: `fan_out_router`'s no-candidates fallback edge caused every `FAN_OUT_TARGET_NODES` node, including `_make_gate2_node`, to no-op via a `subtopic_id is None` guard instead of ever calling `interrupt()`, falling straight through to `END` with no Gate 2 prompt. Fixed: `_make_gate2_node` now always calls the real `gate2_node`/`interrupt()`, synthesizing an empty report via `build_gate2_report({})` on that path — a human always sees and confirms the run's outcome, even "nothing was found." New regression test `test_gate2_surfaces_an_empty_report_when_zero_candidates_are_approved`, confirmed to fail pre-fix. **Issue #102 (closed by PR #100).**

Tech-lead reviewed independently in a fresh clone: verified the retry-config extraction is non-behavior-changing (defaults match old hardcoded values), traced the pre-fix Gate 2 fallback path by hand, confirmed the non-empty-candidates path and other `FAN_OUT_TARGET_NODES` consumers (`sourcing`, `clustering`) are unaffected. `uv run ruff check .` clean; `uv run pytest` 192 passed / 8 deselected.

**Gate 2 fallback fix confirmed live (2026-08-02, `uv run newsresearch run "Taliban Takeover"`, PR #100 post-merge):** GDELT hit one 429 (attempt 1/5, no exhaustion this time — `_throttle()`/backoff behaving), sourcing still came back with zero subtopic candidates, and Gate 2 correctly surfaced its empty report (`cluster_sizes=[]`, `source_spread={}`, real "Press enter to continue" prompt) instead of silently completing — proves the PR #100 fallback fix works against real infra, not just the regression test.

**Root cause of the zero-candidates pattern found and fixed — ✅ closed (branch `task/gdelt-zero-candidates-fallback`, PR #103, merged).** Both live zero-candidate runs traced to two independent thresholds, either one alone sufficient to zero out a thin RSS-only batch — neither a bug in isolation, both tuned for GDELT-scale volume and never designed to degrade gracefully:
- **Cliff 1 — reputation filter** (`sourcing_agent.py:179`): `min_score_threshold=0.5`. Unknown-tier domains with zero corroborating WHOIS/backlink/HTTPS evidence score exactly `0.45` by the formula's own math (`base_score_unknown=0.3` + half of `adjustment_bound=0.30`) — the modal outcome for backfill-sourced domains, one tick under the bar by design, not by noise.
- **Cliff 2 — reconciliation match filter** (`subtopic_agent.py:175-177`): `reconciliation_match_threshold=0.60`. With a handful of survivors forming small/sparse clusters, every LLM-proposed candidate can miss 0.60 cosine similarity to any centroid — all dropped, no fallback existed.

data-scientist reproduced both against real thin-batch data (branch `feat/datascientist`, `notebooks/phase2_zero_candidates_threshold_review.md` — real GDELT-429-blocked runs on "Iraq WMD" and "Taliban" topics, not synthetic numbers) and recommended: a new `min_score_threshold_degraded=0.45` (used only when GDELT contributed zero articles), a new `reconciliation_match_threshold_small_pool=0.55` (used only when the clustering pool is below the existing `kmeans_fallback_threshold=20`), and building a structural fallback — when reconciliation drops every candidate but real clusters exist, surface them as generic unlabeled candidates rather than nothing.

backend-engineer wired exactly that (PR #103): both new thresholds gated precisely as recommended, non-degraded/large-pool paths byte-for-byte unchanged; structural fallback synthesizes `f"Cluster {id} ({n} articles, unlabeled)"` candidates scoped strictly to the all-unclaimed case; stage-by-stage count logging added (dedup/reputation-filter/reconciliation) so a future thin run is diagnosable from CLI output directly. **Bonus fix, not in scope but found while writing tests:** a pre-existing crash in `reconcile_candidates` (`sim_matrix.argmax(axis=1)` on a zero-length axis when clustering produces zero clusters) — added an early-return guard so "clustering finds nothing → empty candidates" actually holds instead of crashing.

Tech-lead reviewed independently in a fresh clone: confirmed the crash was real and pre-existing by literally reverting the guard and reproducing the `ValueError` against the same test; confirmed both thresholds fire only in their intended condition; confirmed the fallback candidate dict shape matches normal reconciled entries (`action`, `cluster_id`, `label`, `merged_from`, `article_count`, `centroid`) so Gate 1/Gate 2 consumers are unaffected. 18 tests pass, ruff clean. One non-blocking nit deferred: a Gate 1 UI display distinction between LLM-labeled and auto-clustered/unlabeled candidates — no UI exists yet to regress, revisit when Phase 6's Streamlit app is built.

**✅ Task 2.8.1 fully closed (2026-08-03, `uv run newsresearch run "Iraq and WMDs"`, run_id `run-ef8fff63-...`).** GDELT still persistently 429-blocked all run (issue #101, unrelated, expected), but PR #103's zero-candidates fallback fired correctly: `reconcile_subtopics: 0 candidates reconciled but 4 real cluster(s) found -- falling back to generic unlabeled candidates`, Gate 1 showed 4 editable unlabeled candidates, edit path exercised (kept all), and Gate 2 surfaced a real **non-empty** per-subtopic cluster report for the first time (`cluster_sizes=[1]`, `source_spread={'nature.com': 1}`, one real article) — the original acceptance bar for this task, finally met end-to-end against real infra, not a stub.

**New bug found by this same run, filed not fixed — issue #104 (open):** the one article that surfaced (a nature.com genomics paper on mouse colitis/spatial transcriptomics) is completely irrelevant to "Iraq and WMDs" — points to a real relevance/query-matching gap in RSS (`sourcing/rss.py`) and/or Google News backfill (`sourcing/google_news_backfill.py`) query construction, not yet triaged for root cause or dispatched to an engineer.

**Phase 2 fully closed.** All Story 2.8 Done-when bullets now hold against real infra: full CLI run through both real gates with a genuinely non-empty Gate 2 report (2.8.1), kill/restart durability (2.8.2), Langfuse trace confirmation (2.8.3). Remaining open threads are tracked as separate issues, not blockers: GDELT's external IP block (#101), and the new RSS/backfill relevance gap (#104). Phase 3 can start.

### Phase 2 — Role Ownership & Parallelization Re-analysis (`project-manager`, 2026-07-24) ✅ tech-lead approved, with three corrections applied in-line (2026-07-24)

**`tech-lead` review summary:** the frontend-engineer-zero-ownership citations were checked against their cited source lines and are accurate, not paraphrased-stronger. The DS→BE splits (2.1.2a/b, 2.2.1a/b) are clean design-vs-wiring boundaries with no interface ambiguity. The 5-wave dependency chain is internally consistent and each wave's formal task dependencies match its placement. Three real gaps were found and fixed directly in this section rather than left as blocking findings: (1) Task 2.2.3a's Acceptance line omitted the distinctiveness-score formula it was supposed to also deliver — the same design/wiring-bundling risk this re-analysis warns about elsewhere, applied reflexively to its own split; fixed by folding the formula into 2.2.3a's Acceptance line, with a downstream note correcting Task 2.2.4's stale `Depends on: 2.2.3` reference. (2) The Wave 1 "zero rework later" claim for stub-built Gate 1/2 mechanics overstated what Phase 0's actual `graph/state.py` supports today (checked directly: `GraphState`/`SubtopicState` have no candidate/excess/report fields yet) — corrected to distinguish "interrupt/resume/PostgresSaver mechanics need no rework" (true) from "payload-construction code is untouched from Wave 1 to Wave 4" (not true); Wave 1 should stub the payload *shape*, not just placeholder scalars. (3) The Story 2.8 "data-scientist co-signs" language risked reading as acceptance-verifier delegating its verdict rather than gathering evidence — clarified to keep acceptance-verifier's own re-derive-independently charter intact. None of these three findings change any task's stated Owner or wave placement, and none reopen the core proposal (splits, ownership table, wave order) for renegotiation — they tighten specific deliverable/dependency wording so a `backend-engineer`/`data-scientist` picking this up doesn't hit the exact ambiguity the fix closes. Task numbering/dependency references were spot-checked against the Phase 0/1/2 breakdowns; all cited task IDs (e.g. Phase 0 Tasks 0.6.1/0.6.3/0.5.2/0.3.1/0.7.2/0.8.1, Phase 1 Task 1.9.1) exist as cited, and no lettered sub-ID collides with an existing task ID.

**Why this exists:** Phase 0/1 only ever used `project-manager` / `tech-lead` / `backend-engineer` / `devops-engineer` / `acceptance-verifier` — no ML/prompt-design or UI work existed yet. Phase 2 is the first phase where `data-scientist` (prompt design, embedding/clustering algorithm choice and tuning, output-quality evaluation — explicitly **not** production wiring or git/PR management) becomes real work, and the first phase close enough to Phase 6 to need an explicit "not yet" on `frontend-engineer`. Nothing below changes any task's acceptance criterion from the Phase 2 breakdown above — it only annotates who does it, calls out where a task should be split by design-vs-wiring, and proposes a wave order. Task numbering above is preserved as-is (no renumbering); any proposed split is given a lettered sub-ID (`2.1.2a`/`2.1.2b`, etc.) and is a **proposal**, not adopted until approved.

**`frontend-engineer`: zero ownership in Phase 2, confirmed, not silently assumed.** Three separate places in this document already say so directly:
- Cross-Cutting Concerns, "Dev-time pipeline harness" (line 31): "Streamlit (Phase 6) is deliberately last, but Phases 2–5 all involve blocking human gates... Resolve by repurposing `main.py` into a `typer` CLI from Phase 0 onward." Gate 1/Gate 2 UX in Phase 2 is explicitly delivered via the CLI, not a UI framework.
- Phase 6's own header and Story 6.x preamble: "Usability only — deliberately last, no new pipeline logic."
- Suggested Build Order Summary table, Phase 6 row: "Usability only — deliberately last, no new pipeline logic."

Story 2.7 (CLI harness rendering both gates) is therefore `backend-engineer`-owned dev tooling, not a `frontend-engineer` task, even though it's the closest thing Phase 2 has to a "UI."

**Ownership annotation, story by story:**

| Story/Task | Owner | Rationale / handoff |
|---|---|---|
| 2.0 (lib adds) | `backend-engineer` | Trivial `uv add`; libraries (`hdbscan`, `scikit-learn`) already chosen by the TRD, not a live DS decision. |
| 2.1.1 `clustering/embeddings.py` wrapper | `backend-engineer` | Thin wrapper over Phase 0's `get_embeddings()` — no algorithm/tuning content. |
| 2.1.2 `cluster.py` (HDBSCAN/KMeans) | **split, DS→BE handoff** (see proposed 2.1.2a/2.1.2b below) | Choosing HDBSCAN hyperparameters (`min_cluster_size`, `min_samples`) and the KMeans-fallback article-count threshold is a real clustering-algorithm decision — `data-scientist` territory per their scope (embedding/clustering algorithm choices). Productionizing the chosen defaults behind `Settings.clustering.*` and writing the fixed-fixture unit test is `backend-engineer` territory. |
| 2.2.1 subtopic-propose prompt + schema | **split, DS→BE handoff** (see proposed 2.2.1a/2.2.1b below) | Prompt content/iteration for subtopic-proposal quality is `data-scientist` work (prompt design is explicitly in their scope); wiring it through `get_chat_model("subtopic").with_structured_output(...)` per the LLM-layer convention (NFR-6) and confirming the Langfuse trace is `backend-engineer` work — must route through the existing `llm/models.py`/prompt-template convention, not a parallel path. |
| 2.2.2 broad topic-scoped fetch | `backend-engineer` | Pure re-use of Phase 1's already-approved `sourcing_agent`; zero algorithm/prompt content. |
| 2.2.3 embed+cluster broad set + reconciliation (merge/split/drop) | **split, DS→BE handoff** (see proposed 2.2.3a/2.2.3b below) | The merge/split/drop *matching rules and similarity thresholds* are an algorithm-design decision (same family as clustering tuning) — `data-scientist`. Wiring the decided rules into `subtopic_agent.py` against Story 2.1's `cluster()` and covering merge/split/drop with unit tests is `backend-engineer`. |
| 2.2.4 rank/cap/excess-retention | `backend-engineer`, consumes a DS-defined metric | The "distinctiveness score" formula itself should be specified by `data-scientist` as part of 2.2.3a's design output (documented, not invented ad hoc by the implementer); ranking/truncation/excess-bookkeeping logic against that formula is deterministic `backend-engineer` work. |
| 2.3 Gate 1 (interrupt/resume, kill-restart durability) | `backend-engineer` | Pure LangGraph/Postgres mechanics — no ML or prompt content at all. Can be built against a **stub** candidate list (see parallelization below), doesn't need 2.2's real output to exist first. |
| 2.4 fan-out (`Send`) | `backend-engineer` | Pure LangGraph wiring. |
| 2.5.1 per-subtopic topical clustering | `backend-engineer` wires; `data-scientist` validates | Wiring is Phase-1-sourcing + Story-2.1's `cluster()`, both already-decided — `backend-engineer`. But the acceptance criterion is explicitly "manually spot-checked, since clustering quality is this phase's flagged risk" — that spot-check judgment (does this grouping look right on real news articles) is squarely `data-scientist` output-quality-evaluation scope, not something `backend-engineer` or `acceptance-verifier` should sign off on alone. |
| 2.6.1 `reports/gate2_report.py` (zero-LLM aggregation) | `backend-engineer` | Pure aggregation, explicitly zero LLM calls — no DS content. |
| 2.6.2 `graph/nodes/gate2.py` interrupt node | `backend-engineer` | Pure LangGraph mechanics, same as Gate 1. Also stub-buildable in parallel (see below). |
| 2.7 CLI harness extension | `backend-engineer` | Dev-tooling per Cross-Cutting Concerns' CLI-harness paragraph — **not** `frontend-engineer`; see confirmation above. |
| 2.8 Done-when verification | `acceptance-verifier` executes; `data-scientist` co-signs 2.5.1's quality bullet | Matches Phase 0/1 precedent (`acceptance-verifier` runs the phase-level walkthrough independently). The clustering-quality portion of "Done when" specifically should carry a `data-scientist` sign-off alongside `acceptance-verifier`'s mechanical checks (gate durability, Langfuse traces), since "is this clustering plausible" isn't a mechanical check. **`tech-lead` clarification, to keep this from blurring `acceptance-verifier`'s "never on trust, always re-derives" charter:** `acceptance-verifier`'s own operating rules already treat this exact kind of judgment call (manual, non-mechanical quality review) as legitimate `UNVERIFIABLE HERE` territory rather than something it resolves itself. The "co-sign" here should mean: `data-scientist` produces the dated, evidence-backed clustering-plausibility write-up (their own real deliverable, in scope per their charter); `acceptance-verifier` then verifies *mechanically* that such a write-up exists, is dated to this run, and actually corresponds to the real output being checked (not a stale or unrelated review) — and reports that as its evidence for this bullet, rather than either (a) rendering its own clustering-quality opinion it isn't equipped to hold, or (b) adopting `data-scientist`'s "looks plausible" as unquestioned ground truth. If no such write-up exists at verification time, `acceptance-verifier` still reports the bullet as `UNVERIFIABLE HERE` / not met, exactly as its charter already directs — the co-sign is an input to `acceptance-verifier`'s evidence, not a delegation of its verdict. |

**Proposed task splits (new sub-IDs, not yet adopted — flagging per the same convention as Story 1.12's "proposed" framing, pending approval):**

- **Task 2.1.2a** (`data-scientist`): offline prototyping/evaluation — compare HDBSCAN vs. KMeans against a fixture/sample article-embedding set (can reuse real embeddings pulled from a Phase 1 live sourcing run, or a constructed multi-cluster fixture), recommend `min_cluster_size`/`min_samples` defaults and the `kmeans_fallback_threshold` value. Acceptance: a short written recommendation (notebook or doc) with the chosen defaults and the reasoning, committed alongside `tests/fixtures/` sample data used to derive them.
  Depends on: none (needs only Phase 0's `get_embeddings()`, already merged) — can start immediately, in parallel with any `backend-engineer` Phase 2 work.

  ✅ tech-lead approved (design-deliverable review, 2026-07-24) — `notebooks/phase2_clustering_eval.py`, `notebooks/phase2-clustering-recommendation.md`, `tests/fixtures/clustering_synthetic_topics.json` (branch `origin/task/phase2-clustering-subtopic-prompt-design`). `min_cluster_size=4`/`min_samples=2` are genuinely earned from the ARI sweep (0.781 peak at mcs=4 vs. 0.71/0.55/0.32 at neighboring settings, on a fixture deliberately re-designed to be hard — a first easier fixture scored a trivial ARI=1.0 everywhere and was correctly discarded); `kmeans_fallback_threshold=20` is a documented, reasoned judgment call (triggers while HDBSCAN ARI is already weak at 0.24, before it collapses to 0 at n=12) rather than an asserted number, and is honestly labeled a starting point pending real-run recalibration. The fixture is a real discriminating test (not trivially separable — an incorrect clustering implementation would not land near ARI 0.78), uses real precomputed local-backend embeddings, and needs no network access at test time. Zero production code touched; `uv run ruff check` clean on the new `.py` file.
  **Required before Wave 2 treats the numbers as final:** the sweep was run against `sklearn.cluster.HDBSCAN`, not the standalone `hdbscan` package named in `TRD.md` §3, because Task 2.0.1 hadn't landed in this worktree. `sklearn.cluster.HDBSCAN` (sklearn ≥1.3) is a from-scratch reimplementation of the same McInnes et al. algorithm, not a wrapper around the standalone package — it shares the same core algorithm, default `cluster_selection_method="eom"`, and default `metric="euclidean"`, so `min_cluster_size`/`min_samples` should carry over as the same concept and the *qualitative* conclusion (mcs=4 is the peak, KMeans wins when k is known, HDBSCAN degrades below ~20 points) is almost certainly correct in either implementation. But the two libraries diverge in MST/tree-construction internals, tie-breaking, and available `algorithm`/`cluster_selection_epsilon` options, so exact ARI values are not guaranteed to reproduce bit-for-bit against the standalone `hdbscan` package. `backend-engineer` must re-run `phase2_clustering_eval.py` (swapping the import to `hdbscan.HDBSCAN`) against the same committed fixture as a Task 2.1.2b sanity check before locking `min_cluster_size=4`/`min_samples=2`/`kmeans_fallback_threshold=20` in as final `Settings.clustering.*` values — cheap to do (same fixture, same script shape), and this is exactly the caveat the data-scientist's own doc already flags, not a new finding.
  One methodology nit, not blocking: the subsample sweep that derived `kmeans_fallback_threshold=20` ran at the programmatically-selected "best" full-fixture setting, which resolved to `min_samples=1` (first tie-break in iteration order) even though the final recommendation is `min_samples=2`; the doc states the two were tied on the full fixture, so this is very unlikely to change the threshold, but `backend-engineer` should note it when re-running against real `hdbscan`.

  **Resolved by Task 2.1.2b (see that task's approval note above):** the suspected divergence was real. Against the real `hdbscan` package, `min_samples=2` collapses to ARI=0.324 (2 of 4 clusters found); `min_samples=1` reproduces ARI=0.781. `Settings.clustering.hdbscan_min_samples` is locked in at `1`, not the originally-recommended `2` — independently re-verified by `tech-lead`, not just accepted from the branch's reported numbers. `kmeans_fallback_threshold=20` held up unchanged (same degradation curve at the corrected `min_samples=1`). This also resolves the tie-break nit above: the "best full-fixture setting" the subsample sweep locked onto (`min_samples=1`) turned out to be the *correct* final value against the real package, not an accidental artifact of iteration-order tie-breaking as originally worried.

- **Task 2.2.1a** (`data-scientist`): draft and iterate `llm/prompts/subtopic_propose.txt` + the candidate-list schema shape, evaluated against several representative sample topics for output quality (plausible, distinct, non-overlapping candidate subtopics). Acceptance: a documented set of sample-topic outputs judged plausible, prompt file committed under the existing `llm/prompts/` convention (Phase 0 Task 0.6.3), schema fields proposed in `llm/schemas.py`-ready form.
  Depends on: Phase 0 Task 0.6.3 (prompt/schema convention scaffold)

  ✅ tech-lead approved (design-deliverable review, 2026-07-24) — `newsresearch/llm/prompts/subtopic_propose.txt`, `notebooks/phase2-subtopic-prompt-design.md` (same branch). Prompt is well-formed against the `ChatPromptTemplate.from_template()` convention (plain `{topic}`/`{n_candidates}` placeholders) and the proposed `SubtopicCandidate`/`SubtopicCandidateList` schema is genuinely paste-ready for `llm/schemas.py`. Rule 4 (no political-leaning/outlet/audience-framed candidates) is a sound upstream guard, not scope creep into Phase 4's job: it traces directly to PRD's flagged risk against reintroducing a scalar political-lean axis and TRD §4.6's "descriptive, not a rigid left/center/right scale" framing-label design — it prevents the *candidate list itself* from smuggling an ideology axis into what's supposed to be a topic/angle decomposition step; it performs no framing/bias analysis of any article and does not duplicate Phase 4's actual work. The no-`OPENAI_API_KEY` limitation is represented honestly — the 4 sample-topic outputs are explicitly and repeatedly marked "ILLUSTRATIVE / EXPECTED — not a real model call," not presented as real output, and the doc names its own methodology gap (can't catch real-model failure modes like ignoring the "propose fewer" instruction or drifting into rule-4-violating framing language) rather than glossing over it.
  **Required before Wave 2 is done, not before it starts:** `backend-engineer`'s Task 2.2.1b acceptance (Langfuse trace confirmation) already forces a real API call, so re-running these same 4 sample topics against real output is a natural, already-required part of that task — call this out explicitly in 2.2.1b's own work, not a new gate.
  One doc nit, not blocking: `phase2-subtopic-prompt-design.md` has one stale cross-reference to `docs/phase2-clustering-recommendation.md` — the actual file lives at `notebooks/phase2-clustering-recommendation.md` (correctly, since `docs/` is gitignored per PRD/TRD convention and the deliverable was correctly relocated to `notebooks/`). Cosmetic; `backend-engineer` should not be confused since the real path is discoverable via the wave's other file list, but worth a one-line fix whenever this doc is next touched.
- **Task 2.2.1b** (`backend-engineer`, was Task 2.2.1): wire the DS-authored prompt via `get_chat_model("subtopic").with_structured_output(Schema)`, confirm the Langfuse trace. Acceptance: unchanged from the original Task 2.2.1 acceptance above.
  Depends on: 2.2.1a, Phase 0 Task 0.6.1

  ✅ tech-lead approved, ready to merge (branch `origin/task/2-2-1b-subtopic-propose`, now at commit `c1a6edf`) — **both previously-open items are now genuinely closed, re-reviewed and independently re-verified, not just accepted from the implementer's report.** `llm/schemas.py`'s `SubtopicCandidate`/`SubtopicCandidateList` is a faithful, near-verbatim port of Task 2.2.1a's design-doc proposal (field names, descriptions, and the "may be shorter than n_candidates, no length validation" design intent all carried over correctly). `agents/subtopic_agent.py::propose_candidates` loads `subtopic_propose.txt` via `(_PROMPTS_DIR / ...).read_text()` + `ChatPromptTemplate.from_template()`, matching Phase 0's `llm/prompts/example.txt`-established convention exactly, and correctly leaves `broad_topic_fetch` (Task 2.2.2, already merged) untouched. `get_chat_model("subtopic").with_structured_output(SubtopicCandidateList)` is the correct NFR-6 single-factory call, no parallel model-construction path.

  **Item 1 (config-forwarding), resolved:** `propose_candidates` now accepts `config: RunnableConfig | None = None` and merges it with the Langfuse callback/`trace_metadata` via `langchain_core.runnables.config.merge_configs` (`merged_config = merge_configs(config, call_config)`) rather than replacing it — matching `graph/build.py::_make_subtopic_stub_node`'s established propagation pattern, so `cost_callback.py`'s ambient callback will keep firing once Task 2.2.3b wires this into a real graph node. Confirmed the new `test_propose_candidates_merges_ambient_config_callbacks` test is a genuine assertion, not a doesn't-crash check: it builds an `ambient_config` with its own `ambient_callback` + `metadata={"existing_key": "existing_value"}`, invokes with both that and a mocked Langfuse callback, and asserts on the resulting `config["callbacks"].handlers` that **both** `ambient_callback` and `langfuse_callback` are present, and on `config["metadata"]` that `existing_key` survives alongside the newly-added `run_id`/`stage` keys. Read the full diff myself (not just the test names) — this is real merge-semantics coverage.

  **Item 2 (real-API verification), resolved, independently re-run — not accepted on the implementer's report alone:** with the now-live `OPENAI_API_KEY`, I called `propose_candidates("renewable energy policy in the European Union", n_candidates=8, run_id="run-tech-lead-independent-verify")` myself from a fresh worktree at this commit and got a real, schema-conformant `SubtopicCandidateList` of 8 distinct, non-overlapping candidates (real `ChatOpenAI` output, not a fixture). Queried `langfuse-clickhouse` directly (not the Langfuse UI, and not trusting the implementer's ClickHouse numbers) and confirmed a real `RunnableSequence` trace tagged `run_id:run-tech-lead-independent-verify`, with a child `GENERATION` observation on `provided_model_name = gpt-4.1-2025-04-14`, real `usage_details` (`input=749, output=330, total=1079`) and a real non-null `total_cost` (`$0.004138`) — structurally identical to, and corroborating, the implementer's own reported drill-down (747/329/1076 on their different sample topic). The acceptance criterion ("real API call + Langfuse trace") is now genuinely verified by two independent parties against the live stack, not asserted.

  **Secret-hygiene check, clean:** `git log --all --diff-filter=A --name-only` and `git log -p --all -- .env` on this branch show `.env` was never committed at any point in this branch's history; `.gitignore` covers `.env`/`.env.development`/`.env.production`; only `.env.example` is tracked; `git diff master origin/task/2-2-1b-subtopic-propose` contains no `sk-`-shaped or `api_key=`-literal strings. The implementer's claim of removing their temporary `.env` copy from the worktree afterward is consistent with what's actually in the branch — no leaked secret, no accidental dependency on a committed key.

  **Test/lint re-run, my own, in a fresh worktree at `c1a6edf`:** `uv run pytest -q` gives 154 passed, 1 failed, 7 deselected — the 1 failure is `test_sourcing_agent_raises_without_database_url_or_pool`, reproduced only because I copied `.env` into the worktree to get a live `OPENAI_API_KEY` for the real-API check above; this is the same pre-existing, already-documented `.env`-leaking-`DATABASE_URL` artifact called out in Task 2.1.2b's approval note above (unrelated to this branch, and absent when `.env` isn't present — 155 passed / 7 deselected in that case, matching the task description's expectation). `uv run ruff check .`: clean.

  No scope creep: no parallel config/LLM-call/cost-logging path introduced, `broad_topic_fetch` untouched, and the final `propose_candidates(topic, n_candidates=8, *, run_id="dev", settings=None, config=None)` signature is a sound, ambient-config-forwarding building block for Task 2.2.3b's orchestration. **Verdict: approve, ready to merge, no further changes required.**

- **Task 2.2.3a** (`data-scientist`): design the merge/split/drop matching rules and similarity thresholds (e.g. what embedding-distance/cluster-overlap condition constitutes "candidate maps to cluster" vs. "cluster spans multiple candidates"), validated against constructed fixture scenarios for each of merge/split/drop. Also specifies the distinctiveness-score formula consumed by 2.2.4. Acceptance: documented rule set + threshold values (feeding `Settings` where applicable) + the fixture scenarios used to validate each branch **+ the distinctiveness-score formula itself, written down (not just referenced in prose) as part of this task's deliverable, since Task 2.2.4 cannot be implemented without it.** (`tech-lead` note: the original draft of this task specified the formula only in the surrounding prose, not in the Acceptance line — the same design-vs-wiring bundling risk this whole re-analysis warns about, reflexively applied to itself. Fixed here.)
  Depends on: 2.1.2a (shares the same offline-evaluation track)

  ✅ **tech-lead approved (design deliverable), ready for `backend-engineer` to consume in Task 2.2.3b** (branch `origin/feat/datascientist`, not yet merged; reviewed at commit `d8f58c0`) — `notebooks/phase2-reconciliation-design.md` + `notebooks/phase2_reconciliation_eval.py` are a complete, correct, implementable-against spec, not just a plausible narrative. Independently re-ran `phase2_reconciliation_eval.py` from a fresh worktree (own execution, not trusting the doc's reported numbers): all three scenarios (merge/split/drop) pass their assertions, exit code 0, and every cosine-similarity value the design doc cites in prose (0.866/0.861/0.828 for the merge case, 0.776/0.795/0.453 for the split case, 0.525/0.422/0.866 for the drop case) matches this independent re-run's output exactly — the two narrated design corrections (per-article-vote heuristic replaced by direct candidate-candidate comparison after it produced a 5/3 split on a genuine near-duplicate pair; `MATCH_THRESHOLD` raised from an initial 0.42 to 0.60 after 0.42 wrongly admitted an unsupported candidate at 0.525) are real, reproduced corrections, not retrofitted narrative. The merge condition (best-cluster similarity ≥ 0.60 AND pairwise candidate-candidate similarity ≥ 0.65) is coherent and well-motivated: both thresholds are derived from an actual measured gap on real embeddings (0.776-0.908 genuine match vs. 0.363-0.525 unsupported-same-domain; 0.828 near-duplicate vs. 0.453/0.483 genuinely-distinct), not chosen a priori, and the doc states plainly that this is one fixture's worth of evidence, not a certified calibration. The split rule's per-article reassignment (closest claim-group by best-matching claimant) is sound, and the merge/split decision boundary is unambiguous by construction: `reconcile()`'s claim-group step is single-linkage union-find over *all* claimant pairs, not a hardcoded 2-claimant special case, so a 3-claimant scenario with 2 similar + 1 dissimilar claimant resolves deterministically into a 2-group split without any special-casing needed. That said, none of the three committed fixtures actually exercises a ≥3-claimant scenario — the algorithm's generalization to that case is code-reviewed-sound but not empirically validated here. Not a blocking gap against this task's stated acceptance (merge/split/drop, one fixture each, is what was asked for), but flagged as a recommended (not required) addition to Task 2.2.3b's unit test suite. The drop rule's shared 0.60 boundary with the merge rule's accept condition is intentional and correct, not an off-by-one: it's the same "is this candidate's best cluster a real match at all" question asked from two directions (claim vs. reject), and the measured gap (0.525 max-unsupported vs. 0.776 min-genuine) puts 0.60 comfortably in the middle with no boundary-condition risk in either constructed scenario. The distinctiveness formula (`0.5 * volume_norm(s) + 0.5 * avg_pairwise_distance(s)`) is written down explicitly and completely per the corrected Acceptance line above (not left in prose only) — Task 2.2.4 can implement directly from it. The `n == 1` edge case (single surviving subtopic, no peer) is explicitly defined (`avg_pairwise_distance = 0.0`, no divide-by-zero) both in the doc's formula block and in the reference implementation. Re-ran `scenario_distinctiveness` independently and reproduced the exact reported table (0.362/0.335/0.316/0.309 for the four merge-scenario subtopics). Settings-ready naming (`reconciliation_match_threshold`, `reconciliation_dup_threshold`, `distinctiveness_volume_weight`/`distinctiveness_distance_weight`) fits `ClusteringSettings`'s existing convention cleanly and does **not** collide with the two fields already on `ClusteringSettings` in `newsresearch/config.py` on `master` today: `similarity_threshold` (Phase 1's `dedup.py` title-similarity threshold, unrelated) and `subtopic_match_threshold` (reserved for Phase 5's cross-run subtopic-matching, Story 5.3 — a different comparison entirely, current-vs-*prior-run* subtopic embeddings, not candidate-vs-cluster reconciliation). Confirmed via `grep` that `subtopic_match_threshold` is presently unused in `master`'s code (only referenced in `test_config.py`'s type-check test) but is legitimately spoken for by Phase 5, not an orphaned stub this task should have repurposed. The unclaimed-clusters gap (real article cluster, zero LLM claimant → dropped, never force-labeled) is the right v1 default, my own judgment and not just a rubber-stamp of the flag: force-labeling an unclaimed cluster would require inventing a new LLM-labeling path this task was never scoped for (real scope creep risk into Task 2.2.1/2.2.4 territory) purely to guard against a `n_candidates`-undershoot edge case that the doc's own recommended mitigation (log unclaimed-cluster count/sizes per run) already handles as a cheap, correctly-deferred telemetry decision — resolving it now would be manufacturing false completeness, not real safety. Confirmed zero production code touched: `git diff master origin/feat/datascientist -- newsresearch/` is empty (not just `cluster.py`/`subtopic_agent.py` — the whole package). `uv run ruff check notebooks/phase2_reconciliation_eval.py`: clean. All three `tests/fixtures/reconciliation_*.json` files contain only short synthetic candidate labels/rationales + embedding vectors, no full article text — spot-checked directly, matching the no-full-text-persistence convention. **Verdict: approve, no changes required to this branch.** One forward-looking, non-blocking note for `backend-engineer`'s Task 2.2.3b: add at least one ≥3-claimant mixed-similarity unit test case (not present in the DS's three fixtures) to empirically confirm the generalized union-find grouping behaves as the code implies before relying on it for real Gate-1 candidate counts (recommended default 8) where 3+ claimants per cluster is plausible.
- **Task 2.2.3b** (`backend-engineer`, was Task 2.2.3): implement 2.2.3a's rules in `subtopic_agent.py`, unit-tested per branch. Acceptance: unchanged from the original Task 2.2.3 acceptance above.
  Depends on: 2.2.3a, 2.1.2b, 2.2.1b, 2.2.2

  ✅ **tech-lead approved, ready to merge (branch `origin/task/2-2-3b-2-2-4-reconciliation`, Tasks 2.2.3b + 2.2.4)** — reviewed `newsresearch/config.py`, `newsresearch/agents/subtopic_agent.py` (`reconcile_candidates`/`reconcile_subtopics`/`rank_and_cap_subtopics`), `tests/test_subtopic_reconciliation.py` against the design doc, `phase2_reconciliation_eval.py`'s reference `reconcile()`, and the Cross-Cutting Concerns. `ClusteringSettings.reconciliation_match_threshold`(0.60)/`reconciliation_dup_threshold`(0.65)/`distinctiveness_volume_weight`/`distinctiveness_distance_weight`(0.5/0.5) match the design doc exactly, name and comment in the same style as `hdbscan_min_cluster_size` etc., and don't collide with `similarity_threshold`/`subtopic_match_threshold`. `reconcile_candidates` is a faithful line-for-line port of the eval script's `reconcile()` — same sim-matrix/argmax claim step, same union-find (`find`/`union` over `group_of`, unchanged), same merge-vs-split branch on `len(groups)`, same per-article group reassignment for splits; no reinterpretation. Unclaimed clusters are dropped (never force-labeled) and `reconcile_subtopics` logs the count/ids, matching the design doc's explicit default. `rank_and_cap_subtopics` implements `volume_weight*volume_norm + distance_weight*avg_pairwise_distance` off `Settings.clustering.*` (not hardcoded 0.5/0.5, a legitimate tightening beyond the doc's literal formula since the weights are already Settings fields), truncates to `Settings.pipeline.max_subtopics` via a plain slice, and returns every excess subtopic under a separate `"excess"` key — confirmed via `test_rank_and_cap_truncates_and_retains_excess` (7 subtopics capped to 5, the other 2 recoverable, checked by label set) that nothing is silently dropped; the `n==1` case is handled (`others` empty → `avg_dist = 0.0`, no divide-by-zero), covered by `test_rank_and_cap_single_subtopic_has_zero_avg_pairwise_distance`. Test suite reuses the data-scientist's actual committed fixtures (`reconciliation_{merge,split,drop}.json`, loaded byte-for-byte, not reinvented) for the three base scenarios, and adds a real, non-trivial ≥3-claimant test (`test_reconcile_three_claimants_mixed_similarity_splits_two_from_one`) using hand-built unit vectors with checked cosine similarities (A/B ≥0.65, A/C and B/C <0.65, asserted directly before exercising `reconcile_candidates`) — this is the forward-looking gap I flagged when approving Task 2.2.3a, now closed with a case that would genuinely fail (3-way merge or 3-way split) if the union-find grouping were buggy, not one that passes regardless. No new dependency added (`git diff master...this-branch -- pyproject.toml uv.lock` empty) — cosine/union-find are plain numpy/stdlib, matching the lazy-coding constraint. `newsresearch/graph/nodes/gate1.py` confirmed untouched (empty diff) — the `reconcile` hook is still `stub_reconcile`, correctly out of this branch's scope. `uv run pytest`: 165 passed, 7 deselected, exact match to expectation. `uv run ruff check .`: clean. No scope creep, no parallel config/LLM-call/cost-logging path. **Forward note for the next task (wiring into `gate1.py`'s `reconcile` parameter):** `reconcile_subtopics` currently returns raw `reconcile_candidates` output (with `centroid` arrays still attached) plus `total_articles`; the wiring task needs to run `rank_and_cap_subtopics` on `reconcile_subtopics`'s `"reconciled"` list before handing anything to `gate1.py`'s `interrupt()` payload, since `rank_and_cap_subtopics` is the step that strips the non-JSON-safe `centroid` field — passing `reconcile_subtopics`'s raw output straight into a Gate 1 payload would leak numpy arrays into what's meant to be a durable, human-readable interrupt payload. Not a defect in this branch (rank/cap is correctly a separate, composable function per Task 2.2.4's own scope), just a sequencing note for whoever wires Task 2.3.1's real Gate 1 integration.

  ✅ **tech-lead approved, ready to merge (branch `origin/task/2-3-1-gate1-real-reconcile`, PR #28) — Task 2.3.1 fully closed** — reviewed `newsresearch/graph/nodes/gate1.py` and `tests/test_gate1.py` against the forward note above and this section's Wave 3 scoping. `make_real_reconcile(articles, *, settings=None)` builds a closure calling `reconcile_subtopics(articles, subtopic_candidates, settings=settings)` and then unconditionally `rank_and_cap_subtopics(reconciled["reconciled"], reconciled["total_articles"], settings=settings)` before returning `capped["candidates"]` — the exact order flagged as the sequencing risk, with no branch that could skip rank/cap and no path where raw `centroid` arrays reach the `interrupt()` payload; confirmed by the new test's explicit `assert "centroid" not in c`. `make_gate1_node`'s signature/type hint (`reconcile: ReconcileFn = stub_reconcile`, `ReconcileFn = Callable[[list[dict]], list[dict]]`) is untouched — `make_real_reconcile` returns a plain closure matching that exact hook shape, so no `gate1_node` signature change was needed, matching the design promised at Wave 1 approval. `stub_reconcile` is kept, unchanged, as the default, and the four pre-existing Wave 1 mechanics tests (approve-resume, pluggable-hook identity, bad-action, kill/restart durability) still pass against it. `test_gate1_edit_resume_runs_real_reconciliation` is a real proof, not a smoke test: it reuses `clustering_synthetic_topics.json` and `reconciliation_merge.json` byte-for-byte (no new fixtures invented), monkeypatches only `subtopic_agent.embed` (dispatched by input length to return either the article or candidate fixture vectors — no live embedding-model call), and lets `cluster()`/`reconcile_subtopics`/`rank_and_cap_subtopics` run unmocked end to end through the real `interrupt()`/`Command(resume=...)`/`PostgresSaver` graph built by the existing `_build_gate1_graph` helper. The edit drops one of the fixture's four candidates, and the assertions check recomputed structure, not just non-crash: `len(result["candidates"]) == 2` (real merge collapsed two near-duplicate EU AI Act labels), the merge's `merged_from` set matches the two specific labels, every surviving candidate has `article_count > 0` and a `distinctiveness_score` key populated by real reconciliation math, and `centroid` is absent — this would fail if `rank_and_cap_subtopics` were skipped or if reconciliation silently passed the edited list through unchanged. Ran independently in an isolated worktree off `origin/task/2-3-1-gate1-real-reconcile`: `uv run pytest` → 166 passed, 7 deselected (165 prior + this one new test, no regressions, no stray-env pre-existing failure reproduced); `uv run ruff check .` → clean. No scope creep — diff is exactly `gate1.py` + `test_gate1.py`, no `graph/build.py` topology touched, no parallel config/LLM-call/cost-logging path introduced (uses existing `Settings`/`subtopic_agent` entrypoints throughout). **Forward note for Task 2.3.2 and Wave 4:** Task 2.3.2's kill/restart durability test still runs against the stub-payload shape (per its own "regardless of what real data later populates the same fields" scoping) — that property is orthogonal to reconciliation and does not need re-verification from this merge alone, but Wave 3's plan explicitly calls for a re-check once real data flows through Gate 1 end-to-end (i.e. once Task 2.4.1's fan-out feeds real `articles`/candidates in from the actual subtopic agent, not a hand-built fixture state as here) — don't treat this branch's fixture-driven test as having already discharged that re-check. Wave 4's Gate 2 stub-swap and fan-out work are unaffected; `make_real_reconcile`'s `articles` parameter is exactly the shape `Send`-based fan-out will need to bind per-branch once 2.4.1 lands.

**Downstream dependency correction (`tech-lead`):** Task 2.2.4's Depends-on line above (line ~470, "Depends on: 2.2.3, Phase 0 Task 0.3.1") predates this split and still names the now-superseded single ID `2.2.3`. Once this re-analysis is adopted, read Task 2.2.4's dependency as **`2.2.3a` (for the distinctiveness-score formula) and `2.2.3b` (for the reconciled-cluster output it ranks)**, not the original `2.2.3`. Left as a note here rather than silently rewriting the original Task 2.2.4 entry, so the original breakdown's text stays an accurate historical record of what was written before this proposal.

**Parallelization plan (waves), accounting for the DS/BE split above — mirrors Phase 1's "waves of parallel `backend-engineer` work, sequential only where a real dependency exists" pattern:**

- **Wave 1 (fully parallel, no cross-track dependency):**
  - `backend-engineer` track A: Task 2.0.1 (lib adds), Task 2.1.1 (embeddings wrapper) — both trivial, unblock nothing else in this wave.
  - `backend-engineer` track B: build Gate 1 (Story 2.3) and Gate 2 (Story 2.6.2) interrupt/resume mechanics **against a stub candidate list / stub cluster output** shaped like Phase 0's state schema — the LangGraph `interrupt()`/`Command(resume=...)`/`PostgresSaver` durability work needs no real subtopic-agent or clustering output to exist, only the state shape. This is the concrete case the user asked about: gate mechanics and the eventual "real" data source are genuinely independent. **`tech-lead` correction to the "zero rework later" framing:** confirmed against the merged `graph/state.py` — `GraphState` today only carries `topic`, `canonical_topic`, `run_id`, `subtopics: list[str]`, `approved: bool`, and `SubtopicState` only `run_id`, `subtopic_id`, `label`. Neither has a field yet for a candidate-with-excess payload (Gate 1) or a per-subtopic cluster report (Gate 2) — Phase 0 defined the *topology* placeholder, not Phase 2's actual payload shape. The `interrupt()`/`Command(resume=...)`/`PostgresSaver` durability mechanics genuinely need no rework when the stub is swapped for real data — that part of the claim holds. But the gate nodes' *payload-construction* code (what fields get packaged into the `interrupt()` call for the human to read) will still need to be written/extended in Wave 3/4 once `GraphState`/`SubtopicState` gain the real candidate/excess/report fields — that's ordinary incremental schema growth, not a redesign, but it is code that gets touched twice, not zero. Wave 1's Gate 1/2 build should stub the *payload shape* (e.g. a placeholder `candidates: list[dict]` / `excess: list[dict]` / `cluster_report: dict`) deliberately, not just a bare `bool`/`str`, so the Wave 3/4 swap is a field-population change rather than a payload-shape change.
  - `backend-engineer` track C: Task 2.2.2 (broad fetch reusing Phase 1's `sourcing_agent`) — no dependency on anything else in Phase 2.
  - `data-scientist` track: Task 2.1.2a (clustering prototyping/eval) and Task 2.2.1a (subtopic-propose prompt design) in parallel with each other — both are offline/notebook/prompt-playground work against fixture or sample data, no code dependency on any `backend-engineer` Phase 2 output.

  **✅ tech-lead approved, ready to merge (branch `task/gate1-gate2-interrupt-mechanics`)** (2026-07-24) — **Wave 1 track B mechanics only**, not full Story 2.3/2.6.2 acceptance (that still needs Wave 3/4's real reconciliation and `Send`-based fan-out, per this section's own scoping). Reviewed: `graph/state.py`'s additions (`GraphState.candidates`/`excess: {"label": str, "article_count": int}`, `SubtopicState.cluster_report: {"cluster_sizes": list[int], "sample_headlines": list[str], "source_spread": dict[str, int]}`) are purely additive (no field removed/renamed; confirmed via diff against `master`) and match this section's own corrected Wave 1 guidance (stub the payload *shape*, not a bare scalar) and Tasks 2.2.4/2.6.1's eventual real output shape. `graph/nodes/gate1.py` uses real `langgraph.types.interrupt()`/`Command(resume=...)`, handles both `approve` and `edit` resume paths, and exposes a pluggable `reconcile` hook defaulting to a documented identity stub (`stub_reconcile`, explicit docstring pointing at Task 2.2.3b) — not silently passed off as real reconciliation. `graph/nodes/gate2.py` is the same mechanics, and `tests/test_gate2.py::test_gate2_blocks_each_subtopic_branch_independently` proves the actual property (one branch's pending interrupt does not block a sibling's independent resume) via distinct `thread_id`s against one shared `PostgresSaver`-backed graph — a fair substitute for real `Send`-based fan-out, since LangGraph's own branch-isolation unit is the checkpoint namespace keyed by `thread_id`, not the `Send` call itself; this doesn't trivially pass (both branches' `.next` and post-resume state are asserted independently, in an order that would catch cross-branch bleed-through). Task 2.3.2's kill/restart durability test (`test_gate1_interrupt_state_survives_simulated_process_restart`) discards the original in-process graph/checkpointer object and builds a fresh `ConnectionPool`/`PostgresSaver`/compiled graph against the same real `testcontainers` Postgres URL — accepted as a legitimate proxy for a process kill/restart: `PostgresSaver`'s durability guarantee is specifically that no state lives in the Python process, so a fresh object with no shared in-process state reading the same DB is functionally equivalent to a restart for this property; a real subprocess-kill test would exercise OS-level process semantics this task was never scoped to test. Confirmed `graph/build.py` is untouched by this branch (verified via `git diff master origin/task/gate1-gate2-interrupt-mechanics -- newsresearch/graph/build.py` — empty) — the standalone `StateGraph` instances in `tests/test_gate1.py`/`test_gate2.py` correctly avoid touching the compiled production topology, which remains Task 2.4.1's job; only `tests/test_graph_build.py`'s existing state-construction test was extended with the two new fields. `uv run pytest`: 145 passed, 6 deselected (matches expectation exactly). `uv run ruff check .`: clean. No scope creep. **Pre-merge note (mechanical, not a code defect):** this branch's `EXECUTION_PLAN.md` predates this Role Ownership section's commit to `master` (`8c65107`) and does not contain it — a straightforward rebase/merge onto current `master` is needed before merging so this section survives; the branch itself never touched these lines, so no real conflict is expected. **Forward note for Wave 3/4:** when Task 2.2.3b's real reconciliation lands, it plugs into `gate1.py`'s `reconcile` parameter directly (no `gate1_node` signature change needed, per `make_gate1_node`'s existing design); when Task 2.4.1's real fan-out lands, Gate 2's per-branch blocking property carries over unchanged since it already rests on `thread_id`-keyed checkpoint isolation, which `Send`'s branches will also get for free — re-verify with real fanned-out `thread_id`s at that point rather than assuming Wave 1's manual-`thread_id` test proves the `Send` case argument-for-argument.
- **Wave 2 (depends on Wave 1 DS output):**
  - `backend-engineer`: Task 2.1.2b (productionize clustering, needs 2.1.2a's recommendation), Task 2.2.1b (wire subtopic prompt, needs 2.2.1a's draft).
  - `data-scientist`: Task 2.2.3a (reconciliation rule design) — can start once 2.1.2a's clustering approach is settled, since reconciliation logic reasons about cluster output shape.
- **Wave 3 (depends on Wave 2):**
  - `backend-engineer`: Task 2.2.3b (reconciliation wiring), then Task 2.2.4 (rank/cap using DS's distinctiveness formula), then swap the stub gates (Wave 1 track B) over to real subtopic-agent output for Gate 1 (Task 2.3.1's real integration, plus the 2.3.2 kill-restart re-check against real data).
- **Wave 4 (depends on Wave 3):**
  - `backend-engineer`: Story 2.4 (fan-out), Story 2.5.1 (per-subtopic clustering wiring), Story 2.6.1 (Gate 2 report), swap Gate 2's stub over to real per-subtopic cluster output.
  - `data-scientist`: spot-check Story 2.5.1's real clustering output quality (the flagged risk) in parallel with `backend-engineer` finishing Gate 2 wiring — this is a genuine parallel track since quality review of one subtopic's clusters doesn't block wiring the next.
- **Wave 5 (integration, sequential by nature):**
  - `backend-engineer`: Story 2.7 (CLI harness renders both gates end-to-end).
  - `acceptance-verifier` + `data-scientist` co-sign: Story 2.8 Done-when walkthrough, with `data-scientist` specifically covering the clustering-plausibility bullet.

**Gaps flagged, not silently dropped:** the original Phase 2 breakdown (as written before this re-analysis) bundled prompt/algorithm *design* and pipeline *wiring* into single tasks (2.1.2, 2.2.1, 2.2.3) — the same anti-pattern the Cross-Cutting Concerns section already guards against for prompts/models generally (NFR-6's vendor-agnostic template layer exists precisely so design and wiring stay separable). Recommend adopting the 2.1.2a/b, 2.2.1a/b, 2.2.3a/b splits above before `data-scientist` and `backend-engineer` work starts on this phase, so a `data-scientist` isn't stuck opening PRs against `subtopic_agent.py` (git/PR management is explicitly out of their scope) and a `backend-engineer` isn't left guessing at HDBSCAN hyperparameters or prompt wording without a documented rationale to implement against.

---

## Phase 3 — Claim Extraction + Framing Clustering + Summarization

**Rationale:** validate claim-cluster quality manually before wiring in the highest-risk bias/briefing stage.

**Modules:**
- `sourcing/fulltext.py` — `trafilatura`-based fetch, strictly in-memory (returns a plain string, no path to any `persistence/` write function — enforce via module boundary, not just discipline).
- `agents/claim_extraction_agent.py` — small model via `get_chat_model("claim_extraction").with_structured_output(Claim)`, structured output `{claim_text, subject, attributed_source}` per article.
- `clustering/claim_clustering.py` — embed `claim_text`, cluster across *all* articles in the subtopic (not per-article) so assert/omit membership falls out naturally.
- `agents/sentiment.py` — auxiliary per-article/claim sentiment (small model call or lightweight lexicon — explicitly not the clustering axis, per FR-14/NFR risk table).
- `agents/summarization_agent.py` — small/medium model, one call per claim cluster.

**Libraries:** `trafilatura`.

**Tasks:**
1. Full-text fetch per article in a topical cluster, passed directly into extraction — no persistence path exists (schema already excludes body text).
2. Claim extraction prompt + schema; small model.
3. Claim clustering across the subtopic's full article set.
4. Sentiment attached as an attribute, not fed into clustering.
5. Persist `claim_clusters` + `claim_cluster_articles` (`asserts`/`omits` relation, `claim_text`) to Postgres.
6. Summarization Agent per cluster.

**Done when:** for a Gate-2-cleared subtopic, Phase 3 end-to-end produces claim clusters with correct assert/omit article membership and a per-cluster summary; manually spot-check several clusters that grouping reflects real agreement/disagreement rather than embedding noise; confirm claim-extraction traces are visible and inspectable in Langfuse.

### Phase 3 — Story/Task Breakdown (PM decomposition)

**Depends on: Phase 0–2 complete.** Every task assumes a Gate-2-cleared topical cluster (Phase 2 Task 2.6.2) already exists as input.

**Story 3.0 — Phase-3 library additions**
Acceptance: `pyproject.toml` gains `trafilatura`, `uv sync` succeeds.
- [x] Task 3.0.1: `uv add trafilatura`.
      Acceptance: package present; `uv sync` exits 0.
      Depends on: Phase 0 Task 0.1.1
      ✅ **Done (branch `task/phase3-fulltext-fetch`, PR #105, merged).** `trafilatura` added via `uv add`; `uv.lock` gained only its real transitive deps (babel, courlan, dateparser, htmldate, justext, lxml), no unrelated drift.

**Story 3.1 — In-memory full-text fetch, no persistence path**
Acceptance: `sourcing/fulltext.py` returns article body text purely in-memory, with no importable path to any `persistence/` write function — enforced via module boundary, not just discipline.
- [x] Task 3.1.1 (orig. Task 1): `trafilatura`-based fetch per article in a Gate-2-cleared topical cluster.
      Acceptance: given an article URL, returns extracted body text as a plain string; an import-graph check confirms `sourcing/fulltext.py` has zero imports from `persistence/`.
      Depends on: Phase 2 Task 2.6.2, Task 3.0.1
      ✅ **Done (PR #105, merged).** `newsresearch/sourcing/fulltext.py`: `fetch_fulltext(url)` (single-article, soft-fail per NFR-3 — catch/log/return-`None`, matching `rss.py`/`backfill_trigger.py`/`reputation/signals.py`'s established idiom) + `fetch_fulltext_for_cluster(articles)` (batch over a Gate-2-cleared cluster, tolerates one bad URL without failing the whole batch). Article dict shape confirmed to match what `dedup.py`/`gate2_report.py` actually produce/consume (`GraphState.articles: list[dict]`), not guessed. AST-based boundary test (`tests/test_fulltext.py`) enforces zero imports from `persistence/`, mirroring `tests/test_sourcing_module_boundaries.py`'s existing pattern. Not wired into `graph/build.py` yet — correctly scoped, Task 3.2.1 (claim extraction) consumes this next. Tech-lead reviewed independently in a fresh clone, posted verdict directly on the PR: soft-fail shape, article shape, `sourcing/`-vs-`agents/` placement, and scoping all confirmed correct; `uv run ruff check .` clean, all 6 new tests pass. Two non-blocking nits: missing trailing newlines on new files, unused `ArticleFulltext` TypedDict (cosmetic). `uv run pytest`: 203 passed, 8 deselected.

**Story 3.2 — Claim extraction produces structured claims per article**
Acceptance: `agents/claim_extraction_agent.py` returns `{claim_text, subject, attributed_source}` per article via a small model with structured output.
- [ ] Task 3.2.1 (orig. Task 2): claim-extraction prompt + pydantic `Claim` schema, `get_chat_model("claim_extraction").with_structured_output(Claim)`.
      Acceptance: given full text from 3.1.1, returns one or more schema-conformant `Claim` objects per article; call is traced and inspectable in Langfuse.
      Depends on: 3.1.1, Phase 0 Task 0.6.1, 0.6.3
      - [x] Task 3.2.1a (`data-scientist`, design) ✅ **Done (branch `feat/datascientist`, commit `0c0880f`, pushed, no PR — design write-up only).** Schema goes beyond the acceptance line's minimal `{claim_text, subject, attributed_source}`: adds `attribution_type` (`reported`/`attributed`/`alleged`/`opinion`) and `certainty` (`confirmed`/`developing`/`disputed`), justified by real articles constantly mixing outlet-verified reporting, named-source attribution, and one-party allegations within the same piece — collapsing that into one string loses signal the Phase 4 Bias & Framing agent needs for claim-level comparison. Deliberately no numeric confidence score (would reintroduce the scalar-labeling pattern PRD's risk table warns against). Prompt (`newsresearch/llm/prompts/claim_extraction.txt`) enforces atomicity (split "and"/"while"/semicolon-joined sentences), stay-close-to-wording, per-claim attribution/certainty tagging, dedup of near-identical restatements, and a mid-review fix (rule 9) blocking double-extraction of a fact plus a quote that just restates it — cut one long-form article's claim count 65→31 with zero loss on a dense control article (44→46, unaffected). Tested end-to-end via `get_chat_model("claim_extraction")` (`gpt-4.1-mini`) `.with_structured_output(...)` against 10 real RSS-sourced articles (BBC/Guardian/Al Jazeera; GDELT unavailable per issue #101). Example: an Iran/US article correctly kept Trump's Truth Social claim and Iran's foreign ministry denial as two separate, correctly-attributed entries — exactly the shape claim clustering needs for a disputed cluster. Write-up: `notebooks/phase3_claim_extraction_prompt_review.md` (+ `claim_schema_draft.py`, review/recheck scripts, sample JSON outputs). Caveat stated: 10 articles/1 run/1 day is a manual spot-check, not a golden-dataset eval (PRD §7's named v1 gap); rule 9's fix validated on only 2 articles.
      - [ ] Task 3.2.1b (`backend-engineer`, wiring): not yet dispatched.

**Story 3.3 — Claims cluster across the whole subtopic, not per-article**
Acceptance: `clustering/claim_clustering.py` embeds `claim_text` and clusters across *all* articles in the subtopic, so assert/omit membership falls out of cluster membership itself.
- [ ] Task 3.3.1 (orig. Task 3): embed `claim_text` across the subtopic's full claim set; cluster (reusing Phase 2 Task 2.1.2's `cluster()`).
      Acceptance: given claims from multiple articles asserting/omitting the same fact, they land in the same cluster with correct per-article assert/omit membership recoverable from cluster assignment.
      Depends on: 3.2.1, Phase 2 Task 2.1.2

**Story 3.4 — Sentiment is an auxiliary attribute, never a clustering axis**
Acceptance: `agents/sentiment.py` computes per-article/claim sentiment and attaches it as metadata; Story 3.3's cluster assignments are provably unaffected by it (FR-14 / the plan's own risk table on sentiment-as-bias-proxy).
- [ ] Task 3.4.1 (orig. Task 4): sentiment collector (small model call or lightweight lexicon), attached as an attribute on claims/articles.
      Acceptance: a test confirms cluster assignments from 3.3.1 are identical whether or not sentiment has been computed — sentiment is never read by the clustering step.
      Depends on: 3.3.1

**Story 3.5 — Claim clusters persisted with assert/omit article membership**
Acceptance: `claim_clusters` + `claim_cluster_articles` persist `claim_text` and the `asserts`/`omits` relation per article.
- [x] Task 3.5.1 (orig. Task 5): schema/write path for `claim_clusters` + `claim_cluster_articles`.
      Acceptance: `testcontainers`-backed test writes a subtopic's clusters and confirms each article's `asserts`/`omits` relation is queryable and matches 3.3.1's in-memory cluster assignment exactly.
      ✅ **Done (PR #112, merged).** `newsresearch/persistence/claim_clusters.py` (`write_claim_clusters`/`read_cluster_article_relations`), schema_version 2→3 migration. Write path takes `cluster_claims()`'s real return shape verbatim: one `claim_clusters` row/cluster, one `claim_cluster_articles` row per (article, claim) for asserts, one per omitting article. Also fixed the schema gap tech-lead flagged on PR #111: dropped scalar `sentiment_avg REAL` (didn't match real per-claim sentiment shape), added `sentiment_label`/`sentiment_compound` onto `claim_cluster_articles` instead, matched onto asserts rows only via `(article_id, claim_text)`, NULL on omits. `tests/test_claim_clusters_persistence.py` (testcontainers Postgres) round-trips against real `cluster_claims()`/`score_claim_sentiment()` output, not a hand-built fixture — tech-lead confirmed non-vacuous (only `embed`/`cluster` monkeypatched, per existing convention). Tech-lead independently re-ran: `uv run ruff check .` clean, `uv run pytest` 219 passed (1 flaky testcontainers timeout under docker contention, confirmed unrelated, 11/11 solo), new test 3/3. Two non-blocking nits: row-at-a-time inserts (scale), docstring wording. Story 3.5 closed (issue #78).
      Depends on: 3.3.1, 3.4.1, Phase 0 Task 0.4.2

**Story 3.6 — Per-cluster summarization**
Acceptance: `agents/summarization_agent.py` produces one summary per persisted claim cluster using a small/medium model.
- [ ] Task 3.6.1 (orig. Task 6): summarization agent, one call per cluster.
      Acceptance: given a persisted claim cluster (3.5.1), returns a summary whose content is manually spot-checked against the cluster's actual claims — LLM prompt-quality is explicitly a needs-manual-inspection item per the plan's testing strategy, not an automated assertion.
      Depends on: 3.5.1, Phase 0 Task 0.6.1

**Story 3.7 — Phase 3 Done-when verified end-to-end (integration/demo)**
Acceptance: matches the phase-level Done-when block above.
- [ ] Task 3.7.1: manual spot-check of several real claim clusters on a Gate-2-cleared subtopic for correct assert/omit membership and summary plausibility.
      Acceptance: reviewer confirms grouping reflects real agreement/disagreement rather than embedding noise for at least several inspected clusters; any failure is filed against Task 3.3.1 (clustering) or 3.6.1 (summarization), not against "Phase 3" generally.
      Depends on: 3.6.1
- [ ] Task 3.7.2: confirm claim-extraction traces are visible and inspectable in the local Langfuse UI.
      Depends on: 3.2.1, Phase 0 Task 0.7.2

### Phase 3 — Role Ownership & Parallelization Re-analysis (`project-manager`, 2026-07-29)

**Why this exists:** mirrors the Phase 2 re-analysis. Phase 3 is the next phase (after Phase 2) with real `data-scientist` scope — claim-extraction prompt design, claim-clustering hyperparameter validation, sentiment-approach choice, summarization prompt design — alongside `backend-engineer` wiring. Nothing below changes an existing task's acceptance criterion except where explicitly marked as a gap-fix; splits get lettered sub-IDs and are proposals, not yet adopted.

**`frontend-engineer`: zero ownership in Phase 3.** No new human gate, no new UI surface is introduced in this phase — any dev-inspection CLI extension, if added, is `backend-engineer` dev tooling under the Cross-Cutting Concerns CLI-harness paragraph, not `frontend-engineer` scope (same citation Phase 2's re-analysis used).

**Ownership annotation, story by story:**

| Story/Task | Owner | Rationale / handoff |
|---|---|---|
| 3.0 (lib add) | `backend-engineer` | Trivial `uv add trafilatura`. |
| 3.1.1 full-text fetch | `backend-engineer` | No ML/prompt content; the "no import path to `persistence/`" check is structural, not a design decision. |
| 3.2.1 claim-extraction prompt + schema | **split, DS→BE** (proposed 3.2.1a/3.2.1b) | Prompt/schema iteration for claim quality (does the extracted `{claim_text, subject, attributed_source}` capture the real claim, not a paraphrase-of-a-paraphrase) is `data-scientist` territory; wiring via `get_chat_model("claim_extraction").with_structured_output(Claim)` per NFR-6 is `backend-engineer`. **Gap fixed:** the original acceptance line bundled both with no split — the same anti-pattern the Phase 2 re-analysis found and fixed for 2.1.2/2.2.1/2.2.3. |
| 3.3.1 claim clustering across the subtopic | **split, DS→BE** (proposed 3.3.1a/3.3.1b) | **Gap fixed, not just relettered:** the original acceptance line says clustering "reus[es] Phase 2 Task 2.1.2's `cluster()`," which silently assumes Phase 2's article-title-tuned `Settings.clustering.hdbscan_min_cluster_size=4`/`min_samples=1` (Task 2.1.2a/b) transfer unexamined to claim-text embeddings — a materially different granularity (one short assertion vs. a title+snippet), with no stated reason the same numbers still hold. Promoted to its own `data-scientist` validation/re-tuning task rather than letting `backend-engineer` silently inherit Phase 2's values for a different kind of input. |
| 3.4.1 sentiment collector | **split, DS→BE** (proposed 3.4.1a/3.4.1b) | Choosing small-model-prompted vs. lexicon-based sentiment (the Modules list's own "or") is a real approach decision — `data-scientist`; wiring it as an attribute and proving clustering never reads it (FR-14) is `backend-engineer`. |
| 3.5.1 persistence | `backend-engineer` | Deterministic schema/write path, no ML content. |
| 3.6.1 summarization agent | **split, DS→BE** (proposed 3.6.1a/3.6.1b) | Prompt content for cluster summaries is `data-scientist`; wiring via `get_chat_model("summarization")` is `backend-engineer`. |
| 3.7.1 claim-cluster/summary quality spot-check | `data-scientist` produces the judgment; `acceptance-verifier` verifies mechanically that the write-up exists, is dated to this run, and corresponds to the real output checked | Same co-sign shape as Phase 2 Story 2.8's clustering-quality bullet — "is this grouping real agreement/disagreement or embedding noise" is output-quality-evaluation scope, not a mechanical check. |
| 3.7.2 Langfuse trace check | `acceptance-verifier` | Purely mechanical. |

**Proposed task splits (not yet adopted):**
- **Task 3.2.1a** (`data-scientist`): draft/iterate `llm/prompts/claim_extraction.txt` + the `Claim` schema shape, evaluated against several real full-text article samples for extraction quality (atomic, attributable claims, not run-on paraphrase). Acceptance: documented sample outputs judged plausible, prompt committed under `llm/prompts/`, schema fields proposed in `llm/schemas.py`-ready form.
  Depends on: Task 3.1.1 (needs real full text to evaluate against), Phase 0 Task 0.6.3
- **Task 3.2.1b** (`backend-engineer`, was Task 3.2.1): wire the DS-authored prompt via `get_chat_model("claim_extraction").with_structured_output(Claim)`, confirm a real Langfuse trace. Acceptance: unchanged from the original.
  Depends on: 3.2.1a, Phase 0 Task 0.6.1
- **Task 3.3.1a** (`data-scientist`): validate/re-tune clustering hyperparameters for claim-text granularity against a constructed or real-pulled claim fixture — may land on Phase 2's same values or may need dedicated `Settings.clustering.claim_min_cluster_size`/`claim_min_samples` fields; this task decides which, not assumes. Acceptance: documented recommendation + fixture + reasoning, committed under `notebooks/`.
  ✅ **Done (branch `feat/datascientist`, commit `f16c6d1`, pushed, no PR).** Recommends `claim_min_cluster_size=2`/`claim_min_samples=1` — the opposite direction from Phase 2's article-level `min_cluster_size=4`, confirming claim granularity genuinely needs dedicated knobs (best F1=0.744 vs. 0.29–0.54 at `min_cluster_size` 3–6). Real 120-claim corpus (6 articles, one real subtopic — a DeepSeek-model-cost story with wire-near-duplicate outlets), via `get_chat_model("claim_extraction")` reusing 3.2.1a's prompt/schema unchanged. Ground truth built by hand (cosine≥0.90 auto-accept + 10 hand-reviewed borderline pairs) → 78 true clusters. **Methodology finding, not just a number:** the ARI metric (Phase 2's chosen sweep metric) gave uninterpretable 0.08–0.22 values across every setting on this singleton-heavy data shape — ARI's pairwise bookkeeping penalizes HDBSCAN's `-1` noise sentinel unfairly here — switched to pairwise same-fact recall/false-merge precision, which actually reflects Task 3.3.1's real need. **Honest quality-ceiling caveat:** even at best settings, false-merge precision is only 0.674 (~1 in 3 merges wrong) — flagged as a real ceiling for Phase 4's bias/framing design to account for, not something a different threshold fixes. **`kmeans_fallback_threshold=20` flagged as broken for claims, not silently reused:** it's unit-blind (article count vs. claim count) — real per-article claim volume (13–26/article) means realistic subtopic claim pools (60–200+) sit well above 20, so claims essentially never get HDBSCAN's low-n fallback protection; subsample sweep shows false-merge precision degrading monotonically as n drops (0.674→0.481→0.278→0.122 from n=120→39). Recommends a dedicated `claim_kmeans_fallback_threshold`, provisionally ~40, exact tuning deferred as follow-up once more real subtopics are available. **New bug found and filed while doing this task — ✅ closed (branch `task/fulltext-fetch-timeout`, PR #109, merged, closes issue #108).** `sourcing/fulltext.py::fetch_fulltext` had no request timeout and stalled indefinitely on a real unresponsive URL. Root cause: `trafilatura.fetch_url()` internally swallows download failures (incl. timeout) and returns `None` — the bug was purely "never told a timeout," not a missing soft-fail path. Fix: new `Settings.sourcing.fulltext_fetch_timeout_seconds` (default 30.0), passed through to trafilatura's `ConfigParser`. **First submission had a real blocking bug, caught by tech-lead review, not shipped:** the config value was written as `str(30.0)` but trafilatura reads it via `ConfigParser.getint()`, which rejects non-int strings — `getint("30.0")` raised internally, silently swallowed, causing `fetch_fulltext` to return `None` unconditionally for *every* URL, a total regression, not a fix. Both of the original PR's tests mocked `trafilatura.fetch_url` directly and missed this entirely. Fixed on the same PR: `str(int(timeout_seconds))`, plus a new test that builds the real `ConfigParser` and calls `getint()` directly — verified by tech-lead to fail against the pre-fix commit and pass against the fix commit, proving it genuinely catches this bug class. `uv run pytest`: 206 passed, 8 deselected.
  Depends on: 3.2.1b (needs real claims, not article titles, to embed)
- **Task 3.3.1b** (`backend-engineer`, was Task 3.3.1): wire claim embedding + clustering per 3.3.1a's recommendation, reusing `clustering/cluster.py` unchanged (only its `Settings`-sourced inputs may differ). Acceptance: unchanged from the original.
  Depends on: 3.3.1a
  ✅ **Done (branch `task/3-3-1b-claim-clustering`, PR #110, merged).** Added `claim_min_cluster_size=2`/`claim_min_samples=1`/`claim_kmeans_fallback_threshold=40` (provisional, clearly flagged as such in the `config.py` docstring, not silently presented as final) to `Settings.clustering`. `clustering/cluster.py::cluster()` now takes optional `min_cluster_size`/`min_samples`/`kmeans_fallback_threshold` overrides, defaulting to the existing article-level `Settings` values when omitted — verified byte-identical for Phase 2's existing callers (`topical_clustering_agent.py`, `subtopic_agent.py`) via a real `np.array_equal` regression test, not a smoke check. New `newsresearch/clustering/claim_clustering.py::cluster_claims(article_claims)` embeds `claim_text` across a subtopic, clusters with claim-specific thresholds, derives per-cluster `asserting_article_ids`/`omitting_article_ids` purely from cluster membership — tech-lead confirmed this correctly implements Story 3.3's acceptance line (zero-claim articles counted as "omitting" is a deliberate, documented choice, not an oversight). Tech-lead-reviewed independently in a fresh clone, posted verdict directly on the PR: safe to merge, 9/9 new tests pass. One non-blocking gap flagged: the "real corpus" test fakes embeddings, so it exercises the HDBSCAN wiring at claim thresholds but doesn't actually validate against data-scientist's reported F1=0.744 recall/precision numbers — the PR description overstated this as a real-shaped regression proof; noted as follow-up, not a blocker. `uv run pytest`: 213 passed, 8 deselected.
- **Task 3.4.1a** (`data-scientist`): decide small-model-prompted vs. lexicon sentiment with a short cost/latency-vs-quality justification (sentiment is explicitly auxiliary per FR-14, doesn't need frontier reasoning). Acceptance: documented recommendation + sample outputs on real claims.
  ✅ **Done (branch `feat/datascientist`, commit `b72d53d`, pushed, no PR).** Recommends **VADER lexicon**, not a small-model call — sentiment runs per-article and per-claim (306 claims from just 10 sample articles), and VADER is deterministic (useful for FR-24 cross-run drift comparisons) and near-zero marginal cost for a signal FR-14 says explicitly doesn't need frontier reasoning; no sentiment library exists yet in `pyproject.toml`, so this is the one case where a new small single-purpose dependency beats hand-rolling a worse lexicon. **Scale: 3-class** (positive/neutral/negative) via VADER's own compound thresholds (±0.05), not the raw scalar — a float score invites the false-precision misread FR-14/the PRD risk table warns against; underlying compound score still stored alongside the label for future finer-grained use. Real test on 306 claims: sensible per-claim scores (e.g. "Pauline Hanson has vowed to ban the ABC..." → -0.56 negative). **One real limitation found and reported, not hidden:** whole-article scoring on one article came back +0.98 positive despite the article's actual mixed/negative framing — surface positive words dominate a full document with no discourse structure — argues for claim-level sentiment as the primary attribute, article-level as a rougher secondary signal only. **Clustering-unaffected proof spec, for `backend-engineer`'s Task 3.4.1b to build:** run claim clustering twice on identical claims (with vs. without the `sentiment` field attached), assert embeddings fed to `cluster()` are byte-identical between runs and cluster assignments match exactly (ARI==1.0, not brittle same-ID comparison); additionally spy/monkeypatch the embeddings call to assert no sentiment-derived text ever appears in the strings passed to it (catches subtler leaks, e.g. someone embedding `f"{claim_text} ({sentiment_label})"`).
  Depends on: 3.2.1b
- **Task 3.4.1b** (`backend-engineer`, was Task 3.4.1): wire 3.4.1a's chosen approach; prove clustering is unaffected. Acceptance: unchanged from the original.
  ✅ **Done (branch `task/3-4-1b-sentiment`, PR #111, merged).** `newsresearch/agents/sentiment.py` — VADER-based, wrap-don't-mutate `ClaimSentiment`/`ArticleSentiment` frozen dataclasses mirroring `ScoredArticle`'s pattern, 3-class labels at VADER's default ±0.05 compound thresholds, raw compound stored alongside. `tests/test_sentiment.py::test_sentiment_does_not_affect_clustering` monkeypatches the real `embed`/`cluster` names `claim_clustering.py` calls, exercises sentiment computed on the same claim dict before clustering, asserts text/vector/ARI identity per 3.4.1a's proof spec. `tech-lead` reviewed (PR comment): safe to merge, 3/3 tests pass, ruff clean. Two non-blocking flags: `notebooks/phase3_sentiment_approach_review.md` only exists on unmerged `feat/datascientist`, not landed in this PR or `master` (dangling citation risk); `persistence/schema.sql`'s scalar `sentiment_avg REAL` doesn't map onto this PR's per-claim label+compound shape — Task 3.5.1 needs schema/aggregation work to reconcile, not owed by this PR. Story 3.4 closed (issue #77).
  Depends on: 3.4.1a, 3.3.1b
- **Task 3.6.1a** (`data-scientist`): draft/iterate the per-cluster summarization prompt against real persisted claim clusters for faithfulness (no invented or dropped claims). Acceptance: documented sample outputs judged plausible, prompt committed.
  ✅ **Done (branch `feat/datascientist`, commit `1e81649`, pushed, no PR).** `newsresearch/llm/prompts/summarization.txt` — plain-string output, no schema (`claim_clusters.summary` is `TEXT`, nothing downstream consumes it structurally). Grounded against real corpus (`tests/fixtures/phase3_claim_clustering_corpus.json`, 6 articles/120 claims), real `cluster_claims()` output (35 clusters, matches Task 3.3.1a), real `gpt-4.1-mini` calls via `get_chat_model("summarization")` over 9 hand-picked clusters covering Task 3.3.1a's known failure shape (0.674 false-merge precision — ~1/3 of clustered claim pairs are distinct facts wrongly merged, not paraphrases). Prompt requires: unify only true paraphrases; cover all distinct facts when a cluster holds 2+; never blend distinct numbers/subjects into one vague statement; calibrate "N sources report" language to what's actually listed. Verified against real false-merge cases (e.g. "80% cut for Luna" + "20% cut for Terra" kept distinct, correctly attributed to one source not "sources") and a real 5-distinct-fact single-source cluster (all 5 preserved). Result: held on all 9 samples, no invented/dropped facts — manual spot-check per PRD §7's stated gap, not a scored eval. **Known gap, stated not hidden:** fixture has zero `disputed`/`alleged`-certainty claims, so the prompt's certainty-preservation rule is untested against real contested claims. Files: `notebooks/phase3_summarization_prompt_review.md`, `notebooks/phase3_summarization_review.py`, `notebooks/phase3_summarization_samples.json`.
  Depends on: 3.5.1 (needs real persisted clusters)
- **Task 3.6.1b** (`backend-engineer`, was Task 3.6.1): wire via `get_chat_model("summarization")`. Acceptance: unchanged from the original.
  ✅ **Done (branch `task/3-6-1b-summarization-agent`, PR #113, merged).** `newsresearch/agents/summarization_agent.py::summarize_cluster()` — reads asserting claims off a persisted cluster via `read_cluster_article_relations` (extended to join `articles.domain`), calls `get_chat_model("summarization")` with 3.6.1a's prompt (verified byte-identical to data-scientist's `1e81649`), writes result via new `write_cluster_summary` to `claim_clusters.summary`. Config propagation genuinely mirrors `claim_extraction_agent.py`'s `merge_configs` pattern (tech-lead checked side by side, not superficial). No parallel persistence path, plain-string output (no invented schema). Tests assert real prompt content/config forwarding, not canned mocks. Tech-lead independently re-ran: `uv run pytest` 223 passed/8 deselected, `uv run ruff check .` clean (pre-existing unrelated notebook errors only). Task 3.6.1's original manual-spot-check acceptance deferred to Task 3.7.1 (needs a real pipeline run through summarization). Story 3.6 closed (issue #79).
  Depends on: 3.6.1a, Phase 0 Task 0.6.1

**Parallelization plan (waves):**
- **Wave 1:** `backend-engineer`: Task 3.0.1 (lib add), Task 3.1.1 (full-text fetch — no DS dependency). **Note, a real difference from Phase 2's Wave 1:** every `data-scientist` task in this phase needs real claims/clusters/full-text as input to evaluate against, not just a fixture or sample topic string — so Phase 3's DS work is more sequentially gated on `backend-engineer`'s output than Phase 2's clustering/prompt-design tracks were. Nothing DS-owned can start in Wave 1.
- **Wave 2:** `data-scientist`: Task 3.2.1a (needs 3.1.1's real full text).
- **Wave 3:** `backend-engineer`: Task 3.2.1b. `data-scientist`: Task 3.3.1a and Task 3.4.1a in parallel with each other once 3.2.1b produces real claims (both need only claims to exist, not each other).
- **Wave 4:** `backend-engineer`: Task 3.3.1b, then Task 3.4.1b (sequential — 3.4.1b's "clustering unaffected" proof needs 3.3.1b's real cluster assignments to diff against), then Task 3.5.1 (persistence).
- **Wave 5:** `data-scientist`: Task 3.6.1a (needs 3.5.1's real persisted clusters). `backend-engineer`: Task 3.6.1b once 3.6.1a lands.
- **Wave 6 (integration):** `acceptance-verifier` + `data-scientist` co-sign: Story 3.7 Done-when, same co-sign shape as Phase 2 Story 2.8.

**Gaps flagged, not silently dropped:** three original tasks (3.2.1, 3.4.1, 3.6.1) bundled prompt/approach design with wiring, the exact pattern Phase 2's re-analysis already found and fixed for 2.1.2/2.2.1/2.2.3 — fixed the same way here. A fourth, less obvious bundling in Task 3.3.1 assumed Phase 2's article-level clustering hyperparameters transfer to claim-level text without anyone stating or checking that assumption — fixed by promoting the re-validation to its own `data-scientist` task (3.3.1a).

**Flagged architectural open question for `tech-lead` before Phase 3 starts:** should claim-level clustering (Task 3.3.1) share `Settings.clustering.hdbscan_min_cluster_size`/`min_samples` with Phase 2's article-level topical clustering, or does it need dedicated `Settings.clustering.claim_*` fields? Claims are shorter, more granular, and more numerous per article than the article-title+snippet embeddings Phase 2's thresholds were tuned against — reusing those defaults unexamined risks over-fragmenting genuine agreement or forcing spurious merges. This blocks Task 3.3.1a's `data-scientist` from knowing whether they're tuning a new config surface or validating an existing one.

✅ **Resolved (`tech-lead`, 2026-08-03): dedicated `Settings.clustering.claim_min_cluster_size`/`claim_min_samples` fields — do not share the article-tuned values.** Phase 2's `min_cluster_size=4`/`min_samples=1` were tuned via an ARI sweep against title+snippet embeddings (`notebooks/phase2_task2.8_clustering_review.md`), and that sweep shows HDBSCAN is sharply sensitive to these two knobs (ARI 0.781→0.324 just from `min_samples` 1→3 on the same fixture). Claim-text embeddings are a materially different input distribution — shorter strings, more numerous per subtopic, denser near-duplicate phrasing across sources — with no basis to assume the same density knobs land in a good regime. Sharing one field would mean a future retune of claim clustering (likely, since 3.3.1a is explicitly scoped to re-validate) silently perturbs article-level topical clustering too, coupling two independently-evolving subsystems for zero benefit beyond fewer config lines. `kmeans_fallback_threshold` stays shared for now — no evidence yet it's article-count-specific — flagged to revisit if Task 3.3.1a's data shows otherwise. **Wiring implication for `backend-engineer`'s Task 3.3.1b:** `clustering/cluster.py::cluster()` currently reads `Settings()` internally rather than accepting hyperparams as arguments — needs a small signature change (optional `min_cluster_size`/`min_samples` overrides) so callers can pass either the article- or claim-level `Settings` values through; core HDBSCAN/KMeans logic itself stays unchanged. Task 3.3.1a is now unblocked to tune a real, independent config surface.

---

## Phase 4 — Bias & Framing Agent + Briefing Agent

**Rationale (per TRD):** highest-risk, highest-value stage — budget the most manual review time.

**Modules:**
- `agents/bias_framing_agent.py` — large model; per-cluster + per-source framing/stance label (descriptive, not a left/center/right scale per PRD risk table), claim-level include/omit/emphasis comparison.
- `agents/briefing_agent.py` — large model, sequential after bias agent (consumes its structured output directly, not re-derived from raw clusters).
- `agents/snapshot_assembly.py` — pure aggregation, no LLM call; reads back from Postgres, assembles one JSON run artifact.

**Tasks:**
1. Bias/framing prompt: descriptive labels, operates over `claim_clusters` + article/domain metadata already persisted.
2. Claim-level comparison: largely a re-presentation of `claim_cluster_articles` plus LLM-generated framing text — not a new clustering step.
3. Briefing Agent: sequential dependency enforced at the graph level (edge, not just prompt instruction), consumes bias agent's structured output.
4. Persist `claim_clusters.framing_label`, `briefings` table (consensus/disputed/omissions).
5. Snapshot Assembly reads and assembles final JSON artifact per subtopic.
6. Use Langfuse's session view to review a full subtopic's trace chain (bias agent → briefing agent) during manual QA — this is the main practical payoff of the tracing investment for this phase.

**Done when:** a chained Phase 1–4 run on a real subtopic produces a briefing a human finds plausible on manual review against source articles — validation is inherently qualitative here (PRD's flagged eval gap), so "done" = operator review across at least 2–3 real topics, not an automated check. Log each reviewed run as an MLflow run so config-vs-quality observations accumulate over time.

### Phase 4 — Story/Task Breakdown (PM decomposition)

**Depends on: Phase 0–3 complete.** Highest-risk, highest-value stage per the TRD — budget the most manual review time; validation here is inherently qualitative (PRD's flagged eval gap), not a substitute for writing sharp criteria.

**Story 4.1 — Bias/Framing Agent labels clusters and sources descriptively**
Acceptance: `agents/bias_framing_agent.py` produces a descriptive (never a left/center/right scale, per the PRD risk table) framing/stance label per cluster and per source, using a large model over already-persisted `claim_clusters` + article/domain metadata.
- [ ] Task 4.1.1 (orig. Task 1): bias/framing prompt + structured output, operating over Phase 3 Task 3.5.1's persisted data.
      Acceptance: given a persisted subtopic's claim clusters, returns a descriptive label per cluster and per source; the output schema itself rejects any left/center/right-style scalar field; call is traced in Langfuse.
      Depends on: Phase 3 Task 3.5.1, Phase 0 Task 0.6.1

**Story 4.2 — Claim-level include/omit/emphasis comparison**
Acceptance: for each cluster, a claim-level comparison shows what's included/omitted/emphasized differently relative to other clusters — built from existing `claim_cluster_articles` relations plus LLM framing text, not a new clustering pass.
- [ ] Task 4.2.1 (orig. Task 2): claim-level comparison generator.
      Acceptance: comparison output for a subtopic re-presents Phase 3 Task 3.5.1's `asserts`/`omits` relation per cluster with attached LLM framing text; no new embedding/clustering call is made in this path (verified by absence of a clustering-function invocation here).
      Depends on: 4.1.1

**Story 4.3 — Briefing Agent synthesizes consensus vs. disputed, sequentially after bias agent**
Acceptance: `agents/briefing_agent.py` runs only after the bias/framing agent completes for that subtopic, consuming its structured output directly (not re-derived from raw clusters) — enforced as a graph edge, not a prompt instruction.
- [ ] Task 4.3.1 (orig. Task 3): briefing agent + graph-level sequential edge (bias_framing_agent → briefing_agent).
      Acceptance: graph-topology inspection confirms an explicit edge enforcing bias-before-briefing; the briefing agent's input parameter is the bias agent's structured output object, not raw `claim_clusters`.
      Depends on: 4.1.1, Phase 0 Task 0.5.2

**Story 4.4 — Framing labels and briefings persisted**
Acceptance: `claim_clusters.framing_label` is populated; a `briefings` table stores consensus/disputed/omissions content.
- [ ] Task 4.4.1 (orig. Task 4): persist `claim_clusters.framing_label` + `briefings` table schema/write path.
      Acceptance: `testcontainers`-backed test confirms `framing_label` is written per cluster and a `briefings` row exists per subtopic with consensus/disputed/omissions fields populated.
      Depends on: 4.1.1, 4.3.1, Phase 0 Task 0.4.2

**Story 4.5 — Snapshot assembly produces the final per-subtopic JSON artifact**
Acceptance: `agents/snapshot_assembly.py` performs pure aggregation (no LLM call), reading back from Postgres to produce one structured JSON artifact per subtopic (sub-topic → clusters → summaries → bias labels → briefing).
- [ ] Task 4.5.1 (orig. Task 5): snapshot assembly reader/assembler.
      Acceptance: given a fully-processed subtopic, output JSON contains every named layer sourced entirely from Postgres reads; zero calls to `get_chat_model` during assembly (verified via a call-count test double).
      Depends on: 4.4.1, Phase 3 Task 3.6.1

**Story 4.6 — Phase 4 Done-when verified via qualitative operator review (flagged eval gap)**
Acceptance: matches the phase-level Done-when block — operator finds the briefing plausible against source articles across at least 2–3 real topics; each reviewed run logged as an MLflow run.
- [ ] Task 4.6.1 (orig. Task 6 + Done-when): use Langfuse's session view to review a full subtopic's bias→briefing trace chain during manual QA, across at least 2–3 real topics.
      Acceptance: for each reviewed topic, the operator records a plausible/not-plausible verdict against real source articles — this is explicitly qualitative, not an automatable pass/fail (per the PRD's flagged eval-harness gap); each reviewed run has a corresponding MLflow run for later config-vs-quality comparison.
      Depends on: 4.5.1, Phase 0 Task 0.7.3
      Note for the acceptance-verifier agent: this task's core verdict is inherently **UNVERIFIABLE HERE** for an automated check — mark it BLOCKED pending explicit operator sign-off rather than substituting an automated judgment for the required qualitative review.

### Phase 4 — Role Ownership & Parallelization Re-analysis (`project-manager`, 2026-07-29)

**Why this exists:** Phase 4 is the TRD's explicitly named "highest-risk, highest-value" stage, and the first phase where `data-scientist` prompt-design work (bias/framing labeling, briefing synthesis) carries direct product-risk weight rather than being a quality-of-clustering side concern. Nothing below changes an existing task's acceptance criterion except where marked as a gap-fix.

**`frontend-engineer`: zero ownership in Phase 4.** No new gate, no new UI surface — same citation as Phase 2/3's re-analyses (Cross-Cutting Concerns CLI-harness paragraph, Phase 6's "deliberately last" framing).

**Ownership annotation, story by story:**

| Story/Task | Owner | Rationale / handoff |
|---|---|---|
| 4.1.1 bias/framing prompt + structured labels | **split, DS→BE** (proposed 4.1.1a/4.1.1b) | This is the single highest-risk prompt-design decision in the plan (PRD's flagged political-lean-labeling risk; TRD 4.6's "descriptive, not a rigid left/center/right scale" requirement) — squarely `data-scientist` territory, not something a `backend-engineer` should draft ad hoc while wiring. Wiring via `get_chat_model("bias_framing").with_structured_output(...)` and confirming the schema structurally rejects a left/center/right scalar is `backend-engineer`. **Gap fixed:** the original acceptance line bundled "descriptive... never a left/center/right scale... labels a per-cluster/per-source... traced in Langfuse" as one task with no design/wiring split, despite this being the phase the plan itself calls the riskiest. |
| 4.2.1 claim-level include/omit/emphasis comparison | **split, DS→BE** (proposed 4.2.1a/4.2.1b) | The comparison's *structure* (assert/omit membership) is a re-presentation of Phase 3's already-persisted relations — no new design content there. But the *emphasis-difference language itself* ("where language/emphasis differs on shared claims," TRD 4.6) is LLM-generated prose requiring its own prompt design, not something that falls out of re-presenting existing rows — `data-scientist` owns that prompt; `backend-engineer` owns assembling the relation data it's given. **Gap fixed:** original acceptance line treated the whole task as wiring ("re-presentation... not a new clustering step"), which is true for the assert/omit structure but glosses over the fact that the emphasis-comparison text is itself a prompted LLM output needing the same design/wiring split as 4.1.1. |
| 4.3.1 briefing agent + sequential edge | **split, DS→BE** (proposed 4.3.1a/4.3.1b) | Briefing prompt content (how consensus/disputed/omissions get synthesized from the bias agent's structured output) is `data-scientist`; enforcing the bias→briefing dependency as an actual graph edge (not a prompt instruction) and wiring the model call is `backend-engineer`. |
| 4.4.1 persist framing labels + briefings | `backend-engineer` | Deterministic schema/write path. |
| 4.5.1 snapshot assembly | `backend-engineer` | Pure aggregation, explicitly zero LLM calls (verified via call-count double) — no DS content. |
| 4.6.1 qualitative Done-when review | `data-scientist` runs the structured quality pass (bias/framing label consistency, hallucination checks — literally named in their scope); the **operator** (the actual product user, not an agent role) gives the final "is this briefing plausible against source articles" verdict, since that is exactly what the PRD's own success metric requires and no agent role can substitute for it | Mirrors the existing stub's own note ("mark it BLOCKED pending explicit operator sign-off") — `acceptance-verifier` reports this bullet `UNVERIFIABLE HERE`/not-met until the operator's own sign-off exists, exactly as it did in Phase 0/2, rather than either agent rendering an opinion it isn't equipped to hold. |

**Proposed task splits (not yet adopted):**
- **Task 4.1.1a** (`data-scientist`): draft/iterate the bias/framing prompt against several real persisted subtopics' claim clusters + source metadata, evaluated for (a) genuinely descriptive (not scalar-political) labels and (b) label consistency across sources covering the same claim. Acceptance: documented sample outputs + the schema shape (with an explicit note on how the schema itself structurally prevents a left/center/right field, not just a prompt instruction asking the model not to produce one), committed under `notebooks/`.
  Depends on: Phase 3 Task 3.5.1 (needs real persisted claim clusters to evaluate against)
- **Task 4.1.1b** (`backend-engineer`, was Task 4.1.1): wire via `get_chat_model("bias_framing").with_structured_output(...)`, confirm the Langfuse trace. Acceptance: unchanged from the original.
  Depends on: 4.1.1a, Phase 0 Task 0.6.1
- **Task 4.2.1a** (`data-scientist`): draft/iterate the emphasis-comparison prompt (given two-plus sources' claim text on the same cluster, describe what's included/omitted/emphasized differently), evaluated against real examples. Acceptance: documented sample outputs judged plausible.
  Depends on: 4.1.1b
- **Task 4.2.1b** (`backend-engineer`, was Task 4.2.1): wire the comparison generator — assemble Phase 3's `asserts`/`omits` relations + 4.2.1a's prompt output, no new clustering call. Acceptance: unchanged from the original.
  Depends on: 4.2.1a
- **Task 4.3.1a** (`data-scientist`): draft/iterate the briefing prompt (consensus/disputed/omissions synthesis from the bias agent's structured output), evaluated against real subtopic examples. Acceptance: documented sample outputs judged plausible.
  Depends on: 4.1.1b, 4.2.1b
- **Task 4.3.1b** (`backend-engineer`, was Task 4.3.1): wire the briefing agent + the graph-level sequential edge (bias_framing_agent → briefing_agent). Acceptance: unchanged from the original.
  Depends on: 4.3.1a, Phase 0 Task 0.5.2

**Parallelization plan (waves):**
- **Wave 1:** nothing DS-owned can start yet — same shape as Phase 3's Wave 1, every Phase 4 DS task needs real Phase 3 output to evaluate against, not a fixture. `backend-engineer` has no Phase-4-specific Wave 1 task either (Story 4.4/4.5's persistence/assembly both depend on later waves) — Phase 4 is effectively DS-gated from the start once Phase 3 is done.
- **Wave 2:** `data-scientist`: Task 4.1.1a (needs Phase 3 Task 3.5.1's real persisted clusters).
- **Wave 3:** `backend-engineer`: Task 4.1.1b.
- **Wave 4:** `data-scientist`: Task 4.2.1a (parallelizable with `backend-engineer` if Wave 3 is done sequentially, but 4.2.1a strictly needs 4.1.1b's real output first per its stated dependency).
- **Wave 5:** `backend-engineer`: Task 4.2.1b. `data-scientist`: Task 4.3.1a can start once 4.2.1b lands.
- **Wave 6:** `backend-engineer`: Task 4.3.1b, then Task 4.4.1 (persistence), then Task 4.5.1 (snapshot assembly).
- **Wave 7 (integration, qualitative):** `data-scientist` runs the structured quality pass across 2–3 real topics; the operator gives final sign-off; `acceptance-verifier` verifies the write-up exists and is genuinely tied to the run being checked, reporting `UNVERIFIABLE HERE` if it doesn't.

**Gaps flagged, not silently dropped:** all three original Phase 4 tasks (4.1.1, 4.2.1, 4.3.1) bundled prompt design with wiring — the same anti-pattern found in Phase 2/3, fixed the same way. Task 4.2.1 additionally had a subtler gap: its acceptance line correctly identified the assert/omit *structure* as pure re-presentation with no new clustering call, but didn't acknowledge that the emphasis-difference *text* is itself a prompted LLM output requiring its own design pass — fixed by giving it its own DS sub-task (4.2.1a) rather than letting it ride along as an unexamined part of "wiring."

**Flagged architectural open question for `tech-lead` before Phase 4 starts:** does the Bias & Framing Agent make one LLM call per claim cluster, or one call per subtopic covering all of that subtopic's clusters + sources at once? TRD 4.6 states "Input: claim clusters + source metadata for the subtopic" without disambiguating call granularity. This isn't a stylistic detail — it directly determines (a) cost, since this is explicitly the plan's "large model" stage and NFR-1's cost-containment goal scales with call count, not just token count, and (b) the output schema's shape (a single nested structured-output object covering every cluster/source vs. many independent per-cluster calls, with materially different structured-output reliability characteristics at each cluster-count scale). Both Task 4.1.1a's `data-scientist` prompt design and Task 4.1.1b's `backend-engineer` wiring are blocked on this being decided first, not discovered independently by whichever of them starts first.

---

## Phase 5 — Timeline Agent + Scheduling + Cross-Run Matching

**Modules:**
- `agents/timeline_agent.py` — triggered only on scheduled non-first runs for a topic hash.
- `persistence/topic_identity.py` — canonicalize (lowercase, whitespace-normalize, optional LLM-normalize call) → hash, keys the `topics` table.
- `clustering/subtopic_matching.py` — cosine similarity of current vs. prior subtopic embeddings against `Settings.clustering.subtopic_match_threshold`.
- `scheduling/scheduler.py` — APScheduler job or documented cron entrypoint invoking `newsresearch/scheduling/run_scheduled.py`.

**Libraries:** `apscheduler`.

**Tasks:**
1. Topic canonicalization → hash pipeline.
2. Scheduler entrypoint: looks up due topics by cadence, invokes the full graph headlessly. **Open design decision the TRD doesn't resolve** — Gate 1/2 need a non-interactive policy for scheduled runs: auto-approve on scheduled reruns, or fail-and-notify if gates can't be cleared unattended. Decide before building this task.
3. Timeline Agent: fetch prior runs for topic hash, embed current subtopic labels, match by threshold, compute drift (sentiment shift, volume change, new/dropped claim clusters).
4. Persist `subtopic_matches`, trend report as part of run artifact.
5. Verify NFR-3 specifically under the scheduled/unattended path (not just Phase 1's interactive path) — simulate Google News RSS failure during an actual scheduled run and confirm graceful degradation.
6. Confirm unattended scheduled runs still produce Langfuse traces and MLflow runs — no gaps in observability just because a human isn't watching.

**Done when:** running the same topic twice (simulated second scheduled run) correctly matches unchanged subtopics, flags a deliberately-varied one as newly emerged, produces a sane drift report; a real APScheduler/cron job fires an actual unattended run at least once, fully traced.

### Phase 5 — Story/Task Breakdown (PM decomposition)

**Depends on: Phase 0–4 complete.** Primary risk retired here: cross-run identity/drift correctness and unattended scheduled-run reliability, plus a gate policy the TRD never specified — that policy is promoted to its own blocking task rather than left implicit inside the scheduler task.

**Story 5.0 — Phase-5 library additions**
Acceptance: `pyproject.toml` gains `apscheduler`, `uv sync` succeeds.
- [ ] Task 5.0.1: `uv add apscheduler`.
      Acceptance: package present; `uv sync` exits 0.
      Depends on: Phase 0 Task 0.1.1

**Story 5.1 — Topic canonicalization and identity hashing**
Acceptance: `persistence/topic_identity.py` canonicalizes (lowercase, whitespace-normalize, optional LLM-normalize) and hashes a topic string, keying the `topics` table so trivial rephrasings resolve to the same tracked topic (FR-21).
- [ ] Task 5.1.1 (orig. Task 1): canonicalization → hash pipeline.
      Acceptance: unit test confirms `"Ukraine War"`, `" ukraine war "`, and an LLM-normalized rephrasing all hash identically — one of the plan's explicitly unit-testable-now items.
      Depends on: none

**Story 5.2 — Gate policy for scheduled/unattended runs is decided and implemented (open design decision)**
Acceptance: an explicit, documented policy exists for what happens when Gate 1/2 can't be cleared unattended (auto-approve vs. fail-and-notify) — the TRD/plan does not resolve this, so it must be decided here, not implicitly during coding.
- [ ] Task 5.2.1 (blocking open decision, named in orig. Task 2's text and the "Flagged open decisions" summary but not previously its own task): decide and record the gate policy (auto-approve vs. fail-and-notify) for scheduled reruns.
      Acceptance: the decision is recorded in this document (or a config default) before Task 5.2.2 begins; it names one of the two options explicitly, not left ambiguous.
      Depends on: none — must precede 5.2.2
- [ ] Task 5.2.2 (orig. Task 2): scheduler entrypoint — looks up due topics by cadence, invokes the full graph headlessly, applying the Task 5.2.1 policy at Gate 1/2.
      Acceptance: a due topic runs headlessly through both gates per the decided policy with zero interactive stdin/UI involvement.
      Depends on: 5.2.1, 5.1.1, Phase 2 Task 2.3.1, Phase 2 Task 2.6.2

**Story 5.3 — Cross-run subtopic matching and drift computation**
Acceptance: `clustering/subtopic_matching.py` matches current vs. prior subtopic embeddings by cosine similarity against `Settings.clustering.subtopic_match_threshold`; `agents/timeline_agent.py` computes drift (sentiment shift, volume change, new/dropped claim clusters) for matched and unmatched subtopics.
- [ ] Task 5.3.1 (orig. Task 3, part a): `subtopic_matching.py` — cosine-similarity matcher against the `Settings`-sourced threshold.
      Acceptance: given fixed embedding vectors for a matched pair and an unmatched pair, correctly classifies each with no live LLM call — one of the plan's explicitly unit-testable-now items (subtopic cross-run matching math).
      Depends on: Phase 2 Task 2.1.1, Phase 0 Task 0.3.1
- [ ] Task 5.3.2 (orig. Task 3, part b): `agents/timeline_agent.py` — triggered only on scheduled non-first runs for a topic hash; fetches prior runs, computes drift.
      Acceptance: given two runs for the same topic hash with one deliberately-varied subtopic, the drift report correctly separates matched-continuing subtopics from the newly-emerged one, with sentiment/volume deltas attached.
      Depends on: 5.3.1, 5.1.1

**Story 5.4 — Trend report persisted as part of the run artifact**
Acceptance: `subtopic_matches` + a trend report are persisted and included in the run's assembled artifact.
- [ ] Task 5.4.1 (orig. Task 4): persist `subtopic_matches` table + trend report attached to the run artifact (extends Phase 4's snapshot assembly).
      Acceptance: `testcontainers`-backed test confirms `subtopic_matches` rows exist per matched pair and the trend report is retrievable alongside the Phase 4 snapshot for the same `run_id`.
      Depends on: 5.3.2, Phase 4 Task 4.5.1

**Story 5.5 — NFR-3 graceful degradation re-verified under the unattended path**
Acceptance: a simulated Google News RSS failure, and a simulated GDELT failure (Phase 1 Task 1.12.1), during an actual scheduled (not interactive) run both degrade gracefully — the same guarantees as Phase 1, now proven headless.
- [ ] Task 5.5.1 (orig. Task 5): force/block `google_news_backfill` during a real scheduled-run invocation and confirm no crash, primary-source results still returned.
      Acceptance: identical guarantee to Phase 1 Task 1.11.2, re-run specifically through the Task 5.2.2 scheduler entrypoint rather than the interactive CLI path.
      Depends on: 5.2.2, Phase 1 Task 1.8.1
- [ ] Task 5.5.2 (added per Phase 1 Task 1.12.2): force/simulate a `GDELTError` during a real scheduled-run invocation and confirm no crash, RSS(+backfill-if-triggered)-only results still returned.
      Acceptance: identical guarantee to Phase 1 Task 1.12.1's soft-fail behavior, re-run specifically through the Task 5.2.2 scheduler entrypoint rather than the interactive CLI path — so an unattended run landing during a GDELT rate-limit window degrades gracefully rather than failing the whole scheduled run.
      Depends on: 5.2.2, Phase 1 Task 1.12.1

**Story 5.6 — Observability holds under unattended runs**
Acceptance: a scheduled run with no human watching still produces Langfuse traces and an MLflow run, identical in completeness to an interactive run.
- [ ] Task 5.6.1 (orig. Task 6): confirm the Phase 0 Story 0.7 callback wiring is attached identically in the scheduler entrypoint's `graph.invoke()` call, not just `cli.py`'s.
      Acceptance: an unattended scheduled run produces the same three observability artifacts (a `run_costs` row, a Langfuse trace, an MLflow run) as Phase 0 Task 0.7.4's interactive-path check.
      Depends on: 5.2.2, Phase 0 Task 0.7.4

**Story 5.7 — Phase 5 Done-when verified end-to-end (integration/demo)**
Acceptance: matches the phase-level Done-when block above.
- [ ] Task 5.7.1: simulated second scheduled run of the same topic — confirm unchanged subtopics matched, a deliberately-varied one flagged as newly emerged, a sane drift report produced.
      Depends on: 5.4.1
- [ ] Task 5.7.2: at least one real APScheduler/cron job fires an actual unattended run, fully traced end-to-end.
      Depends on: 5.6.1, 5.2.2

### Phase 5 — Role Ownership & Parallelization Re-analysis (`project-manager`, 2026-07-29)

**Why this exists:** Phase 5 is the first phase where `devops-engineer` has real, non-Docker-Compose scope again (unattended scheduled-run process supervision, explicitly named in their scope: "scheduler/unattended-run process supervision (relevant for Phase 5's scheduling story)") — Phases 2–4 had none. It's also the first phase with a named, still-open architectural decision already sitting in the stub text (Story 5.2's gate policy) rather than one this re-analysis has to surface itself. Nothing below changes an existing task's acceptance criterion except where marked as a gap-fix.

**`frontend-engineer`: zero ownership in Phase 5.** No new UI surface — scheduled runs are headless by definition. Same citation as prior phases.

**Ownership annotation, story by story:**

| Story/Task | Owner | Rationale / handoff |
|---|---|---|
| 5.0 (lib add) | `backend-engineer` | Trivial `uv add apscheduler`. |
| 5.1.1 topic canonicalization + hash | **split, DS→BE** (proposed 5.1.1a/5.1.1b) — **gap found and fixed** | The stub's acceptance criterion ("`... ukraine war ...`, and an LLM-normalized rephrasing all hash identically") requires an actual LLM call for the rephrasing case, but no model stage for it exists: `Settings.models`' five named stages (`subtopic`, `claim_extraction`, `summarization`, `bias_framing`, `briefing`) don't obviously fit "topic normalization," and model-selection-per-stage is explicitly `data-scientist` scope. This needed a decision before it could be honestly assigned to `backend-engineer` alone — split accordingly. |
| 5.2.1 gate policy decision | `tech-lead` | This is an architecture/open-decision resolution (auto-approve vs. fail-and-notify for unattended gates), not an implementation task — squarely `tech-lead`'s scope ("resolves flagged open decisions"), not `backend-engineer`'s to decide while coding 5.2.2. The stub already names this correctly as its own blocking task (good — confirmed adequate, no companion note needed beyond this explicit ownership assignment); it's carried forward here, not re-litigated. |
| 5.2.2 scheduler entrypoint | `backend-engineer` | Implements the decided policy; no design content of its own once 5.2.1 resolves. |
| 5.2.3 (**new task**, see below) unattended process supervision | `devops-engineer` | The stub's Tasks list only specifies *what* the scheduler entrypoint does (look up due topics, invoke the graph); nothing names *what actually invokes it on a schedule without a human* — cron entry, systemd timer/unit, or an APScheduler-as-long-running-daemon process, plus what happens if that process crashes. This is exactly the gap the instructions asked to check for by name ("Phase 5's scheduling story needing a devops-owned 'get the scheduler running unattended' task that isn't yet named") — it wasn't named, so it's added here as Task 5.2.3, continuing this phase's own numbering. |
| 5.3.1 subtopic matching | **split, DS→BE** (proposed 5.3.1a/5.3.1b) — **gap found and fixed** | `Settings.clustering.subtopic_match_threshold`'s concrete value is explicitly still open per TRD §9's own listed open question ("Concrete similarity threshold for cross-run subtopic matching — needs tuning once real run-to-run data exists") — the same class of empirical-threshold decision as Phase 2's reconciliation thresholds (Task 2.2.3a), which got its own `data-scientist` design task there. The original Task 5.3.1 (part a) bundled "given fixed embedding vectors... correctly classifies each" as if the threshold were already decided, with no task actually deciding it — fixed by splitting design from wiring, matching the Phase 2 precedent this phase didn't originally carry forward. |
| 5.3.2 timeline agent drift computation | `backend-engineer` | Deterministic diff/aggregation logic (sentiment/volume deltas, new/dropped cluster sets) over already-computed inputs (Phase 3's per-article sentiment, Phase 3/4's cluster/claim data) — no new DS decision needed here. |
| 5.4.1 persistence | `backend-engineer` | Deterministic schema/write path. |
| 5.5.1 / 5.5.2 NFR-3 reverification | `backend-engineer` | Mirrors the Phase 1 pattern this phase explicitly re-verifies (Task 1.11.2 / Task 1.12.1) under the scheduled path; no new design content. |
| 5.6.1 observability under unattended runs | `backend-engineer` | Confirms existing Phase 0 wiring is attached identically in a second entrypoint — no new design. |
| 5.7.1 / 5.7.2 Done-when | `acceptance-verifier` | Unlike Phase 2/3/4, Phase 5's Done-when criteria are fully mechanical/deterministic (matching-correctness against a deliberately-varied test case, a real scheduler firing once) — no open-ended quality judgment requiring a `data-scientist` co-sign here. |

**Proposed task splits / additions (not yet adopted):**
- **Task 5.1.1a** (`data-scientist`): decide whether the optional LLM-normalize step is needed at all for realistic rephrasings (vs. lowercase/whitespace-normalize alone being sufficient for FR-21's stated cases), and if needed, which model stage funds it — reuse an existing cheap stage (e.g. `claim_extraction`'s small model) or propose a new `Settings.models.topic_normalization` stage. Acceptance: a documented recommendation with a few real rephrasing examples showing the chosen approach resolves to the same canonical form.
  Depends on: none
- **Task 5.1.1b** (`backend-engineer`, was Task 5.1.1): wire the canonicalization → hash pipeline per 5.1.1a's recommendation. Acceptance: unchanged from the original.
  Depends on: 5.1.1a
- **Task 5.2.3** (`devops-engineer`, new): stand up actual unattended process supervision for Task 5.2.2's scheduler entrypoint — a cron entry, or a systemd timer/unit, or a supervised long-running APScheduler daemon process (`devops-engineer`'s call, documented in README once chosen), including crash/restart behavior so a due topic isn't silently skipped if the process dies mid-run. Acceptance: with no human invoking anything, a due topic's scheduled run actually fires at its scheduled time and completes (or is retried per the documented restart policy) — this is the literal precondition for Task 5.7.2's "a real APScheduler/cron job fires an actual unattended run" to be true at all, not merely for the entrypoint function to exist.
  Depends on: 5.2.2
- **Task 5.3.1a** (`data-scientist`): pick and validate `Settings.clustering.subtopic_match_threshold` against a constructed or real cross-run embedding-pair fixture (matched pair vs. deliberately-distinct pair), same methodology as Phase 2 Task 2.2.3a's reconciliation-threshold design. Acceptance: documented recommendation + threshold value + the fixture used to derive it, committed under `notebooks/`.
  Depends on: Phase 2 Task 2.1.1 (embeddings wrapper)
- **Task 5.3.1b** (`backend-engineer`, was Task 5.3.1 part a): wire the cosine-similarity matcher against 5.3.1a's threshold. Acceptance: unchanged from the original.
  Depends on: 5.3.1a, Phase 0 Task 0.3.1

**Parallelization plan (waves):**
- **Wave 1 (parallel, no cross-track dependency):** `backend-engineer`: Task 5.0.1 (lib add). `tech-lead`: Task 5.2.1 (gate policy decision — no dependency, should happen as early as possible since 5.2.2 is blocked on it). `data-scientist`: Task 5.1.1a (canonicalization LLM-step decision) and Task 5.3.1a (match-threshold tuning, can reuse Phase 2's already-existing embeddings/fixtures) in parallel with each other. `devops-engineer`: can begin researching/scaffolding the process-supervision approach (cron vs. systemd vs. daemon) without needing real code yet.
- **Wave 2:** `backend-engineer`: Task 5.1.1b (needs 5.1.1a), Task 5.3.1b (needs 5.3.1a).
- **Wave 3:** `backend-engineer`: Task 5.2.2 (needs 5.2.1 + 5.1.1b), Task 5.3.2 (needs 5.3.1b).
- **Wave 4:** `devops-engineer`: Task 5.2.3 (needs a real 5.2.2 entrypoint to supervise). `backend-engineer`: Task 5.4.1 (needs 5.3.2 + Phase 4 Task 4.5.1).
- **Wave 5:** `backend-engineer`: Task 5.5.1, Task 5.5.2, Task 5.6.1 (all need 5.2.2 actually running).
- **Wave 6 (integration):** `acceptance-verifier`: Story 5.7 Done-when, including confirming Task 5.2.3's supervision actually fired a real unattended run.

**Gaps flagged, not silently dropped:** two real gaps found beyond the already-correctly-flagged gate-policy decision: (1) Task 5.1.1's optional LLM-normalize step had no assigned model stage — the same "which stage funds this call" question the Cross-Cutting Concerns config system exists to answer explicitly, left implicit here; fixed via the 5.1.1a/b split. (2) Task 5.3.1's match threshold was treated as a given constant despite TRD §9 itself listing it as an unresolved open question needing empirical tuning — the same class of gap Phase 2's re-analysis fixed for reconciliation thresholds, not carried forward into this phase's original breakdown; fixed via the 5.3.1a/b split. (3) No task anywhere in the original Phase 5 breakdown named the actual unattended-invocation mechanism (cron/systemd/daemon) despite `devops-engineer`'s own scope note flagging this as relevant to Phase 5 — added as new Task 5.2.3.

**Flagged architectural open question for `tech-lead`:** Story 5.2's gate-policy decision (auto-approve vs. fail-and-notify for unattended Gate 1/2) is already correctly flagged in the existing stub text and in the "Flagged open decisions" summary at the end of this document — confirmed adequate as-is, no companion note needed. It remains the single most load-bearing open call for this phase: it blocks Task 5.2.2 directly, and by extension every downstream task in Waves 3–6 that needs a real scheduler entrypoint to test against.

---

## Phase 6 — Streamlit + Jupyter Front Ends

**Modules:**
- `app/streamlit_app.py` — topic input, Gate 1/2 interactive UI, snapshot/briefing/timeline views, calling `newsresearch.graph` + `newsresearch.persistence` only (no pipeline logic in the UI layer).
- `notebooks/exploration.ipynb` — same core module, exploratory/ad hoc charting.

**Libraries:** `streamlit`, `jupyter`/`ipykernel`.

**Tasks:**
1. Streamlit: topic form → invoke graph to Gate 1 → render/approve/edit → resume → Gate 2 per subtopic → resume → final views.
2. State handling: Streamlit re-executes the script per interaction — rely on the `PostgresSaver` checkpointer + `run_id` lookup, not `st.session_state` alone, so gate state is durable (this is exactly why Phase 0 chose a Postgres-backed saver over an in-memory one).
3. Jupyter notebook: thin wrapper over the same graph/core functions, auto-approve or manual cell-stepping through gates, charting over `articles`/`claim_clusters`/`briefings`.
4. Add a link/deep-link from the Streamlit run view to the corresponding Langfuse trace (`run_id`-tagged) for one-click debugging during operation, not just during development.

**Done when:** a full run is executable start-to-finish through Streamlit including both gates; the same run is inspectable from the notebook against the same Postgres database; the Langfuse trace for that run is reachable from the Streamlit UI.

### Phase 6 — Story/Task Breakdown (PM decomposition)

**Depends on: Phase 0–5 complete.** Usability only — deliberately last, no new pipeline logic; both front ends must call `newsresearch.graph` + `newsresearch.persistence` only, never reimplement pipeline behavior in the UI layer.

**Story 6.0 — Phase-6 library additions**
Acceptance: `pyproject.toml` gains `streamlit`, `jupyter`/`ipykernel`, `uv sync` succeeds.
- [ ] Task 6.0.1: `uv add streamlit jupyter ipykernel`.
      Acceptance: all three present; `uv sync` exits 0.
      Depends on: Phase 0 Task 0.1.1

**Story 6.1 — Streamlit UI drives a full run through both gates, calling only graph + persistence**
Acceptance: `app/streamlit_app.py`'s topic form invokes the graph through Gate 1 approve/edit, resumes to per-subtopic Gate 2, resumes again to final views — with zero pipeline logic living in the UI layer.
- [ ] Task 6.1.1 (orig. Task 1): Streamlit flow — topic form → Gate 1 render/approve/edit → resume → Gate 2 per subtopic → resume → final views.
      Acceptance: a manual walkthrough completes a real topic run through both gates in the browser; an import/code-review check confirms `streamlit_app.py` contains no direct sourcing/clustering/agent logic, only `graph.invoke`/`Command(resume=...)` calls and persistence reads.
      Depends on: Phase 2 Task 2.3.1, Phase 2 Task 2.6.2, Phase 4 Task 4.5.1
- [ ] Task 6.1.2 (orig. Task 2): durable gate-state handling via the `PostgresSaver` checkpointer + `run_id` lookup, not `st.session_state` alone.
      Acceptance: reloading the Streamlit page mid-gate (simulating its per-interaction script re-execution) still resumes correctly from Postgres, not from lost in-memory session state.
      Depends on: 6.1.1, Phase 0 Task 0.5.2

**Story 6.2 — Jupyter notebook provides exploratory access to the same core**
Acceptance: `notebooks/exploration.ipynb` wraps the same graph/core functions as the Streamlit app, supports auto-approve or manual cell-stepping through gates, and charts over `articles`/`claim_clusters`/`briefings` from the same Postgres database.
- [ ] Task 6.2.1 (orig. Task 3): notebook wrapper — auto-approve or manual cell-stepping, charting.
      Acceptance: running the notebook against the same Postgres instance as a prior Streamlit run reproduces/inspects that run's data (the same `run_id` queryable from both).
      Depends on: 6.1.1

**Story 6.3 — One-click Langfuse trace access from the Streamlit run view**
Acceptance: a link/deep-link from the Streamlit run view opens the corresponding `run_id`-tagged Langfuse trace.
- [ ] Task 6.3.1 (orig. Task 4): deep-link from the run view to the Langfuse trace URL.
      Acceptance: clicking the link from a completed run's Streamlit view opens the correct trace at `localhost:3000` for that exact `run_id`.
      Depends on: 6.1.1, Phase 0 Task 0.7.2

**Story 6.4 — Phase 6 Done-when verified end-to-end (integration/demo)**
Acceptance: matches the phase-level Done-when block above.
- [ ] Task 6.4.1: full run executed start-to-finish through Streamlit including both gates.
      Depends on: 6.1.2
- [ ] Task 6.4.2: the same run inspected from the notebook against the same Postgres database.
      Depends on: 6.2.1
- [ ] Task 6.4.3: Langfuse trace for that run reachable from the Streamlit UI.
      Depends on: 6.3.1

### Phase 6 — Role Ownership & Parallelization Re-analysis (`project-manager`, 2026-07-29)

**Why this exists:** Phase 6 is `frontend-engineer`'s only phase with real ownership in this entire plan — every other phase explicitly has zero `frontend-engineer` scope (see each phase's own re-analysis above and the Cross-Cutting Concerns CLI-harness paragraph). It's also the first phase where `user-guide`'s "refresh README after a wave lands" role has an obvious, concrete trigger: the throwaway CLI dev harness is explicitly superseded here. Nothing below changes an existing task's acceptance criterion except where marked as a gap-fix.

**`data-scientist`: zero *new-decision* ownership in Phase 6.** No new prompts, models, or clustering/matching logic are introduced — Phase 6 is explicitly "usability only... no new pipeline logic" per this phase's own header and the Suggested Build Order Summary table. Naming this explicitly, not silently omitting it, mirrors how Phase 2's re-analysis handled `frontend-engineer`'s zero ownership there.

**Ownership annotation, story by story:**

| Story/Task | Owner | Rationale / handoff |
|---|---|---|
| 6.0.1 (lib add) | `backend-engineer` | Same shape as every other phase's trivial `uv add` task — a dependency addition, not UI authorship, even though the libraries (`streamlit`, `jupyter`, `ipykernel`) are UI-facing. |
| 6.0.2 (**new task**, see below) persistence read-helpers for every front-end-needed table | `backend-engineer` | **Gap found and fixed** — see below. Blocks Task 6.1.1. |
| 6.1.1 Streamlit flow (topic form → gates → resume → views) | `frontend-engineer` | Core Phase 6 UI work, explicitly this role's scope. |
| 6.1.2 durable gate-state via `PostgresSaver` + `run_id` lookup | `frontend-engineer` | The underlying durability guarantee (`PostgresSaver` survives process restarts) is Phase 0's already-proven work, reused, not new backend design — `frontend-engineer` owns wiring the Streamlit-side read/lookup against it, matching their stated scope ("Gate 1/2 UI"). |
| 6.2.1 Jupyter notebook | `frontend-engineer` | Explicitly named in their scope ("exploratory notebook charting"). |
| 6.3.1 Langfuse deep-link | `frontend-engineer` | UI-layer link construction; the trace itself already exists per Phase 0's Langfuse wiring. |
| 6.4.1 / 6.4.2 / 6.4.3 Done-when | `acceptance-verifier` | Fully mechanical here (run completes through both gates, notebook reads the same `run_id`, link resolves to the correct trace) — no open-ended quality judgment needed, same shape as Phase 5's Done-when. |
| README refresh (**new task**, see below) | `user-guide` | Framed as "refresh docs after a wave lands," not implementation — per this role's stated scope. |

**Proposed task addition (not yet adopted):**
- **Task 6.0.2** (`backend-engineer`, new): audit and, where missing, add typed read helpers in `persistence/queries.py` for every table a front end needs to display — `runs`, `subtopics`, `articles`, `claim_clusters`, `claim_cluster_articles`, `briefings`, `subtopic_matches`, `run_costs`. **Gap found and fixed:** Task 6.1.1's own acceptance criterion requires an import/code-review check confirming `streamlit_app.py` "contains no direct sourcing/clustering/agent logic, only `graph.invoke`/`Command(resume=...)` calls and persistence reads" — but no task in Phases 3, 4, or 5 explicitly committed to building a *typed read* helper per table in `persistence/queries.py` (their acceptance criteria only required a write path plus a `testcontainers` write-then-read-back test using ad hoc queries inside the test itself, not a reusable, importable reader function). Without this task, `frontend-engineer` would either have to write raw SQL directly in `streamlit_app.py`/`exploration.ipynb` (violating Task 6.1.1's own acceptance line and this codebase's stated "no raw SQL scattered in agents" convention from `persistence/queries.py`'s module description) or silently absorb backend-design work outside their stated scope. Acceptance: for each named table, a typed function exists in `persistence/queries.py` and returns real data from a `testcontainers`-backed integration test; Task 6.1.1's frontend code can import and call these directly with zero raw SQL.
  Depends on: Phase 3 Task 3.5.1, Phase 4 Task 4.4.1/4.5.1, Phase 5 Task 5.4.1 (the tables/columns these tasks' write paths actually produced)
- **Task 6.5.1** (`user-guide`, new): once Story 6.1/6.2 land, refresh README's "Commands"/usage section to document `streamlit run app/streamlit_app.py` and the Jupyter notebook as the real front ends, explicitly noting the `uv run newsresearch run "<topic>"` CLI path is retained as a dev/debug harness (per its own documented "throwaway-quality UX" status in Cross-Cutting Concerns) rather than removed. Acceptance: README's usage instructions match the actual post-Phase-6 repo state, reviewable by a new contributor without needing to read this execution plan.
  Depends on: 6.1.1, 6.2.1

**Parallelization plan (waves):**
- **Wave 1 (parallel):** `backend-engineer`: Task 6.0.1 (lib add), Task 6.0.2 (read-helper gap-fix — no dependency on any Phase 6 UI work, only on Phases 3–5's already-existing write paths). `frontend-engineer`: no Phase-6-real-data task can start yet without 6.0.2; static UI scaffolding/layout work against mock data is possible but not this story's actual acceptance criterion.
- **Wave 2:** `frontend-engineer`: Task 6.1.1 (needs 6.0.2 + the already-complete Phase 2/4 gate/snapshot deliverables).
- **Wave 3:** `frontend-engineer`: Task 6.1.2 and Task 6.3.1 in parallel with each other (both depend only on 6.1.1, not on each other); Task 6.2.1 can also run in this wave in parallel (depends only on 6.1.1's underlying graph/persistence wiring, not on 6.1.2's session-state polish or 6.3.1's deep-link).
- **Wave 4 (integration):** `acceptance-verifier`: Story 6.4 Done-when. `user-guide`: Task 6.5.1, in parallel with Wave 4's verification (README accuracy doesn't gate the Done-when demo itself).

**Gaps flagged, not silently dropped:** the persistence-read-helper gap (Task 6.0.2) is the substantive one — every earlier phase's persistence task was written to prove a write path exists via an ad hoc read-back inside its own test, not to guarantee a reusable typed reader function exists for a future caller; Phase 6 is the first phase whose own acceptance criteria assume that reusable reader layer is already there. Fixed by adding an explicit `backend-engineer` task ahead of `frontend-engineer`'s work, rather than letting the gap surface mid-implementation as either a raw-SQL violation or scope creep into backend design.

**Flagged architectural open question for `tech-lead` before Phase 6 starts:** how should the Streamlit UI represent and drive resume for **N concurrently-interrupted Gate 2 branches** within Streamlit's rerun-per-interaction execution model? The CLI's own solution (Task 2.7.1, PR #38) works by blocking on synchronous stdin prompts in a `while "__interrupt__" in result` loop and dispatching resume payloads by shape (`_resume_payloads`) — a model with no direct Streamlit equivalent, since Streamlit reruns the whole script per widget interaction rather than blocking on input mid-execution. Task 6.1.2's acceptance criterion ("reloading the Streamlit page mid-gate... still resumes correctly from Postgres") covers *durability* but not *how a single Streamlit session displays and collects approve/edit decisions for several concurrently-pending Gate 2 branches at once* — this is a real UI/state-management design question, not a polish detail, and both Task 6.1.1's and Task 6.1.2's `frontend-engineer` work are shaped by whichever answer `tech-lead` picks (e.g. one form per pending subtopic on the same page vs. a per-subtopic sub-view/tab model).

---

## Suggested Build Order Summary

| Phase | Depends on | Primary risk being retired |
|---|---|---|
| 0 | — | Nothing exists yet; every later phase blocked without it — now includes standing up Docker Compose infra (Postgres + Langfuse) alongside the code scaffold |
| 1 | 0 | Source quality/coverage, and feasibility of two reputation signals (WHOIS, backlink proxy) |
| 2 | 0, 1 | Human-gate UX (interrupt/resume durability against Postgres) + clustering quality |
| 3 | 0–2 | Claim-cluster quality — foundation for bias/briefing |
| 4 | 0–3 | Bias/framing + briefing quality — highest value, least automatable validation |
| 5 | 0–4 | Cross-run identity/drift correctness, unattended scheduled-run reliability, gate policy for headless runs |
| 6 | 0–5 | Usability only — deliberately last, no new pipeline logic |

**Flagged open decisions to resolve before/during the relevant phase:**
- ~~Phase 1: concrete backlink-proxy data source~~ **RESOLVED**: Tranco top-100k snapshot at `newsresearch/data/tranco_top100k.csv`, log-scale normalization, neutral 0.5 on absence — see Story 1.6's decision note.
- Phase 5: gate policy for scheduled/unattended runs (auto-approve vs. fail-and-notify) — the TRD doesn't specify this; needs a decision, not just implementation.
- Future (not v1): `pgvector` for in-database embedding similarity search — a natural fit now that the app is on Postgres, but not adopted now; the plan keeps the original Python/numpy-side similarity computation to avoid re-architecting clustering/matching beyond what was asked.
- **[NEW, raised 2026-07-23, PM analysis pending user decision] GDELT DOC 2.0 as the primary sourcing provider.** This session surfaced real operational pain (sustained-use rate-limiting, a since-fixed query-grammar bug that masqueraded as rate-limiting, 10+ minute live-verification runs, and `sourcing_agent`'s current hard-fail treatment of GDELT). Three options on the table: (1) keep GDELT as-is; (2) keep GDELT but harden it to soft-fail (see the proposed Story 1.12 above — small, contained, recommended regardless of how (3) resolves); (3) replace GDELT with a different provider or drop it for RSS(+backfill)-only. `tech-lead` is running the technical comparison for option (3) separately (paid APIs — NewsAPI/GNews/Bing News Search/Mediastack/Currents — vs. RSS-only) in parallel; this entry tracks the decision itself, not the technical analysis. **Load-bearing constraint for option (3): PRD.md line 107 ("Sourcing must remain free of paid data licenses") and PRD goal #7 ("cheap/free to source news from") are explicit, already-approved requirements — any paid-API replacement is a PRD change, not just a code change, and needs to be raised as such, not folded in silently as a "sourcing swap."** An RSS-only drop-GDELT path stays PRD-compliant but has its own real consequence worth deciding explicitly rather than discovering later: it would make Google News backfill practically load-bearing far more often (RSS-only primary counts will frequently fall under `Settings.sourcing.min_primary_article_count`), which cuts against NFR-3's and `google_news_backfill.py`'s own stated framing of backfill as non-load-bearing — and it would need `TRD.md`'s sourcing row/diagram (which name GDELT explicitly) updated too, since TRD is a source-of-truth document, not just an implementation detail. PM recommendation: land Story 1.12 (soft-fail hardening) now, early in Phase 2, independent of this larger decision; treat the provider-replacement/removal question as its own decision on `tech-lead`'s technical findings, not blocking Phase 2's start.